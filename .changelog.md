
## v5.88.0 - Hole 17: All Power to the Ball
- **Course Data (`course_pasture.js`):** Added Hole 17. A 370-yard Par 4 featuring the "Magnetic Gate" at the 250-yard mark. The `towers` array injects two 15-yard magnetic fields, leaving a strict 16-yard safe gap in the center of the fairway.
- **Dynamic Ambient Audio (`physics_core.js` & `physics_collisions.js`):** Programmed contextual audio hot-swapping for Hole 17. The `am_farm1_voltage.mp3` ambient track plays off the tee. If the player gets zapped by a tower, or if the ball safely lands past the 250-yard depth, the engine dynamically swaps the track to `am_farm1.mp3` for the remainder of the hole.
## v5.86.1 - Hole 16 Physics Hotfix
- **Topography Interceptors (`physics_collisions.js`):** Corrected an omission where Hole 16's strict geometric penalties were skipped. The Crowned Fairway now aggressively sheds off-center drives into the rough, and the Spined Green funnels offline approaches directly into the bunkers.
## v5.86.0 - Hole 16: Marquis de Gesundheit
- **Course Data (`course_pasture.js` & `data_ag.js`):** Added Hole 16, a 432-yard Par 4 featuring a tight 12-yard wide spined green guarded by bunkers, and added "The Marquis' Spine" to the Green Dictionary.
- **Topography Interceptors (`physics_collisions.js`):** Engineered strict geometric penalties for Hole 16. Shots missing the dead-center of the "Crowned Fairway" are automatically kicked into the rough. Approaches missing the center of the green hit a "Vicious Spine" and are forcibly funneled into the left or right greenside bunkers.
- **Psychological Warfare (`audio_core.js` & `input_ag.js`):** Implemented a new downswing interceptor. The Marquis now triggers sneezes, coughs, and party poppers (`marquis_interrupt1-3.mp3`) at the exact millisecond the player initiates their downswing, mirroring the psychological hazards of Hole 7.
## v5.85.0 - Default Music Volume Adjustment
- **Audio Mixing (`main_ag.js`):** Lowered the default background music volume from 20% to 10% by changing `window.musicVolumeIndex` from 3 to 2. This ensures the music sits further back in the mix by default, giving the Caddy TTS and spatial sound effects more breathing room.
## v5.84.4 - Acorn Dome TDZ Crash Fix
- **Variable Hoisting (`physics_core.js`):** Fixed a fatal Temporal Dead Zone (TDZ) crash on Hole 15. The v5.84.3 Acorn Dome Interceptor was attempting to append text to `flightPathNarrative` before the variable was lexically declared, causing the shot calculation to silently fail and trigger the 20-second watchdog reset. Hoisted the variable declaration above the interceptor to restore stability.
## v5.84.3 - Acorn Dome Physics Desync Fix
- **Mid-Flight Math:** Relocated the Hole 15 Acorn Dome interceptor in `physics_core.js`. Previously, it was injected too late in the physics pipeline, penalizing the carry/roll variables *after* the ball's final X/Y coordinates and total distance were already locked in. It now executes before the Absolute Coordinate Resync, ensuring the ball physically drops out of the sky.
- **Float Truncation:** Wrapped the penalized distance calculations in `Math.round()` to prevent 14-decimal floating-point values in the telemetry readout.
- **Audio Routing Fix:** The missile directional audio now uses `lateralX` (true offline dispersion) instead of the absolute map coordinate `ballX`, ensuring the audio panner accurately reflects the ball's trajectory relative to the target line.
## v5.84.2 - Acorn Dome Audio Sync
- **Missile Timing:** Adjusted the audio trigger delay for Hole 15's missile interceptor in `physics_core.js`. Shifted the trigger from 50% of the hang time to 20% (`hangTimeSecs * 0.2`), ensuring the pre-recorded missile whistle and explosion resolve before the physical ball bounce occurs.
## v5.84.1 - Dr. Bobby's Bus Hotfix
- **Physics Collisions:** Corrected a rogue implementation of Dr. Bobby's Bus. Shots striking the bus now correctly result in a "Rough" lie, apply a 5-yard kickback penalty (`ballY -= 5`), and trigger the proper "THUD!" narrative.
## v5.84.0 - Nut Jobs & Hole 14 Physics Patch
- **Course Data:** Added Hole 15 "Nut Jobs" to `course_pasture.js`. A 232-yard Par 4 heavily guarded by trees and a forced gusty wind parameter.
- **The Acorn Dome:** Implemented a mid-flight interceptor in `physics_core.js`. On Hole 15, shots apexing higher than 6.66 yards (20 feet) trigger a missile event, crippling distance by 40-50%.
- **X-Coordinate Audio Router:** The missile event routes to 1 of 5 pre-panned audio files based on `finalX` lateral dispersion.
- **Hole 14 Physics Sync:** Updated `physics_collisions.js` to officially recognize the "Toxic Moat" as a deadly water hazard and added a solid wall interceptor for "Dr. Bobby's Bus".
## v5.83.0 - Drip, Drip, Hooray
Added Hole 14 to The Pasture. Introduced a sensory distraction mechanic utilizing shifting ambient audio (bus idle and rhythmic dripping). Constructed a 3-tier elevated green surrounded entirely by a "Toxic Moat" bounding box.
## v5.82.3 - The Choke Point
Patched Hole 13 ("The Tower Heist"). Drastically widened the "Tower Runoff" stream to eat the left side of the fairway, and moved the "Inner Canopy" trees directly onto the right edge. The driver gap at 240 yards has been choked to zero, forcing players to hit a 3-Wood layup or take the Tower shortcut.
## v5.82.2 - The Inner Canopy
Patched Hole 13 ("The Tower Heist"). Added physical tree objects to the inside corner of the dogleg to ensure corner-cutting draws and fades are properly intercepted by the 3D physics engine.
## v5.82.1 - The Sherwood Blockade
Patched Hole 13 ("The Tower Heist"). Converted the static "Tree Cluster" hazard into physical `trees` array objects to enable proper acoustic collisions and block the wide-right 10-degree slice exploit.
## v5.82.0 - Tower Heist Telemetry & Overlap Fix
- **Double-Strike Prevention:** Added a `!rollStopTriggered` guard to the Water Tower interceptor in `physics_collisions.js` to prevent the overlapping Catch-All bounding box from firing a duplicate audio and narrative event.
- **Warp Telemetry Sync:** The Water Tower interceptor now dynamically recalculates `totalDistance` to match the warped coordinates, and injects a definitive "Caddy Override" string into the flight narrative to correct the stale proximity readout for screen reader users.
## v5.81.0 - Airborne Hazard Immunity Fix
- **Physics Engine Core:** Fixed a massive structural flaw in `physics_collisions.js` where hazard CARRY and ROLL checks were chained as mutually exclusive `if/else` blocks. Previously, carrying a ball directly into a custom interceptor (like the Water Tower, Fences, or Drains) bypassed the interceptor logic entirely.
- **Bunker Roll-Out Fix:** Unifying the collision logic also fixed a day-one bug where balls carrying directly into a Bunker failed to trigger `rollStopTriggered`, allowing them to roll through the sand like a fairway.
## v5.80.0 - Hole 13: The Tower Heist
- **Course Data:** Added Hole 13 to `course_pasture.js`. A 420-yard Par 4 featuring a 3-stage ambient audio progression (`water_tower` -> `stream1` -> `farm1`).
- **The Catch-All Backboard:** Leveraged the `(Spectating)` UI filter to deploy a massive, invisible 360-yard wide AABB bounding box on the left side of the hole. Shots striking this area trigger the metallic grab-bag audio and warp the ball to the 110-yard fairway sweet spot.
- **Dynamic Water Hazards:** Expanded the `inWater` boolean logic in `physics_collisions.js` to natively recognize "Tower Runoff" and "The Spillway" as deadly water hazards.
## v5.79.0 - Auto-Equip Lie Multiplier Sync
- **Auto-Equip Brain:** Fixed a severe club-selection bug in `window.autoEquipBestClub` inside `physics_core.js`. The function previously defaulted to a `1.0` lie multiplier for Mud, Manure, Pine Needles, and Packed Earth, causing it to drastically under-club players in hazards. It now uses the comprehensive lie evaluation block (matching the Oracle Caddy), accurately forcing players to club up when buried in deep Manure or Mud.
## v5.78.0 - Mud/Manure TDZ Crash Fix
- **Physics Engine Core:** Fixed a fatal `ReferenceError` in `physics_core.js` caused by attempting to modify `backspinRPM` before its initialization during Mud and Manure shots. The spin-killing penalty has been safely relocated below the variable's declaration, preventing the 20-second shot recovery deadlock.
## v5.77.0 - Hole 12: Cow Pie Surprise
- **Course Data:** Added Hole 12 to `course_pasture.js`. A 510-yard Par 5 overrun by the Bovine Herd and spectating hazards.
- **The Manure Lie:** Injected `"Manure"` into all `physics_core.js` mud checks. It applies the same severe distance reduction, dispersion multiplication, and spin-killing penalties as deep mud.
- **Cow Pie Russian Roulette:** Expanded the Bovine Bounce interceptor in `physics_collisions.js` to include Hole 12. Added a 50/50 RNG fork: balls hitting the herd either ricochet off a cow, or sink dead into a fresh Cow Pie, triggering the wet splat grab-bag and assigning the new `"Manure"` lie.
## v5.76.0 - The Falsy Zero Hazard Fix
- **Physics Engine Core:** Fixed a critical bug in `physics_collisions.js` where hazards with an `offset` of `0` (center fairway) were being ignored by the collision engine due to a falsy zero check. Updated the guards in `getTerrainAt` and `resolveHazardLie` to use strict `undefined` checks, bringing Hole 11's Mud Trench, Hole 9's Cart Path, and Hole 3's Water fully online.
## v5.75.0 - Hole 11: Floyd's Main Drain
- **Course Data:** Added Hole 11 to `course_pasture.js`. A 403-yard Par 4 featuring a 25x15 yard rectangular green and a massive 20-yard-wide mud trench splitting the fairway.
- **Universal Ambient Expansion:** Expanded the universal hot-swap router in `main_ag.js` to support `bgAmbientPostHazard` and `hazardSwapY`. If players pass a specific Y-coordinate, the ambient audio automatically transitions.
- **The Squelch Penalty:** Engineered "The Main Drain" interceptor in `physics_collisions.js`. Shots entering the trench trigger a `splat_wet` grab-bag audio event, apply a 1-stroke penalty, and force a drop on the tee-side of the hazard.
## v5.74.0 - Speed Slot Audio Void Fix
- **Audio Routing:** Injected `rollStopTriggered = false;` into the Hole 10 Speed Slot Graze block in `physics_collisions.js`. This overrides the standard rooster dead-stop penalty, allowing the engine's 4-second fairway roll audio to play during the 40-yard magnetic warp, eliminating the awkward TTS delay void.
## v5.73.0 - Bounded Speed Slot Graze
- **Geographic Bounding Box:** Updated the Hole 10 "Speed Slot Graze" in `physics_collisions.js`. The momentum boost and magnetic fairway warp will now *only* trigger if the ball lands on the left side of the fairway (`ballX < 0`) AND lands between 170 and 300 yards down the hole (`ballY >= 170 && ballY <= 300`). All other chicken strikes on Packed Earth default to the standard dead-stop penalty.
## v5.71.0 - Hole 10: Foul Territory
- **The Feed Bowl:** Added "The Feed Bowl" to `data_ag.js` green dictionary. A massive 30-yard green where the outer 15 yards funnel aggressively down toward a flat center.
- **Hole 10 Course Data:** Added Hole 10 ("Foul Territory") to `course_pasture.js`. A 430-yard Par 4 dogleg left.
- **Packed Earth Speed Slot:** Updated the `physics_collisions.js` terrain re-mapper to convert Rough/Light Rough into "Packed Earth" on Hole 10, rewarding wayward drives with massive roll.
- **Chicken Wire Penalty:** Engineered a "Chicken Wire" OB hazard block in `physics_collisions.js` sitting exactly 40 yards left of center (`offset: -90`, `width: 100` -> catches everything from -40 to -140). Bounces the ball backward with a 1-stroke penalty.
- **Loot Mechanics:** Expanded the Foul Plate Rooster interceptor in `physics_collisions.js` to include `hole === 10`.
## v5.70.0 - Modular Audio Router Fix
- **Scope Correction:** Resolved a module-privacy bug where `main_ag.js` could not access the active ambient audio object to perform hot-swaps.
- **Audio Core Router:** Built `window.hotSwapAmbient` in `audio_core.js` to safely receive target ambient URLs and execute the cross-fade/pause logic internally. Updated `main_ag.js` to route all post-tee and post-green ambient requests to this new function.
## v5.69.0 - Universal Ambient Hot-Swaps
- **Architecture Fix:** Removed hardcoded `hole === 1` and `hole === 4` checks from the ambient audio hot-swap logic in `main_ag.js` (`window.advanceTurn`). 
- **Universal Logic:** The engine now universally checks for `holeData.bgAmbientPostGreen` and `holeData.bgAmbientPostTee`. If all players meet the geographic requirements (everyone off the tee, or everyone on the green), the engine will automatically swap the ambient track regardless of the current hole number.
## v5.68.0 - Hole 9: This Sign for Rent
- **Green Topography:** Added "The Highway Ridge" to `data_ag.js`. Features a severe back-to-front slope (`slopeY: -0.6`) designed for elevated embankment greens.
- **Course Data:** Added Hole 9 to `course_pasture.js`. A 180-yard Par 3 playing 25 yards uphill. Utilizes three-stage ambient audio (`am_farm1_golfcarts.mp3` on the tee, `_highway.mp3` in the fairway, `am_farm1.mp3` on the green).
- **Custom Terrain & Geometry:** Updated `getTerrainAt` in `physics_collisions.js` to override the circular green logic for Hole 9, creating a strict 35x12 yard rectangular putting surface. Converted all surrounding fairway terrain to "Packed Earth".
- **Highway Hazards:** Built custom "Highway Fence" (1-stroke penalty, 15-yard backward ricochet) and "Scrub Brush" (Mud physics) AABB interceptors in `physics_collisions.js`.
## v5.67.0 - The Royal Bowl (Hole 8 Overhaul)
- **Topography Nomenclature:** Renamed "The Swirling Moat" to "The Porcelain Swirl" in `data_ag.js` to match the new hole lore.
- **Lore & Audio Polish:** Updated Hole 8 in `course_pasture.js`. Renamed to "The Royal Bowl", applied the new Roman bathhouse lore description, and pointed the ambient tracks to `am_farm1_toilet.mp3`.
- **Physics Funnel Narrative:** Updated the Hole 8 funnel interceptor in `physics_collisions.js` to announce that the ball "caught the upper rim and spiraled violently down the porcelain bowl!"
## v5.66.0 - Standard Duck Grab Bag Upgrade
- **Standard Duck Array:** Replaced the single `window.audioDuck` object in `audio_core.js` with a 4-file grab bag array (`window.audioStandardDucks`) pointing to `audio/swings/duck[1-4].mp3`.
- **Duck Router Upgrade:** Updated the `else` branch in `window.triggerDuckEvent` to utilize the standard shuffle-and-pop grab bag logic, ensuring all 4 duck sounds cycle randomly without immediate repeats.
## v5.65.0 - Duck Router & Toilet Physics Fix
- **Duck Router Fix:** Updated `triggerDuckEvent` in `audio_core.js` to correctly route `window.audioDuck` to the active audio channel when the player is not on the Marquis hole.
- **Toilet Evaluation Order:** Moved the Hole 8 Toilet reward trigger in `physics_core.js` to execute *after* `resolveHazardLie`. The engine now correctly knows if the tee shot will land on the green before scheduling the mid-flight 800ms flush audio.
## v5.64.0 - Standard Duck Restoration
- **Audio Initialization:** Fixed a bug introduced in the v5.56.0 Unified Duck Logic where the global standard duck (`window.audioDuck`) was never initialized in `audio_core.js`. Re-added the `Audio` object declaration so standard mishaps correctly trigger `audio/swings/duck.mp3` on all non-Pasture-Hole-7 courses.
## v5.63.0 - Hole 8 Toilet Audio Effect
- **Toilet Asset Array:** Added a 6-clip preloaded grab bag of toilet sounds (`audio/swings/toilet[1-6].mp3`) and a `triggerToiletEvent()` function to `audio_core.js`.
- **Mid-Flight Trigger:** Updated the mishap interceptor block in `physics_core.js`. If a tee shot on Hole 8 (The Sinkhole) results in the ball landing on the Green, the engine schedules a toilet flush sound 800ms into the ball flight. This operates mutually exclusively with the duck mishap (ducks trigger on <80 accuracy; toilets trigger on successful green landings).
## v5.62.0 - Pin Finder Topography Fix
- **Grid Targeting Oracle:** Corrected an issue in `input_ag.js` and `physics_collisions.js` where using the Pin Finder grid from long distances would incorrectly report "relatively flat" for complex greens. The `getLandingZoneEffect` function now correctly uses `startY` and `endY` schema checks against the target's distance to the pin, rather than the ball's distance to the pin.
## v5.61.0 - Cache Busting & Asset Sync
- **Cache Busting:** Appended `?v=5.61.0` to all local `<script src="...">` tags in `index.html`. This ensures the browser always fetches the latest JavaScript architecture from the local server during development, preventing stale code (like v5.59.0 audio paths or physics math) from persisting after an update.
## v5.60.0 - 3D Auto-Clubbing & Sinkhole Polish
- **Green Schema Fix:** Corrected "The Swirling Moat" in `data_ag.js` to use the standard `startY` and `endY` contour brackets. The Pin Finder and putting engine now correctly read the severe elevation changes and breaks.
- **3D Auto-Equip:** Upgraded `window.autoEquipBestClub` in `physics_core.js` to account for `elevationDiff` (`targetZ - ballZ`). The engine now automatically hands the player the correct club for severe uphill/downhill shots when cycling targets.
- **Sinkhole Polish:** Updated Hole 8 in `course_pasture.js` to utilize the custom `am_farm1_sinkhole.mp3` ambient track for the entire duration of the hole.
## v5.59.0 - Hole 8: The Sinkhole & The Swirling Moat
- **Topography Data:** Added "The Swirling Moat" to `greenDictionary` in `data_ag.js`. Features a 5-tier putting surface consisting of an outer slope, a swirling 6-yard trench, a dead-flat 3-yard ring, and a steep 10-yard center dome.
- **Course Data:** Added Hole 8 (The Sinkhole) to `course_pasture.js`. It is a 195-yard Par 3 featuring a massive 40-yard drop from the tee (`pinZ: -40`) and a massive 35-yard green radius.
- **Collision Mechanics:** Built "The Sinkhole Wall" funnel interceptor into `physics_collisions.js`. Shots that miss the green but land within the sinkhole crater are violently funneled down the embankment to the edge of the 35-yard green.
## v5.58.0 - Restoration of the Downswing Goats
- **Restored Asset Loading:** Re-added the `audioGoatInterrupts` array and `playGoatInterrupt` function to `audio_core.js`. This mechanic was erroneously removed during the v5.56.0 mishap audio consolidation.
- **Restored Hazard Trigger:** Re-injected the Hole 7 Pasture course check into `input_ag.js`. The engine once again triggers a perfectly timed 4-second goat bleat exactly when the player initiates a downswing (`swingState === 3`) to serve as a psychological hazard before impact.
## v5.56.0 - Unified Course-Specific Mishap Audio
- **Audio Consolidation:** Replaced the complex, multi-stage Marquis audio sequence with a unified `audioPastureDucks` array. The engine now relies on pre-merged audio files (`duck_pasture1.mp3` through `12`) to handle both the laugh and the insult seamlessly.
- **Dynamic Caddy Yield:** Introduced `triggerDuckEvent` and `waitForDuckToClear` in `audio_core.js`. The engine now tracks the currently playing mishap audio. When a shot finishes rolling, the Caddy dynamically waits for the specific audio file to finish playing before announcing the shot report, preventing TTS overlap regardless of the audio's length.
## v5.54.0 - Hole 7 Audio Polish & Dynamic Timing
- **Ambient Audio Links:** Added `bgMusic`, `bgAmbient`, and `bgAmbientPostGreen` file paths to Hole 7 in `course_pasture.js` so the environmental audio loads correctly.
- **Comedic Timing (Marquis):** Adjusted `playMarquisSequence` in `audio_core.js` to include a 1000ms "breathing room" delay before the laugh begins. 
- **Dynamic Caddy Handoff:** Removed the hardcoded 12-second Caddy delay in `physics_core.js`. The engine now passes a callback function to `playMarquisSequence`, utilizing the `onended` HTML5 audio event to seamlessly trigger the Caddy's shot report the exact millisecond the Marquis finishes his insult.
## v5.53.1 - Hole 7 Lore Injection
- **Course Data Update:** Updated the `courseData` array inside `course_pasture.js` for "The Pasture". Surgically updated the Hole 7 text properties to officially title it "Revenge of the Marquis" and injected the new descriptive lore referencing the "Fight Club" podcast episode, warning players about the impending goat distractions.
## v5.53.0 - Hole 7: Revenge of the Marquis
- **Audio Asset Pre-Loading:** Added `goat_interrupt`, `marquis_laugh`, and `marquis_insult` arrays to `audio_core.js` for The Pasture's 7th hole.
- **The Goat Distraction:** Injected a trigger into `input_ag.js`. When a player initiates a downswing (`swingState === 3`) on Hole 7 (not on the Green), a 4-second baked goat bleat fires to ruin their tempo.
- **The Marquis Insult Intercept:** Modified the shot resolution logic in `physics_core.js`. If a player hits a "Duck" event on Hole 7, the standard duck sound is suppressed. Instead, the Marquis laughs (5s) and delivers a randomized insult. 
- **Caddy Delay:** If the Marquis intercept is triggered, the Caddy's post-shot textual and vocal report is delayed by 12 seconds to ensure the Marquis is not interrupted.
## v5.52.0 - 8-Bit Audio Balancing
- **Audio Mixing:** Applied a global 0.75x (75%) volume dampener to the 8-bit end-of-hole scoring jingles (Triumph, Neutral, and Sadness). This mix adjustment ensures the synthesized assets no longer overpower the screen reader's score announcements when a hole is completed.
## v5.51.0 - Zombie Audio & Swing Failsafes
- **Input Cooldown Failsafe:** Fixed a critical Web Audio API race condition where rapidly overlapping swings (or simultaneous key-mashes) would orphan active audio oscillators, resulting in a permanent high-pitched drone. 
- **The Watchdog:** Injected a state-monitoring interval into `input_ag.js`. Whenever `swingState` returns to `0` (after a practice swing, whiff, or shot resolution), it automatically enforces an 800ms global input lock, ensuring all audio tails have completely decayed before a new swing can be initiated.
## v5.50.0 - Oracle Calibration & Version Sync
- **Version Centralization:** Added `window.AG_VERSION` to the top of `physics_core.js`. The Markdown Telemetry Dump (`Shift + C`) in `input_ag.js` now dynamically reads this global variable instead of relying on a hardcoded string, ensuring exported match settings always reflect the true physics engine version.
- **AI Brain Calibration:** Fixed a critical bug where the Oracle Caddy and AI Brain over-hit approach shots with Woods and Long Irons. The `getOracleBlueprint` and `getCaddyAdvice` simulation loops now accurately predict and apply the `focusIndex` distance multiplier (specifically the +10% buff from Power Focus) to their `baseCarry` calculations *before* selecting a club, resulting in surgical, pin-high accuracy on long approaches.
## v5.48.1 - Clubhouse Roster Sync
- **UI Roster Update:** Fixed an oversight from v5.48.0 where the manual Clubhouse setup menu was still offering the retired Tour Pro bots. Updated the `roster_bot_tour` state block in `ui_ag.js` to correctly instantiate **Bot Rory** and **Bot Seve** with their proper AI stat blocks.
## v5.48.0 - The New Tour Pro Roster
- **Roster Update:** Retired Bot Woods and Bot Strickler from the Tour Pro tier. Introduced **Bot Rory** and **Bot Seve**.
- **Bot Rory (The Bomber):** - **AI Logic:** Programmed with extreme Driver aggression (bypassing standard hazard layup math) and a slight aversion to high-risk Flop shots. Natural power-draw bias.
  - **Audio Signature:** "The Belfast Reel" — A fast, driving pentatonic sequence synthesized via Web Audio API, played on high-accuracy strikes.
- **Bot Seve (The Magician):**
  - **AI Logic:** Programmed with massive Oracle bonuses when selecting Pitch and Flop shots, making him incredibly dangerous and creative when recovering from around the greens. Natural penetrating-draw bias.
  - **Audio Signature:** "The Flamenco" — A fast, descending Phrygian scale triplet run ending on a sustained chord, synthesized via Web Audio API, played on high-accuracy strikes.
- **Foursome Macro:** Updated the `Shift + K` clubhouse macro in `input_ag.js` to automatically load the new legendary lineup: Bot Rory, Bot Golden Bear, Bot Lefty, and Bot Seve.
## v5.47.2 - Putting End-of-Loop Blindspot Fix
- **Final Resting Spot Check:** Fixed a critical bug in the putting step-simulation loop. Previously, if a ball's `speedRemaining` hit 0 while inside the cup's `captureRadius` on its final simulation step, the `while` loop would terminate before evaluating the drop, causing perfect putts to miss "dead center" by 1 inch. The engine now performs a definitive `captureRadius` check on the ball's final resting coordinates after the loop completes.
## v5.47.1 - Auto-Focus Parameter Hotfix
- **Cycle Override Fix:** Removed the `true` argument from the `window.autoSetFocus()` calls inside the `PageUp` and `PageDown` listeners in `input_ag.js`. Previously, cycling clubs was accidentally triggering the `isPuttingOverride`, permanently locking the player's focus into "Touch" regardless of their lie or distance.
## v5.47.0 - Contextual Auto-Focus
- **Intelligent Focus Routing:** Rebuilt the `window.autoSetFocus()` function in `physics_core.js` to automatically manage the `focusIndex` based on tactical context. 
- **Auto-Focus Ruleset:** 1. Shots < 50 yards auto-equip **Touch** (Index 2).
  2. Shots from Rough, Sand, Mud, or Pine Needles auto-equip **Recovery** (Index 5).
  3. Irons and Wedges auto-equip **Accuracy** (Index 4).
  4. Woods and Drivers auto-equip **Power** (Index 1), *unless* the target distance is less than the max baseline distance of a 3-Wood, in which case it assumes an approach shot and auto-equips **Accuracy** (Index 4).
- **Trigger Integration:** Wired `window.autoSetFocus()` into the end of `window.autoEquipBestClub()` (firing on target changes and turn advances) and into the `PageUp`/`PageDown` manual club cycling listeners in `input_ag.js`.
## v5.46.2 - The Shadow Origin Fix
- **Proximity Origin Resync:** Fixed a "Shadow Origin" bug in the delayed proximity reporter in `physics_core.js`. The engine was previously reverse-engineering the shot origin by subtracting `moveX`/`moveY` (which contained un-penalized raw distances) from `ballX`/`ballY` (which contained wind-penalized final distances). This caused landing spots to be reported dozens of yards short. The proximity block now exclusively uses the absolute `startX` and `startY` coordinates to calculate exact landing metrics.
## v5.46.1 - Relative Proximity Math
- **Relative Vector Proximity:** Completely rewrote the green proximity calculator in `physics_core.js`. Previously, "Long/Short" and "Left/Right" were hardcoded to global North/South axes, causing backwards readouts when hitting backward down the fairway. The engine now uses Vector Dot and Cross Products to project the landing and resting coordinates onto the player's true line of sight, ensuring absolute directional accuracy from any angle on any course.
## v5.46.0 - Oracle Accuracy & Legacy Physics Purge
- **Loft Math Synchronization:** Updated `calculateShot` in `physics_core.js` to use the modern `1 + ((26 - dynamicLoft) * 0.005)` formula for `loftDistMod`. Previously, it was using a legacy stance-index multiplier, causing the engine's actual distance output to massively desync from the Oracle's predictions on heavily altered stances.
- **Rogue Multiplier Removed:** Stripped out an undocumented `hingeTimeBack` distance multiplier from the `potentialDist` equation in `calculateShot`. This ensures that dev inputs and AI execution no longer trigger a secret +7.5% distance overshoot that the Oracle cannot calculate.
- **Box Fan Volume:** Removed the `* 0.8` volume dampener from the Hole 6 mid-flight wind gusts.
## v5.45.0 - The Short Game & Putting Overhaul
- **Tournament Greens UI:** Replaced the 1-15 Stimp meter slider in the Clubhouse menu with a binary `Tournament Greens: [OFF/ON]` toggle (`window.tournamentGreens`). Standard is equivalent to Stimp 10; Tournament is equivalent to Stimp 13.
- **Slope Dampener Removed:** Stripped out the artificial `slopeDampener` on the putting surface. Greens now exert 100% of their physical gravitational pull on the ball, restoring realistic break mechanics.
- **Stimp-to-Touch Magnetism:** Overhauled the `touchMultiplier` hole-size logic. On Standard Greens, a perfect Touch/Hinge yields a massive 4.0x hole radius. On Tournament Greens, it shrinks to a punishing 1.5x. 
- **The "Gravity Well" Chip-In:** Added a short-game magnetism mechanic. Shots hit from the Fairway, Rough, or Sand with >95% accuracy and a near-perfect Hinge (Touch) will trigger a 6.0x hole radius multiplier, allowing for realistic chip-ins.
- **Bot Pace Scatter:** Injected a `paceScatter` variable into AI putting execution. On Tournament Greens, bots will now suffer a randomized +/- up to 8% distance error on their putts, introducing the possibility of blowing past the hole and 3-putting on fast slopes.
## v5.44.2 - The Phantom Wind Desync & Oracle Gyro Hotfix
- **Phantom Physics Desync:** Resynced `ballX` and `ballY` updates to the absolute end of the `calculateShot` loop. Previously, late-stage distance modifiers (Headwind, Elevation, Driver-off-deck) were only altering the telemetry text strings (`carryDistance`/`totalDistance`), but failing to physically move the ball's X/Y coordinates, causing shots to fly unpenalized.
- **Oracle `gyroMod` Awareness:** Injected the `gyroMod` (wind-penetration) mathematics into the Fairway Oracle loops (`getCaddyAdvice` and `getOracleBlueprint`). The Oracle now correctly understands that high-backspin shots cut through the wind, preventing it from drastically over-clubbing into headwinds.
## v5.44.1 - Local Clipboard Fallback & Telemetry Hotfixes
- **Local File Clipboard Fix:** Created a universal `window.copyToClipboard` helper in `input_ag.js`. If the modern `navigator.clipboard` API is blocked by the browser (common when playing via local `file://` protocols), it safely falls back to `document.execCommand('copy')` using a hidden textarea. All copy events now use this helper.
- **Putting Green Telemetry Fix:** Updated the `KeyC` listener inside the Putting Controls block in `input_ag.js` to properly respect `Shift`, allowing players to copy mid-hole telemetry while standing on the green.
- **Human Telemetry Scorecard Fix:** Injected `telemetryLog: typeof holeTelemetry !== 'undefined' ? holeTelemetry.join('\n\n') : ""` into the human `roundData.push` event inside `finishPutt()` (`physics_core.js`), ensuring the player's telemetry is properly permanently archived for the Scorecard export.
## v5.44.0 - QoL, Audio Cleanup & Global Browser Keys
- **Global Key Unlocks:** Unlocked `F5` (Refresh) and `F6` (Address Bar Focus) globally. The engine's keyboard listener now explicitly ignores these keys, allowing screen reader users to safely escape focus traps and refresh the client at any time.
- **Mid-Hole Telemetry Dump:** Upgraded the `Shift + C` command. It now stitches together and copies the *entire* `holeTelemetry` array, allowing users to pause mid-hole and copy a full, multi-shot report to their clipboard for debugging.
- **Audio Cleanup on Quit:** Implemented a master `window.stopAllCourseAudio()` function. When quitting a round and returning to the clubhouse, all ambient music and background tracks (which were previously orphaned and kept playing) are explicitly paused and reset.
## v5.43.1 - Hole 6 Wind Gust & Divergent Wind Hotfix
- **Physics Crash Fix:** Relocated the `wind_gust` audio trigger in `physics_core.js` to the bottom of the `calculateShot` function to prevent a `ReferenceError` on the `totalDistance` variable, restoring ball flight logic and telemetry generation.
- **Dynamic Wind Recalibration:** Created `window.applyDivergentWind()` to explicitly force the 15mph fan vector. Injected this recalibration into `getSetupReport()`, `calculateShot()`, and the `KeyW` listener (`input_ag.js`) so the wind dynamically updates as the player aims or checks conditions, rather than relying solely on turn initialization.
## v5.43.0 - Clifford's Box Fan (Hole 6) & Divergent Wind
- **Hole 6 Added:** Added "Clifford's Box Fan", a 380-yard Par 4.
- **Divergent Wind Field:** Hijacked `driftWind` and `generateWind` in `physics_core.js`. On Hole 6 (off the green), the wind is deterministically locked to a 15mph vector originating from the windmill (0, 395). Missing the center line dynamically alters the wind from a pure headwind into a punishing crosswind that pushes the ball further off the map.
- **The Egg Green:** Injected "The Egg" contour into `data_ag.js`. Features a massive false front (`-0.6` slope) and a narrow back tier.
- **The Catch Bunker:** Implemented a massive 60-yard wide, 20-yard deep wrap-around bunker 15 yards behind the pin to catch players who over-club the wind.
- **Dynamic Audio:** Added 6 `wind_gust` audio files that trigger mid-flight (at 80% volume) on any shot over 25 yards to simulate the fan catching the ball. The ambient audio cross-fades from the mechanical windmill to standard farm ambience upon reaching the green.
## v5.42.2 - Terrain Probe & Pasture Topography Fix
- **Global Scope Patch:** Fixed a silent failure in the Pin Finder and Oracle logic caused by the v5.30.0 module split. Replaced all legacy `greenDictionary` references across `physics_core.js`, `ui_ag.js`, and `input_ag.js` with explicit `window.greenDictionary` calls, restoring the engine's ability to read green contours.
- **Pasture Early Greens:** Assigned standard topography to the first three holes of The Pasture in `course_pasture.js` so they are no longer perfectly flat:
    - Hole 1: `"The Welcoming Bowl"`
    - Hole 2: `"Back-to-Front"`
    - Hole 3: `"The False Front"`
## v5.42.1 - Theatrical Audio Refinement & Sadness Melodies
- **Three-Tiered Scoring Audio:** Updated `window.playScoringAudioSequence` in `physics_core.js` to support three distinct emotional tiers.
    - `diff < 0` (Birdie/Eagle): Plays `8bit_triumph` (1-6).
    - `diff === 0` (Par): Plays `8bit_neutral` (Expanded to 1-6).
    - `diff > 0` (Bogey or worse): Plays the new `8bit_sadness` (1-6) grab bag.
- **Melody Audio Mixing:** The 8-bit scoring melodies now play at `vol * 0.8` (80% of the active ambient volume) to prevent them from overpowering the environment, while the announcer voice line remains at 100% relative volume.
## v5.42.0 - Theatrical Scoring Audio Update
- **Cup Sink Audio:** Replaced the generic `score_02` ping with a randomized 6-file grab bag (`cup1` through `cup6`) that plays exactly when the ball hits the hole.
- **Dynamic Scoring Melodies:** Created `window.playScoringAudioSequence`. When a hole is completed, a 5-second 8-bit melody plays (`8bit_triumph1-6` for under par, `8bit_neutral1-5` for par or worse).
- **Voice Announcer:** Appended a delayed voice line (e.g., `score_birdie`, `score_bogey`, `score_know`) immediately following the 8-bit melody.
- **Delayed TTS Execution:** Refactored the end-of-hole logic in `physics_core.js`. The engine now dynamically delays the Caddy's TTS shot report and the Turn Transition by 7.5 seconds whenever a hole is completed, allowing the music and announcer to finish playing first.
## v5.41.0 - The Pecking Order (Hole 5)
- **Hole 5 Added:** Added "The Pecking Order", a 460-yard dogleg right Par 4 to The Pasture. 
- **The Chicken Coop:** Implemented a massive 40-foot tall structure at the dogleg apex (260 yards) using the `trees` physics array. This physically blocks low drives from cutting the corner, forcing players to shape their shots around it.
- **Turtleback Green:** Injected a new "Turtleback" contour into `data_ag.js`. The green features a false front and a run-off back, crowning in the center.
- **Packed Earth Terrain:** Replaced all "Rough" on Hole 5 with a new "Packed Earth" terrain. 
- **Packed Earth Physics:** Landing on Packed Earth applies a `1.5x` multiplier to `rollDistance`, simulating the hard, bare dirt. Hitting *out* of Packed Earth carries a slight distance penalty (`0.95x`) and reduced forgiveness (`0.85x`) due to the tight lie.
- **Extended Roosters:** Updated the Poultry Pinball mechanic in `physics_collisions.js` to trigger on both Hole 3 and Hole 5, allowing players to farm Free Gimmes and Mulligans.
- **Spectating Flock:** Added a `Free-Range Flock (Spectating)` hazard deep left of the green for screen reader discovery.
## v5.40.2 - Telemetry Clipboard & Mud Advice Hotfix
- **Caddy Mud Advice:** Updated `window.getCaddyAdvice` and `window.getSetupReport` in `physics_core.js` to explicitly handle `currentLie === "Mud"`. The Caddy (`KeyX`) and the Style Switcher now correctly report a 50-70% distance range and warn the player of massive dispersion and zero roll.
- **Mid-Round Telemetry Copy:** Fixed the `Shift + C` keybind in `input_ag.js`. Instead of looking for legacy variables, it now targets `document.getElementById('caddy-panel-text').innerText`, successfully copying the full Markdown diagnostic report (including Club, Result, and Telemetry) to the user's clipboard immediately after a shot.
## v5.40.1 - Floyd's Mud Bowl Hotfix
- **Mud Terrain Assignment:** Fixed a bug in `physics_collisions.js` where shots missing the green on Hole 4 were defaulting to "Fairway" instead of "Mud". Added an explicit override inside `window.resolveHazardLie` to guarantee `currentLie = "Mud"` if the ball is not on the green or in water, properly activating the mud physics and audio squelch.
## v5.40.0 - Floyd's Mud Bowl & Mud Physics
- **Hole 4 Added:** Added a 165-yard Par 3 to The Pasture ("Floyd's Mud Bowl"). Features a massive 35-yard radius green to accommodate extreme contours.
- **Triple-Tier S-Curve:** Injected "The Pig Tiers" into `data_ag.js` `greenDictionary`. The green features three distinct slope zones that pull the ball in alternating directions.
- **Mud Terrain:** Introduced "Mud" as a new universal terrain type. On Hole 4, any shot missing the green lands in Floyd's Mud Wallow.
- **Mud Physics:** Hitting out of mud applies a severe penalty: massive dispersion (`4.0x`), slashed forgiveness (`0.4x`), and reduced distance (`50-70%`). Roll distance upon landing in mud is instantly reduced to 0. 
- **Mud Acoustics:** Landing in mud triggers a spatial-panned grab-bag of 6 squelch MP3s (`mud_rock_small1` through `6`).
- **Post-Green Ambient Swap:** Added `bgAmbientPostGreen` logic to `main_ag.js`. The ambient audio seamlessly hot-swaps from Floyd rooting around (`am_farm1_pig.mp3`) to standard farm ambiance (`am_farm1.mp3`) the moment the entire foursome safely reaches the green.
v5.38.0a - Inventory UI, Oracle Aim Patch, & Course Audio Bleed 
v5.38.0 - Inventory UI, Oracle Aim Patch, & Course Audio Bleed ## v5.38.0 - Inventory UI, Oracle Aim Patch, & Course Audio Bleed Fix
- **Hazard Containment:** Fixed an audio/logic bleed bug in `physics_collisions.js` where The Pasture's dynamic hazards (Tractor, Cows, Roosters) were triggering on Hole 1, 2, and 3 of *every* course. Wrapped all three blocks in a `window.currentCourse.name === "The Pasture"` condition.
- **Oracle Aim Precision:** Fixed a floating-point bug in `physics_core.js` where perfectly straight putts were announced in scientific notation (e.g., `1.016e-13 degrees Right`). Aim angles are now rounded to 1 decimal place, and anything under 0.1 degrees announces simply as "aim straight."
- **Poultry Redemption UI:** Fully wired the `M` (Mulligan) and `G` (Gimme) hotkeys in `input_ag.js` to prioritize the active player's personal inventory (`earnedMulligans` and `earnedGimmes`). Added `window.usingEarnedMulligan` and `window.usingEarnedGimme` state flags to bypass the global Match Settings during the `KeyY`/`Enter` confirmation phase.
## v5.37.0 - The AI Brain Overhaul
- **AI Putting Target Fix:** Modified `takeAITurn` in `main_ag.js`. Bots no longer lock their target cursor to the pin and hammer putts at 110%. The bot now dynamically adjusts its `puttTargetDist` cursor to exactly match the Oracle's `pace` advice, and executes a smooth 100% power swing.
- **Oracle Sweep Resolution:** Updated both `getCaddyAdvice` and `getOracleBlueprint` in `physics_core.js`. The putting simulation now tests aim angles in **0.2-degree** increments (down from 1.0) and tests pace in **1-foot** increments (`p += 0.33`), virtually eliminating "blind spots" on breaking putts.
- **Oracle Fairway Roll & Lie Fix:** The Fairway Oracle now actively pulls from the dynamic `roughConditions[rIndex].penalty` array rather than using hardcoded values. Additionally, removed an arbitrary `* 0.8` roll dampener from the Oracle's math, allowing it to correctly calculate total roll-out distance when suggesting a club.
## v5.36.1 - Foul Plate Redesign & Tractor Logic Fix
- **Hole 3 Risk/Reward Redesign:** Shortened "Foul Plate" from 520 yards to 485 yards to entice players to go for the green in two. 
- **Choked Approach:** Added `approachWidth: 15` and twin greenside bunkers to Hole 3 to severely penalize players who go for the eagle and miss laterally. 
- **Tractor Infinite Loop Fix:** Modified the Clifford's Tractor trigger in `physics_collisions.js` on Hole 1. Instead of checking `strokes === 1`, it now checks `players[currentPlayerIndex].currentLie === "Tee"`, guaranteeing the tractor can only be hit from the tee box, even if the stroke count was mathematically reset to 0.
## v5.36.0 - Foul Plate Roosters (Hole 3)
- **Poultry Pinball:** Implemented a 100% collision rate on Hole 3 for a player's first and second shots, provided the shot travels over 50 yards and does not land on the Green.
- **Micro-Bounces:** Rooster impacts cause a much smaller 360-degree deflection (3 to 6 yards) compared to cows.
- **Bonus Loot System:** Hitting a rooster triggers a random drop of either a free Mulligan or a free Gimme. The engine dynamically assigns `earnedMulligans` or `earnedGimmes` to the active player's object.
- **Audio Stacking:** Plays a panned rooster grab-bag file (`rooster1` - `rooster6`) upon impact, followed 800ms later by `freechicken.mp3`, both localized to the ball's final X-coordinate.
- **Course Data Patch:** Updated Hole 3 in `course_pasture.js` to replace the legacy string array with a `Water` bounding box and a `Chicken Flock` flag object.
## v5.35.0 - The Bovine Bounce (Hole 2)
- **Pinball Physics:** Implemented a 100% cow collision rate on Hole 2 for any shot traveling over 50 yards that misses the green. Upon impact, the ball ricochets 5 to 18 yards in any random 360-degree direction.
- **Dynamic Spatial Audio:** Wrapped the collision audio in a `StereoPannerNode`. The engine now calculates the ball's final X-coordinate and dynamically pans the cow impact sound left or right to match the ball's landing position.
- **Bovine Grab-Bag:** Implemented a `window.cowSounds` array (`cow1.mp3` through `cow6.mp3`) that shuffles, pops, and refills, ensuring players hear a unique MOO on every bounce without immediate repetition.
- **Course Data Patch:** Updated Hole 2 in `course_pasture.js` to replace the legacy string array with a `Bovine Herd` flag object.
## v5.34.6 - Texas Scrapyard Menu Restoration
- **Syntax Artifact Fix:** Removed a broken, orphaned `zones` array fragment from line 1 of `course_scrapyard.js` that was causing a fatal parsing error and preventing the course from loading into `window.courses`.
- **ARIA Description:** Updated the `courseData` getter in `main_ag.js` to include a custom screen reader description for "The Texas Scrapyard", replacing the generic default text in the Clubhouse menu.
## v5.34.5 - Clifford's Tractor (Undefined Timing Hotfix)
- **Audio/Text Sync Fix:** Removed the strict `hangTimeSecs` existence check in `physics_collisions.js` that was causing the tractor reward to bypass the timeout and instantly trigger mid-flight. Applied a robust fallback (`(hangTimeSecs || 4.5) * 1000`) to guarantee the grab-bag audio and text announcement wait for the ball to land.
- **Grab-Bag Reset:** Added a reset condition to `window.cliffExclaims` so the array repopulates once empty, ensuring all players in a foursome hear a voice line.
## v5.34.4 - Clifford's Tractor (Timing & Logic Hotfixes)
- **Ambient Hot-Swap Fix:** Modified `advanceTurn` in `main_ag.js` to check `players.every(p => p.currentLie !== 'Tee')` instead of `strokes > 0`. This ensures the tractor ambient track correctly swaps out even if a player's stroke count was reset to 0 by the fairway bonus.
- **Green Override Fix:** Swapped the evaluation order in `physics_collisions.js`. The engine now correctly assigns the `"Green"` lie *before* checking the Clifford Fairway condition, preventing the tractor reward from triggering on great drives that reach the putting surface.
- **Acoustic Sync & Grab-Bag:** Wrapped the Clifford impact narrative and audio in a `setTimeout` synced to `hangTimeSecs` so it triggers on landing rather than on swing. Replaced the generic clang with a global `window.cliffExclaims` array that shuffles and pops `cliff_exclaim1.mp3` through `4.mp3` so players hear a unique voice line on impact without repeats.
## v5.34.3 - Clifford's Tractor (Optimized Ambient Model)
- **Architecture Simplification:** Excised the `getTractorState` clock and the `StereoPannerNode` logic. The tractor's movement is now baked into the `am_farm1_tractor.mp3` ambient file.
- **Ambient Hot-Swap:** Updated `main_ag.js` and `course_pasture.js` to support a `bgAmbientPostTee` property. The engine now swaps from the tractor-heavy ambient to the quiet ambient once all players have finished their first shot.
- **Fairway Reward Logic:** Refined the "CLANG" trigger in `physics_collisions.js`. If a player's first shot (`strokes === 1`) lands in the fairway on Hole 1, they receive a free stroke, a metal impact sound, and a backward bounce.
- **Audio Cleanup:** Removed the 50ms interval loop from `audio_core.js` to save CPU cycles and prevent potential browser audio blocks.
## v5.34.2 - Clifford's Tractor (Fairway Condition & Timing Sync)
- **Audio Timing Sync:** Updated `window.getTractorState()` to a strict 50-second loop: 20s pan Left-to-Right, 10s silent gap, 20s pan Right-to-Left. 
- **MP3 Reset Logic:** Modified `window.startTractorAudio()` to physically `.pause()` and reset `currentTime = 0` during the 10-second inactive gap, ensuring the 20-second MP3 perfectly aligns with the active panning windows.
- **Physics Overhaul:** Removed the massive physical AABB from `course_pasture.js` and `physics_collisions.js`. Clifford is no longer a physical object.
- **Fairway Condition Trigger:** Added logic to `resolveHazardLie`. If the ball finishes its roll in the `"Fairway"` on Hole 1 while the tractor audio is actively playing, it automatically triggers the "hit" effect (free stroke, clang, backward bounce).
## v5.34.1 - Clifford's Tractor (Course Data & Sizing Patch)
- **AABB Injection:** Corrected an oversight where `course_pasture.js` was not updated with the new tractor physics. Replaced the legacy string array `['sand', 'tractor']` on Hole 1 with fully realized physical AABB objects.
- **Massive Hazard Scaling:** Scaled Clifford's Moving Tractor to be intentionally unrealistic (`width: 100`, `depth: 50`, `distance: 170`). This ensures it acts as an unmissable fairway-wide "wall," rewarding any player who hits the fairway with a free stroke, regardless of where the tractor is currently panned in its audio cycle.
## v5.34.0 - Dynamic Hazards (Clifford's Tractor)
- **Time-Synced State:** Introduced `window.getTractorState()` to calculate a continuous 50-second loop for Clifford's tractor. 
- **First-Shot Isolation:** Clifford is now only active during a player's first stroke (`strokes <= 1`). Once the first shot resolves, or if the tractor is hit, Clifford disappears (`xOffset: 9999`) and his audio mutes.
- **Audio Injection:** Updated `audio_core.js` to route `tractor1.mp3` through a `StereoPannerNode`, syncing the pan value to the live tractor state and fading the volume out when inactive.
- **Strict Backward Bounce:** Hitting the tractor intercepts the ball, refunds the stroke, and explicitly bounces the ball *backward and sideways* (5 to 20 yards) to prevent lucky forward bounces.
- **Acoustic Override:** Hitting the tractor triggers a metal clang (`bunker_33`) and sets `window.cliffordHit`, suppressing standard grass bouncing/rolling audio for that shot.
## v5.33.1 - Ambient Volume Control & Granularity
- **Music Granularity:** Expanded `window.musicVolumeLevels` to include a 5% option (`0.05`) for ultra-quiet background mixing. Default remains at 20%.
- **Ambient Volume Control:** Introduced `window.ambientVolumeLevels` (`0.0`, `0.25`, `0.5`, `0.75`, `1.0`) to independently control the environmental atmosphere layer, defaulting to 100%.
- **Live Updating:** Added `window.updateAmbientVolume()` to `audio_core.js` and wired the `currentBgAmbient.volume` property to the new dynamic state.
- **Keybind Injection:** Mapped `Shift + B` in `input_ag.js` to cycle ambient volume levels globally (leaving the standard `B` key strictly for Green Reading).
## v5.33.0 - Global Music Volume Control
- **Dynamic Leveling:** Introduced a 5-step global volume array (`0.0`, `0.1`, `0.2`, `0.3`, `0.4`) specifically for the `bgMusic` layer, capping the max volume at 40%. The default is set to 20%.
- **Live Updating:** Updated `audio_core.js` with `window.updateMusicVolume()` to allow real-time volume adjustments to the actively playing music track without interrupting the ambient layer.
- **Keybind Injection:** Mapped `Shift + V` in `input_ag.js` to cycle the music volume levels and announce the percentage via the ARIA interface.
## v5.31.4 - The Pasture Audio Path Correction
- **Asset Path Alignment:** Updated the `bgMusic` and `bgAmbient` properties in `course_pasture.js` to reflect the correct project directory structure (`audio/courses/pasture/` instead of `assets/audio/`).
- **File Versioning:** Bumped `course_pasture.js` internal header to v5.31.4 to track the path correction.
## v5.31.3 - The Pasture Audio Synchronization
- **Audio Leveling:** Verified and locked the environmental audio scaling (Music at 0.3, Ambience at 1.0) in the modularized `audio_core.js`.
- **Course Wiring:** Confirmed that `main_ag.js` successfully triggers the dual-track environment from `course_pasture.js` asset paths.
- **Header Sync:** Synchronized internal headers to v5.31.3 to ensure project-wide version parity.
## v5.31.2 - Modular Script Alignment
- **Architecture Fix:** Updated `index.html` to load the refactored modular physics (`physics_core.js`, `physics_collisions.js`) and audio (`audio_core.js`) files, replacing the legacy monolithic scripts.
- **Header Sync:** Updated internal version headers for the physics and audio modules to v5.31.2 to reflect the completion of the modular split.
- **Dependency Map:** Confirmed that `physics_collisions.js` loads after `physics_core.js` to ensure terrain queries have access to base math functions.
## v5.31.1 - Architecture Sync & Truth Restoration
- **Documentation:** Restored the missing v5.30.0 and v5.31.0 history to `gemini.md` to ensure project continuity.
- **Race Condition Fix:** Reordered `index.html` script tags to ensure core data and logic (Physics/Audio/UI) load before the `main_ag.js` engine initialization.
- **Version Alignment:** Synchronized internal file headers to v5.31.1 across all modified assets.
## v5.31.0 - Modular Event-Loop Recovery
- **Version Alignment:** Updated `index.html` metadata to reflect current engine state.
- **Global Menu Bridge:** Attached `menuOptions` and `menuIndex` to the `window` object to resolve cross-file visibility issues between `ui_ag.js` and `input_ag.js`.
- **Input Restoration:** Fixed a bug where the `Enter` key failed to trigger menu selections in the Clubhouse due to an incorrect `gameMode` check in the modularized listener.
- **Aria-Live Fix:** Restored screen reader labels to menu navigation by ensuring `ui_ag.js` has access to the `window.announce` global for item descriptions.
## 5.30.0
Major architectural refactor to modularize the game engine for better performance and AI readability. Split `main_ag.js` into functional modules: `audio_core.js` (sound engine), `ui_ag.js` (HUD and menus), and `input_ag.js` (keyboard listeners). Deconstructed the massive `physics_ag.js` into `physics_core.js` (flight math) and `physics_collisions.js` (hazards and lies). Updated `index.html` with the new load order and documented the modular map in `gemini.md`.
## 5.23.1
Refactored course data architecture to support a modular catalog. Extracted all course objects from `data_ag.js` and migrated them into standalone files within a new `courses/` directory (`course_holodeck.js`, `course_scrapyard.js`, `course_pebble.js`, `course_pasture.js`). Converted the static `courses` array into a dynamic `window.courses` registry to allow seamless catalog expansion. Updated `index.html` to load the modular course scripts, and updated `gemini.md` to document the new file structure.
## 5.23.0
Implemented the `window.playEnvironment` audio engine to handle external MP3 looping for course background music and ambient environments. Wired the engine to support dual HTML5 Audio objects, setting ambient music to 30% volume and background environment to 100% volume. Updated "The Pasture" hole generation array to inherit Hole 1's audio assets for holes 4 through 18 to ensure continuous playback during full-round testing.
##v5.22.4: Resolved a critical crash preventing "The Pasture" from loading by adding a parameter fallback (`holeNumber`) for undefined hole numbers in the `loadHole` function. Standardized property keys in "The Pasture" course data, replacing `dist` with `distance` to restore proper functionality to the telemetry archiver.
## [5.22.3] - 2026-03-28
### Fixed
- **The Pasture Truncation Fix:** Bypassed an AI token-limit truncation error in `data_ag.js` by using `Array.from()` to programmatically generate holes 4-18 for The Pasture. This repairs the corrupted array syntax that was preventing the entire course data file from loading and causing the `zones` undefined crash.
## [5.22.2] - 2026-03-28
### Fixed
- **Data Bridge Timing Patch:** Replaced the static `window.courseData` assignment in `main_ag.js` with an `Object.defineProperty` getter. This resolves a race condition where `main_ag.js` was executing before `data_ag.js` had fully loaded, resulting in an undefined `holeData` crash when initializing Bot AI targets.
## [5.22.1] - 2026-03-28
### Fixed
- **The Pasture Array Hotfix:** Re-formatted the 18-hole array for The Pasture in `data_ag.js`. A previous truncation error caused holes 4-18 to be dropped and the `zones` property to be deleted, causing a bot initialization crash. All 18 holes are now fully defined with valid `zones` arrays.
## [5.22.0] - 2026-03-28
### Added
- **The Pasture (Farm Course):** Injected the complete 18-hole array for The Pasture into `data_ag.js`. Holes 1-3 feature custom lore names (Clifford's Shortcut, The Bovine Bounce, Foul Plate) and distinct hazard profiles (tractors, cows, chickens).
- **Ambient Audio Controllers:** Re-established `window.bgMusic` and `window.bgAmbient` HTML5 audio elements inside `audio_ag.js` with a 30% volume lock to prevent Web Audio API memory leaks on long loops.
- **Dynamic Environment Transitions:** Wired `window.playEnvironment()` into the hole-loading sequence in `main_ag.js` to crossfade music and ambient tracks based on the active hole's data object.
## [5.21.1] - 2026-03-28
### Fixed
- **Architecture Patch:** Executed a targeted strike on two remaining legacy array references missed during the Phase 2 global transition. Replaced `courses[currentCourseIndex]` with `window.currentCourse` inside the telemetry dump (`main_ag.js`) and the putting AI calculator (`physics_ag.js`), achieving 100% deprecation of the old memory block.
- **Version Headers:** Bumped engine headers to v5.21.1.
## [5.21.0] - 2026-03-28
### Changed
- **Architecture Switch (Phase 2):** Safely executed a global replacement of the legacy `courses[currentCourseIndex]` array reference with the new dynamic `window.currentCourse` object across both `main_ag.js` and `physics_ag.js`. Because the UI memory block was successfully enriched in Phase 1, this completes the architectural migration and permanently locks the physics AI and main game loop into the modern memory structure.
- **Version Headers:** Bumped engine headers to v5.21.0.
## [5.20.0] - 2026-03-28
### Changed
- **Data Architecture Unification (Phase 1):** Removed the hardcoded, lightweight `window.courseData` array from `main_ag.js`. Replaced it with a dynamic mapping bridge that clones the heavy `courses` array from `data_ag.js` and injects the required UI `id` and `desc` properties. This ensures both the physics engine and the clubhouse menu draw from the same comprehensive memory block.
- **Version Headers:** Bumped engine headers to v5.20.0 to clear the reverted timeline.
## [5.15.0] - 2026-03-28
### Changed
- **Course Selection UI:** Rewired the Clubhouse Menu (`clubhouseState === 'course'` and `'course_quick'`) to dynamically pull from the new `window.courseData` architecture instead of the deprecated `courses` array.
- **Active Course State:** Selecting a course in the menu now explicitly updates `window.currentCourse = window.courseData[idx];`, officially loading the specific 18-hole hazard map into the active game loop.
- **Version Headers:** Updated global file headers and the `index.html` title to v5.15.0.
## [5.14.0] - 2026-03-28
### Added
- **Course Profiles Architecture:** Built the underlying data structures to support distinct 18-hole golf courses. Injected `window.courseData` into the global state, mapping out 4 distinct layouts:
  - **Holo Links:** The standard, pristine digital simulation.
  - **Texas Scrapyard:** Industrial hazards with heavy wind and scrap obstacles.
  - **Pebble Beach:** Coastal hazards, punishing sand, and ocean winds.
  - **The Pasture:** A chaotic farm course featuring cows, manure, chickens, and goats.
- **Hazard Tagging:** Each hole in a course profile now contains a `hazards` array (e.g., `['cows', 'manure']`), laying the groundwork for hole-specific environmental RNG triggers.
### Changed
- **Version Headers:** Updated global file headers and the `index.html` title to v5.14.0.
## [5.13.0] - 2026-03-27
### Fixed
- **Tour Pro Foursome Variable Crash:** Fixed a fatal `ReferenceError` in `input_ag.js` where the `Shift + K` Clubhouse macro incorrectly checked an undefined `shift` variable instead of the native `e.shiftKey` event property. The macro now executes smoothly.
### Changed
- **Version Headers:** Updated global file headers and the `index.html` title to v5.13.0.
## [5.12.0] - 2026-03-27
### Fixed
- **Tour Pro Foursome UI Refresh:** Fixed a bug where pressing `Shift + K` updated the roster state but failed to redraw the visual menu or trigger the course selection screen reader logic. Added `clubhouseIndex = wizardCourse;` and the missing `window.announceClubhouse(false);` call to perfectly mirror the `Shift + L` architecture.
### Changed
- **Version Headers:** Updated global file headers and the `index.html` title to v5.12.0.
## [5.11.0] - 2026-03-27
### Added
- **Tour Pro Foursome Quick-Load:** Added a new macro (`Shift + K`) accessible from the Clubhouse menus. Pressing this instantly populates a 4-player game with Bot Woods, Bot Golden Bear, Bot Lefty, and Bot Strickler, bypassing the manual roster setup and jumping straight to Course Selection.
### Changed
- **Version Headers:** Updated global file headers and the `index.html` title to v5.11.0.
## [5.10.0] - 2026-03-26
### Added
- **Pro Legends Roster:** Added three elite Tour Pro bots with highly specific, historically accurate mathematical playstyles:
  - **Bot Golden Bear:** Accuracy Focus, +15ms Impact Bias (Power Fade). The Oracle AI heavily penalizes risky shots, forcing him to play away from hazards and play smart.
  - **Bot Strickler:** Touch Focus, 0ms Biases (Dead Neutral). The Oracle AI applies a massive negative miss-penalty to Wedges, turning him into a pin-seeking dart thrower from 120 yards and in.
  - **Bot Lefty:** Recovery Focus, -25ms Impact / +10ms Hinge (Aggressive Lefty Hook). The Oracle AI actively subtracts hazard penalties when holding a Wood, forcing him to go for Par 5s in two, and heavily favors the Pitch/Flop shot around the greens.
### Changed
- **Version Headers:** Updated global file headers and the `index.html` title to v5.10.0.
## [5.9.0] - 2026-03-26
### Added
- **High-Accuracy Strike Audio:** Added immediate MP3 audio rewards for swinging with >90% accuracy.
  - Woods & Drivers trigger one of 5 "Woods Blast" sounds (`audio/swings/woods_blast[1-5].mp3`).
  - Irons & Wedges trigger one of 6 "Bullseye" sounds (`audio/swings/bullseye[1-6].mp3`).
  - These play instantly upon impact to create a visceral, instantaneous reward loop for perfect timing.
### Changed
- **Version Headers:** Updated global file headers and the `index.html` title to v5.9.0.
## [5.8.0] - 2026-03-26
### Added
- **"Duck" Audio Feedback:** Added a humiliating auditory mechanic where any shot registering below 80% accuracy triggers one of four random duck quacks (`audio/swings/duck[1-4].mp3`) while the ball is in mid-flight.
- **Roster Expansion:** Officially added "Dusty Bunkers" to the Amateur Bot selection menu in the Clubhouse Wizard.
### Changed
- **Version Headers:** Updated global file headers to v5.8.0.
## [5.7.0] - 2026-03-26
### Fixed
- **Scorecard Input Bleed:** Fixed a UI focus bug where opening the Grid Scorecard failed to lock out background gameplay controls. Added the missing `viewingScorecard = true` state flag to `renderScorecard` to ensure arrow keys navigate the table instead of aiming the player's shot or triggering swings.
## [5.6.0] - 2026-03-26
### Changed
- **Top of Swing Milestone:** Upgraded the 100% power marker to a high-visibility "Double Chirp" using a 1200Hz square wave. This provides a piercing rhythmic landmark to prevent over-torquing.
- **Auditory Feedback Persistence:** Extended the duration of post-shot execution pings (Hinge and Impact feedback) to 0.5s for better pitch recognition.
### Fixed
- **Scorecard Access:** Added a validation check to `renderScorecard`. If the scorecard fails to build at the end of a round, the engine now triggers a fallback ARIA announcement to prevent "audio silence" after the chime.

### Changed
- **Top of Swing Milestone:** Upgraded the 100% power marker to a high-visibility "Double Chirp" using a 1200Hz square wave. This provides a piercing rhythmic landmark to prevent over-torquing the backswing.
- **Auditory Feedback Persistence:** Extended the duration of post-shot execution pings (Hinge and Impact feedback) to 0.5s. The longer decay allows for better pitch recognition when diagnosing swing errors.
## [5.5.1] - 2026-03-26
### Added
- **Telemetry Versioning:** The `Shift + C` Telemetry Dump now explicitly includes the `Engine Version` in the `# MATCH SETTINGS` header. This ensures all future simulation data can be mapped back to specific code iterations (e.g., v5.5.1).
## [5.5.0] - 2026-03-26
### Added
- **Quick Telemetry Readout (`Shift + Up Arrow`):** Added a hotkey that parses the most recent shot's markdown log and announces the exact Power, Hinge Diff, Impact Offset, and Spin rates via ARIA, providing instant diagnostic feedback without needing to export the logs.
- **UI Audio Feedback:** Added missing audio cues for invisible UI actions. `Shift + E` (Scorecard) now plays the `ui_nav_03` menu chime, and `Shift + C` (Copy Telemetry) now plays the `ui_nav_06` success chime when the data hits the clipboard.
## [5.4.0] - 2026-03-26
### Added
- **The Impact Target (Audio Bullseye):** Injected an independent 800Hz triangle ping that plays exactly at the mathematical bottom of the downswing (`dropDurationMs`). This acts as a constant metronome tick, allowing players to hear exactly how early or late they were relative to the perfect 0ms sweet spot.
## [5.3.1] - 2026-03-26
### Fixed
- **Pin Finder Unlock:** Authorized the `Z` and `T` key targeting listeners to fire during `gameMode === 'range'`, restoring Pin Finder capabilities to all practice facilities.
- **Scorecard Failsafe:** Injected a failsafe into `advanceTurn` that forces a scorecard write if a player's hole is complete but their `roundData` array is missing the entry (fixing the bug where Auto-Snowmen and Gimmes vanished from the final score).
### Changed
- **Bot Variance Nerf:** Increased Skill 3 (Ted) base variance from 15ms to 30ms, and Skill 2 (Fred) from 45ms to 55ms. This introduces realistic lateral scatter on approach shots, preventing top-tier bots from shooting sub-60 rounds via robotic proximity.
## [5.3.0] - 2026-03-26
### Changed
- **Practice Facility Unification:** Deprecated the isolated `chipping` game mode. The Pitching/Long Chipping Green and Short Chipping Green now run natively on the `range` architecture, fully unlocking the 3D Object Manager, Edge Finder, and Oracle Caddy for short game practice.
- **Dynamic Object Scaling:** The Object Manager's spatial controls (`[ ]` and `- =`) now dynamically scale their movement increments based on the active practice area (1-yard snaps for Short Chipping, 3-yard snaps for Long Chipping, 5/10-yard snaps for the Holo Range).
### Added
- **Target Randomizer (Key R):** Added a manual randomizer to the practice facilities. Pressing `R` instantly warps the Target Flag to a new random distance and elevation (`pinZ`) within the specific boundaries of the active facility.
## [5.2.0] - 2026-03-26
### Fixed
- **Clubhouse Help Button Crash:** Fixed a silent Javascript crash caused by an uncancelable synthetic `KeyboardEvent` triggering `e.preventDefault()`. 
- **Help Architecture:** Extracted the Help initialization logic into a global `window.openHelpMenu()` function to ensure parity whether launched via a physical hotkey or UI click. Muted the standard `ui_nav_01` Clubhouse select tone when clicking the Help button to prevent audio overlap.
## [5.1.9] - 2026-03-26
### Changed
- **Pro Scramble Algorithm:** Skill 3 bots (Ted) now have a 15% chance to "lapse" on tee shots, forcing an impact offset of 70-100ms. To compensate, they receive an Elite Scrambling bonus when hitting from Hazards or Rough (locking variance to a near-perfect +/- 10ms).
- **Forced Dispersion Ceiling:** Skill 1 and Skill 2 bots (Dusty/Fred) can no longer chain together perfect shots via RNG luck. If the engine rolls a "Flushed" impact (< 25ms offset for Fred, < 40ms for Dusty), the math artificially pushes the impact outward to simulate consistent amateur dispersion.
## [5.1.8] - 2026-03-26
### Added
- **AI Personalities:** Injected name-based mathematical biases into the Oracle Caddy brain (`getOracleBlueprint`). 
  - **Fairway Fred:** Mathematically avoids Drivers/Woods (+30 penalty) and prefers Irons (-15 bonus) to prioritize safety over distance.
  - **Dusty Bunkers:** Actively prefers overswinging (-20 bonus to 105-110% power) and avoids choking down (+25 penalty to <95% power).
### Changed
- **4F Quick Load:** Upgraded the `Shift + L` Foursome of Friends to a linear skill progression: Shawn (0), Dusty Bunkers (1), Fairway Fred (2), and Tour-Pro Ted (3).
- **Holo Range UI:** Removed the `hasHeardHoloOrientation` mute lock. The Holo Range now always announces its critical tools (`O`, `Enter`, `?`, `F12`) upon entry.
- **Help Audio:** Swapped the `?` Help Menu open sound to `ui_nav_03` to match the `Escape` `ui_nav_04` closing chime.
## [5.1.7] - 2026-03-26
### Added
- **Dynamic Help Injection:** The Help Menu (`?`) now dynamically injects contextual keybindings at the top of the list based on your location (e.g., Holo Range Object controls).
- **Clubhouse Help Button:** Added a dedicated "Help & Master Keybindings" button to the root Clubhouse menu.
- **F12 Context Awareness:** Keyboard Explorer now accurately reports when keys like `O`, `[ ]`, and `- =` are unassigned vs. when they are active Holo Range tools.
### Changed
- **Holo Range Dialog:** Shortened the introductory Holo Range speech to reduce cognitive overload, pointing users to the `?` and `F12` menus for the deep-dive controls.
- **Foursome Quick-Load:** `Shift + L` is now a global clubhouse shortcut that instantly builds the 4-bot roster (Moe, Shawn, Ted, Fred) and routes the user directly to Course Selection, focusing the last played course.
- **Help Audio:** Pressing `?` now plays the distinct `ui_nav_06` chime.
## [5.1.6] - 2026-03-26
### Changed
- **Menu Navigation:** The Clubhouse and Practice Facility menus now wrap infinitely. Pressing Up at the top jumps to the bottom, and pressing Down at the bottom jumps to the top.
- **Holo Range Oracle:** Unlocked the Oracle Caddy (`A`) on the Holo Range. The Caddy can now evaluate your custom 3D sandbox setups and provide the exact club, stance, and aim required to hit your targets and clear obstacles.
## [5.1.5] - 2026-03-26
### Added
- **The Holo Range:** Upgraded the Driving Range into a 3D Sandbox. Players can now use the Object Manager (`O`) to spawn and position Target Flags, Sand Bunkers, Single Trees, Tree Walls, and Tree Clusters.
- **Holo Range Topography:** Enabled Pin Finder (`Shift + Z`) on the Holo Range. Players can now adjust Target Flag elevation (`Shift + Dash/Equals`) to practice uphill/downhill shots.
- **Smart Help System:** Implemented a progressive `?` menu. The first press provides contextual orientation for the active practice area. A second press transitions seamlessly to the Master Keybindings list.
### Changed
- **Practice Menus:** Split the Chipping Green into two distinct menus: "Pitching and Long Chipping Green" and "Short Chipping Green".
- **UX Routing:** Pressing `Q` from a practice facility now returns the player directly to the Practice Sub-Menu rather than the Clubhouse root.
### Removed
- **Shortcut Cleanup:** Removed legacy practice warp hotkeys (`R`, `G`, `Shift+P`) to free up keyboard real estate and prevent accidental mid-round warping.
## [5.1.4] - 2026-03-25
### Fixed
- **Help Menu Scope Trapping:** Rescued `renderHelpMenu` and `announceHelp` from being improperly scoped inside the `drawMeter` canvas loop, restoring ARIA announcements and Arrow Key navigation for the Help Menu.
- **Help Menu Interceptor Priority:** Consolidated the `viewingHelp` input intercepts into a single, high-priority block at the top of the event listener to guarantee it works flawlessly in both the Clubhouse and on the course.
- **Help Menu Audio Cue:** Added the `ui_nav_03` "Panel Open" audio cue to the `?` hotkey so players have immediate auditory confirmation that the visual overlay has appeared.
## [5.1.3] - 2026-03-25
### Added
- **Universal Help UI:** Added a stylized, synchronized visual overlay for the Help Menu (`?`). As screen reader users navigate the keybindings with the Arrow Keys, the visual UI automatically highlights the active row and scrolls it into view for sighted users.
### Changed
- **Help Menu Content:** Completely overhauled `helpMenuText` in `data_ag.js`. The list is now categorized with `heading: true` flags (allowing `H` and `Shift+H` navigation). Moved the `F12` Keyboard Explorer instruction to the very top for immediate discoverability.
- **Keyboard Explorer:** Synchronized the `F12` logic in `input_ag.js` to mirror the new Help Menu, adding native announcements for `Shift` and `Control` modifiers.
## [5.1.2] - 2026-03-25
### Fixed
- **Oracle Topography Match:** Updated the `getCaddyAdvice` and `getOracleBlueprint` simulated flight calculations to properly subtract `elevationDiff` from the `effectiveCarry`. This ensures the AI Brain correctly clubs down on steep downhill approaches (like Pebble Beach Hole 7) and clubs up on steep inclines.
### Added
- **Quick-Load Sim Roster:** Added a `Shift + L` hotkey to the Clubhouse Roster menu. Pressing this instantly populates a 4-bot testing roster (Shankin' Shawn, Mulligan Moe, Tour-Pro Ted, Bot Woods).
- **Master Telemetry Scoreboard:** The `Shift + C` Master Dump now injects a `# SCOREBOARD SUMMARY` block at the top of the file, instantly tallying total strokes and scores to par for all active players.
## [5.1.1] - 2026-03-25
### Fixed
- **Practice Sub-Menu:** Converted the Clubhouse "Practice Facilities" button into a navigable sub-menu, restoring access to the Chipping Green and Practice Putting Green after the removal of their mid-round hotkeys.
- **Putting Auto-Rules:** Injected the Auto-Gimme and Auto-Max Score logic directly into the `finishPutt()` physics loop to ensure missed putts that stop near the hole automatically trigger Gimme hole-outs.
## [5.1.0] - 2026-03-25
### Added
- **Formal Match Rules:** Added Mulligans, Gimmes, and Max Score limiters.
- **Setup Wizard Expansion:** Added Match Rules toggles to the Clubhouse setup screen. These settings are now persistent and remember your previous choices.
- **Auto-Gimmes & Max Caps:** The physics engine actively monitors the ball after it stops. If it lands inside the auto-gimme threshold, or if a player hits their Max Score cap, the engine automatically holes them out to keep the pace of play moving.
- **Fast-Forward Setup:** Added a `Ctrl + Enter` hotkey to the Clubhouse wizard that automatically selects the primary "Proceed" or "Start" action on the current screen, allowing players to skip through menus instantly.
### Changed
- **Input Mapping:** Remapped "Manual Player Swap" from `M` to `N` (Next). Assigned `M` to Mulligan, `Shift+M` to Snowman (Max 8), and `G` to Gimme. Practice facility warping has been completely removed from mid-round hotkeys to protect save states.
## [5.0.7] - 2026-03-25
### Added
- **Bot Hazard Failsafe:** Implemented an infinite-loop breaker for AI players. If a bot lands in a Water Penalty or Sand trap 3 consecutive times, they automatically declare an "Unplayable Lie," take an additional penalty stroke, and are dropped 80 yards backward into the center of the fairway to force a safe, full-swing clearance.
## [5.0.6] - 2026-03-25
### Fixed
- **Swing Memory Wipe:** Removed an accidental `window.loadGame()` call from the `startBackswing` function. This rogue call was overwriting the active game state with the `localStorage` save file every time a player initiated a swing, effectively erasing their club and aim selections at the last millisecond.
## [5.0.5] - 2026-03-25
### Fixed
- **Boot Sequence Memory:** Injected the missing `window.loadGame()` call into `window.initGame` to ensure the engine successfully reads `localStorage` upon browser refresh, restoring the "Resume Current Round" button.
## [5.0.4] - 2026-03-25
### Fixed
- **Session Recovery:** The `initGame` boot sequence now actively probes `localStorage` via `loadGame()`. This ensures the Clubhouse Wizard correctly displays the "Resume Current Round" button after a browser refresh.
- **Resume UI Rebuild:** Fixed the "Resume Current Round" button action to safely call `loadActivePlayer` and rebuild the Dashboard, preventing crashes caused by a deprecated `getSetupReport` function call.
### Added
- **AI Thinking Audio:** Implemented a `botThinkingInterval` that plays a subtle 400Hz ticking metronome during the massive `pacingMode` delays, assuring the player that the game has not frozen while waiting for the AI to swing.
## [5.0.4] - 2026-03-25
### Fixed
- **Session Recovery:** The `initGame` boot sequence now actively probes `localStorage` via `loadGame()`. This ensures the Clubhouse Wizard correctly displays the "Resume Current Round" button after a browser refresh.
- **Resume UI Rebuild:** Fixed the "Resume Current Round" button action to safely call `loadActivePlayer` and rebuild the Dashboard, preventing crashes caused by a deprecated `getSetupReport` function call.
### Added
- **AI Thinking Audio:** Implemented a `botThinkingInterval` that plays a subtle 400Hz ticking metronome during the massive `pacingMode` delays, assuring the player that the game has not frozen while waiting for the AI to swing.
## [5.0.3] - 2026-03-24
### Fixed
- **Pebble Beach Schema:** Replaced `hole` keys with `number` keys in the Pebble Beach course data to prevent a fatal crash in the `updateTargetZone` function.
- **Zone Targeting:** Injected missing `zones` arrays into the Pebble Beach data to restore Z-key fairway/green targeting.
- **Menu Syntax Crash:** Surgically replaced the `course` selection block in the Clubhouse wizard to permanently remove the "Coming Soon" duplicate and fix an orphaned `}});` syntax error.
## [5.0.2] - 2026-03-24
### Fixed
- **Duplicate Course Fix:** Removed the hardcoded "Coming Soon" Pebble Beach placeholder from `buildClubhouseMenu` to prevent it from appearing alongside the actual playable course.
## [5.0.0] - 2026-03-24
### Added
- **Pebble Beach Golf Links:** Added the legendary 18-hole championship course.
- **Topographical Realism:** Integrated extreme `targetZ` elevation changes for iconic holes (e.g., the downhill drop of Hole 7, the uphill climb of Hole 6).
- **Strategic Chokepoints:** Utilized `landingZones` and AABB hazard mapping to recreate the ocean chasms, sloping fairways, and the 18th hole Cypress tree.
### Changed
- **Clubhouse Setup:** Removed the "Coming Soon" placeholder; Pebble Beach is now fully unlocked and playable.
## [4.92.0] - 2026-03-24
### Changed
- **Dynamic Rough Physics:** The Clubhouse "Rough Condition" setting now directly drives the `lieMod` and `lieForgivenessMod` inside the physics engine. "Sunday Trim" provides a mild 95% carry, while "Neglected for a week" crushes distance to 55% and shrinks the sweet spot.
- **Telemetry Header:** The `Shift + C` Master Dump now prepends a Match Settings block (Course, Wind, Stimp, Rough Condition) to the top of the exported Markdown file.
## [4.91.0] - 2026-03-24
### Changed
- **Hierarchical Roster Selection:** Restructured the Clubhouse Roster wizard. Selecting a slot now opens a categorized `roster_type` sub-menu (Human, Amateur Bot, Tour Bot), which drills down into specific bot lists, preventing accidental double-Enter selections.
- **Bot Woods:** Added a placeholder profile for "Bot Woods" to the Tour Pro category.
- **UX Prompts:** Added explicit "Use Up and Down arrows" instructions to the Caddy's menu preambles to clarify the new state-machine navigation paradigm.
## [4.90.0] - 2026-03-24
### Changed
- **Clubhouse Setup Wizard:** Completely overhauled `buildClubhouseMenu` into a state-machine. Players now progress through a sequential, accessible setup flow (Root -> Course -> Group Size -> Roster Drill-Down -> Match Settings).
- **Menu Acoustics:** Added `bunker_03` and `bunker_04` sweep tones to up/down arrow navigation in the Clubhouse. Added `menu_02` to the Escape key for backward navigation.
- **Pacing Failsafe:** Starting a new round explicitly resets `pacingModeIndex = 0` (Fast), preventing human players from accidentally launching into silent Simulation pacing.
- **New Variables:** Added `wizardWind`, `wizardStimp`, and `roughConditionIndex` logic to the pre-game setup phase.
## [4.89.0] - 2026-03-24
### Changed
- **Master Telemetry Dump:** Upgraded the Scorecard `Shift + C` clipboard export. It now loops through the entire `players` array, compiling all shot data for the entire foursome into a single, comprehensive Markdown document.
## [4.88.0] - 2026-03-24
### Changed
- **Deterministic Shot Physics:** Replaced uniform RNG dispersion with deterministic edge-weighted penalties. Side spin is now mathematically locked to `impactDiff` (Early = Hook, Late = Slice).
- **Hinge Distance Caps:** `hingeDiff` errors now apply strict distance ceilings. Fat shots (`+45ms` late) physically cannot carry more than 85% of their potential distance, preventing the RNG from "saving" bad swings.
- **Donut Dispersion RNG:** Remaining random scatter now utilizes `1.0 - Math.pow(Math.random(), 2)` to heavily weight outcomes toward the outer edges of the error cone, eliminating the "Lucky Center" flaw.
## [4.87.0] - 2026-03-24
### Added
- **Pine Straw Physics:** Integrated "Pine Needles" into the physics engine. 
- **Frictionless Roll:** Balls landing in or rolling through Pine Needles slide significantly further than on grass.
- **Lie Penalties:** Hitting out of Pine Needles applies a 15% distance penalty, reduces the Sweet Spot by 30%, and brutally slashes Backspin generation by 90%, making it impossible to hold greens on approach shots.
## [4.86.0] - 2026-03-24
### Added
- **Headless Telemetry Engine:** Added "Simulate" to the `KeyP` pacing modes. When active, bot turns bypass all audio, TTS, and flight delays, executing instantly (`window.isQuickSim`). 
- **Pre-Round Setup UI:** Added dynamic "Course" and "Roster" toggles to the Clubhouse menu.
- **AI Personality Biases:** Bot profiles now accept `impactBias`, `hingeBias`, `focusIndex`, and `activeBallIndex`, giving them distinct, data-driven playstyles (e.g., Fairway Frank chunks the ball, Mulligan Moe slices it).
## [4.85.1] - 2026-03-24
### Fixed
- **Scorecard Player Swap:** Reverted the Multiplayer Scorecard swap hotkey from `Tab` to `KeyP` to prevent unresolvable input-swallowing conflicts with native browser and screen-reader focus management.
## [4.85.0] - 2026-03-24
### Added
- **Multiplayer Scorecards:** The Scorecard UI now supports `KeyP` to cycle between the active players' individual scorecards.
- **Isolated Telemetry:** `roundData`, `roundHighlights`, and `currentHoleStats` are now isolated per player in the `players` array. Bot telemetry is no longer overwritten by human players.
## [4.84.0] - 2026-03-24
### Fixed
- **Scorecard UI Lag:** Rewrote `announceScorecardCell` to use O(1) direct DOM targeting (`window.lastHighlightedCell`). It no longer loops through the entire table to reset CSS styles, eliminating input lag when navigating large scorecards.
## [4.83.0] - 2026-03-24
### Added
- **UI Altimeter Pings:** Cycling targets with `Z` now plays an ascending two-note ping if the target is significantly uphill, and a descending ping if it is downhill, giving players an immediate "audio feel" for the hole's elevation.
- **Slope Rangefinder:** The `T` Key distance report now calculates exact 3D elevation differences, telling the player exactly how much uphill/downhill the target is and providing the mathematically "Effective Distance."
- **Z-Axis Hang Time:** The flight audio's duration (`hangTimeSecs`) is now dynamically modified by elevation. Hitting into a sinkhole will audibly extend the flight time, while hitting into an uphill green will cut the flight audio short.
## [4.82.0] - 2026-03-24
### Added
- **Course Completion:** Added Holes 4 through 18 of "The Texas Scrapyard" to `data_ag.js`. 
- **Topography Data:** Injected advanced `tilt`, `slope`, and `z` values into the landing zones to fully utilize the 3D physics engine (canyons, sinkholes, uphill/downhill carries).
- **Visual/Radar Hazards:** Added out-of-play scenic hazards (Washing Machines, Rocker Arms) that trigger purely for auditory world-building via the `KeyH` Edge Finder.
## [4.81.0] - 2026-03-24
### Added
- **Relative Pin Finder:** The Pin Finder grid now uses trigonometric rotation. Navigating Up/Down/Left/Right always moves the cursor relative to your active line of sight to the pin, rather than global map coordinates.
- **Dynamic Ground Topography:** Green slopes (`greenDictionary`) are now mathematically rotated against the ball's final flight vector. Slopes will now physically kick the ball laterally (`lateralKickX`, `lateralKickY`) if approached from an angle.
### Changed
- **Predictive Topography Readouts:** The Caddy's Pin Finder advice (e.g., "Kicks left and releases forward") dynamically updates to reflect how the slope will behave based on your specific approach angle.
## [4.80.0] - 2026-03-24
### Changed
- **3D Coordinate Transformation (Wind):** `windX` and `windY` are now treated as absolute Global Map coordinates.
- **Relative Aerodynamics:** The engine now uses a trigonometric rotation matrix (`Math.cos` and `Math.sin` of the shot heading) to rotate the global wind into Relative Headwinds and Relative Crosswinds.
- **Carry Distance Fix:** Fixed a massive physics bug where `windYEffect` was calculated but never mathematically applied to the actual `carryDistance` of the shot.
- **Dynamic Wind Reporting:** The `W` key and the Dashboard now dynamically update the wind report based on your aim. If a North wind is blowing and you aim East, the Caddy will correctly report it as a Crosswind from the Left.
## [4.80.0] - 2026-03-24
### Fixed
- **AI Fractional Power Penalty:** The Fairway Oracle now heavily penalizes standard club selections that require less than 85% power. This prevents bots from hitting awkward half-swing mid-irons and forces them to use full-swing wedges for short approaches.
## [4.80.0] - 2026-03-24
### Fixed
- **AI Club Selection (The Wedge Fix):** Reversed the Fairway Oracle's search loop. The AI now evaluates short irons first, forcing it to heavily prefer 100% swings with wedges over fractional swings with long irons.
- **AI Driver Ban:** The AI is now strictly prohibited from equipping the Driver unless `currentLie === "Tee"`.
- **Beginner Variance:** Fixed a logic gap where Beginner bots (`botSkill: 0`) were defaulting to Amateur variance. Shankin' Shawn now correctly suffers from up to 200ms of swing timing error.
- **AI Putting Caps:** AI putting power is now hard-clamped between a minimum of 5% and a maximum of 110%.
## [4.80.0] - 2026-03-24
### Added
- **New Course Data:** Added the first 3 holes of "The Texas Scrapyard." Introduces `pinZ` (elevation), `tilt` (uneven lies), and `slope` (roll modifiers) to the landing zones.
- **Topography Wiring:** The engine now properly extracts Z-axis, Tilt, and Slope data from the targeted landing zones when using the `Z` key.
### Changed
- **Bot Roster Swap:** Replaced "Bot Woods" with "Shankin' Shawn" as Player 2. Shawn is configured as a Beginner (`botSkill: 0`) to test extreme physics variances and recovery scenarios.
## [4.80.0] - 2026-03-24

### Added
- **3D Topography Engine:** Introduced Z-Axis physics variables (`ballZ`, `targetZ`, `lieTilt`, `landingSlope`) to the global state to support true terrain contours.
- **Elevation Physics:** Carry distances are now mathematically altered by elevation changes. Hitting uphill reduces carry distance 1:1 with the elevation gain, while hitting downhill extends it.
- **Uneven Lie Physics:** Added side-spin induction for sidehill lies. A ball sitting above the feet (`lieTilt > 0`) automatically generates hook spin, while a ball below the feet generates slice spin.
- **Slope Roll Physics:** Forward roll distance is now multiplied or dampened based on the physical slope of the landing zone (`landingSlope`).
### Changed
- **Fairway Oracle Upgrade:** The Oracle now calculates `effectiveDist` (actual distance + elevation difference) when selecting clubs to ensure it recommends the right club for uphill/downhill approach shots.

## [4.65.0] - 2026-03-24
### Changed
- **Pre-Shot Report Verbosity:** Overhauled the `X` key formatting to be highly economical for screen readers. The report now front-loads the active club and expected distance, and dynamically omits Stance/Alignment modifiers if they are set to Neutral. (e.g., "Driver, 268 expected clear, Focus: Power, Closed Stance, Forward Address.").
## [4.64.0] - 2026-03-24
### Changed
- **Comprehensive Pre-Shot Report (Key X):** Overhauled the `X` keybinding. It now announces the active club, grip status, ball position (stance), alignment, and active shot focus. 
- **True Expected Carry:** The `X` key now calculates expected distance using the fully mature physics engine math, properly factoring in `loftDistMod` (Stance changes) and Power Focus multipliers.
## [4.63.0] - 2026-03-24
### Fixed
- **Bot Woods Audio Restored:** Forcibly removed the leftover MP3 loading logic from `audio_ag.js` that was silently swallowing the AI's audio cues. Bot Woods now correctly utilizes his 5-variant Web Audio API synthesized roar when his turn begins.
## [4.63.0] - 2026-03-24
### Fixed
- **Putting Distance Granularity:** Fixed an issue where auto-assigned putting distances were snapping to whole yards, resulting in all short putts displaying as multiples of 3 (3ft, 6ft, 9ft). The engine now correctly evaluates distances to the nearest exact foot (`Math.round(dist * 3) / 3`), adding massive variety to the short game.
## [4.61.0] - 2026-03-24
### Changed
- **Roster Update:** Replaced "Bot Palmer" with "Mulligan Moe" as Player 3. Lowered Player 3's `botSkill` to 1 (Amateur) to allow playtesting of wider shot dispersion, increased penalty frequency, and conservative Oracle advice.
## [4.59.0] - 2026-03-24
### Changed
- **Base Club Distance Scaling:** Scaled down the `baseDistance` of all clubs (Driver reduced from 255 to 230) to account for the stacking multipliers of 110% Power, Power Focus (+10%), and Back Stance (+6%). A flawless "Hero" drive now maxes out around 330 yards.
- **Stance Trajectory Physics:** Fixed a critical bug where Stance modifiers were inverted. Moving the ball Forward now correctly adds loft and *reduces* carry distance, while moving the ball Back delofts the club and *increases* carry distance.
- **Bot Audio Diagnostic:** Temporarily hijacked the `playBotWoodsSignature` function to point to a known, working proximity MP3 to diagnose browser autoplay blocking.
## [4.58.0] - 2026-03-24
### Added
- **Scorecard Telemetry Archiving:** The engine now permanently archives hole telemetry logs into the `roundData` object upon hole completion.
- **Scorecard Log Export:** While navigating the Scorecard (`Shift + E`), pressing `C` will copy the focused hole's complete telemetry report to the clipboard. Pressing `Shift + C` will compile and export the telemetry for the entire round.
### Changed
- **Dynamic Pacing Adjustments:** Increased the CPS (Characters Per Second) multiplier for AI turn delays to account for slower screen reader symbol processing. (Fast: 20ms/char, Medium: 35ms/char, Slow: 55ms/char).
### Fixed
- **AI Audio Context Suspension:** Added explicit `.resume()` commands and relative pathing (`./audio/bots/...`) to the Bot Woods audio signature to bypass strict browser autoplay policies and 404 pathing errors. Added explicit F12 console warnings if MP3s fail to load.
## [4.57.0] - 2026-03-24
### Changed
- **Dynamic TTS Pacing:** Completely overhauled the AI Turn Manager delay system. The engine now calculates the exact character length of the `lastShotReport` and dynamically scales the bot's waiting time based on the active Game Pacing setting (Fast = 15ms/char, Medium = 25ms/char, Slow = 40ms/char). This eliminates awkward silences on short putts while providing massive breathing room for complex fairway telemetry.
- **Hole Transition Pacing:** The dynamic TTS pacing is also applied to `loadHole`, scaling the AI's first tee shot delay based on the character length of the Hole and Fairway descriptions.
## [4.56.0] - 2026-03-24
### Changed
- **Power Cap Reduction:** Capped the maximum physical swing power at 110% (down from 120%). This prevents arcade-style 400+ yard drives, reigning max distances back into realistic PGA simulation ranges (maxing around 330y under perfect conditions).
- **Massive Bot Pacing Delays:** Added a flat +5000ms delay to all AI turn transitions. The "Slow" pacing mode now waits 16.5 seconds before the bot takes its turn, ensuring the screen reader has ample time to finish reading the long post-shot telemetry reports.
### Fixed
- **MP3 Audio Signature Support:** The engine now actively looks for physical MP3 files (`audio/bots/woods_roar1.mp3`) to play for Bot Woods' signature. If the files are missing, it seamlessly falls back to the native synthesized roars.
## [4.55.0] - 2026-03-24
### Added
- **Stimp Meter (Green Speed):** Added a global green speed variable. Pressing the `;` (Semicolon) key cycles the Stimp reading from 8 to 14, dynamically altering how far putts roll out.
- **Dynamic AI Audio:** Bot Woods now randomly cycles through all 5 of his synthesized audio signatures (roars) when taking his turn to add acoustic variety and personality.
### Fixed
- **X Key Yardage Math:** Fixed a bug where the `X` (Expected Distance) key was ignoring the `chokeMod` and `shotStyle` distance reductions. It now perfectly matches the physics engine's internal math.
- **Wedge Spin Exemption:** Fixed a bug that forced all Pitch and Chip shots to roll forward. Wedges and 9-Irons are now exempt from this rule, allowing their natural backspin to check the ball up or spin it backward.
- **Putter Auto-Focus:** The game now correctly auto-selects the "Touch" focus mechanic the moment you transition onto the green.
- **Bot Pacing Overlap:** Increased the AI's base turn delay from 2.5 seconds to 5.5 seconds to prevent the bot from swinging while the Caddy is still reading your post-shot report.
## [4.55.0] - 2026-03-24
### Added
- **AI Audio Signatures:** Integrated a modular, synthesized audio factory into `audio_ag.js` to generate dynamic character signatures. 
- **Bot Woods "Roar":** The Turn Manager now automatically detects when "Bot Woods" receives the turn and plays his "Type 1" acoustic signature instead of the generic UI blip. The engine stores 4 alternative roars for future AI personality/mood states.
## [4.53.0] - 2026-03-24
### Changed
- **Telemetry Markdown Formatting:** Rewrote the `Shift + C` telemetry clipboard export to use strict Markdown syntax (Headers, Bolds, and Bulleted Lists) for easy review and pasting into code editors.
- **Bot Club Selection (Loft Bias):** Upgraded the AI Brain and Caddy Oracle to prioritize high-lofted clubs (Wedges/Short Irons) for approach shots. The AI will now actively prefer the accuracy and stopping power of a wedge over a slightly closer mathematical rollout from a Wood or Long Iron.
- **Approach Magnetism Expansion:** The fairway "Hole-Out" capture radius is now linked to Global Difficulty. Playing on Casual difficulty massively expands the cup size for approach shots, allowing highly accurate pitches to drop into the hole from further away.
- **Putter Auto-Focus:** Equipping the Putter now forcefully auto-selects the "Touch" shot focus.
- **Bot Pacing Buffers:** Increased the Bot's initial turn delay on new holes from 2.5s to 4.5s to prevent the AI from interrupting the Caddy's hole description ARIA readout.
### Fixed
- **Putting Oracle NaN Crash:** Fixed a floating point error that caused the Caddy's putting fallback advice to return "aim NaN degrees" on highly complex slopes.
## [4.51.0] - 2026-03-23
### Added
- **Precision Putting Telemetry:** The post-putt shot report now details the exact miss coordinates in inches (e.g., "Missed 4 inches left and finished 14 inches past the cup"), rather than vague cardinal directions.
### Changed
- **Oracle Tree Avoidance:** The AI's `getOracleBlueprint()` now actively simulates line-of-sight tree collisions. If a simulated shot apexes below a tree canopy in its path, the Oracle applies a severe penalty, forcing the bot to calculate fades/draws to bend the ball around obstacles.
- **Caddy Aggression Bias:** The `getCaddyAdvice()` fairway logic now prioritizes maximum safe distance on Par 4s and Par 5s from the Tee and Fairway, rather than overly conservative clubbing.
- **Hazard Distance Readouts:** The `H` key (Hazard/Sight report) now explicitly announces the distance to the front edge of the hazard.
### Fixed
- **Bot Target Leakage:** Fixed a bug where bots would inherit a human player's custom `activeTargetType = 'zone'`, causing them to chip aimlessly down the fairway. Bots now strictly reset their target to the pin at the start of their turn.
- **Bot Short Game Selection:** Bots are now strictly forbidden from using Mid/Long Irons (like 4 Irons) for Chip, Pitch, or Flop shot styles, restricting them to 9 Irons and Wedges for delicate short game work.
- **Choke Down Reset:** The `isChokedDown` grip modifier now correctly resets to false after every swing.
- **Putting Oracle Fallback:** Applied the lag-putt fallback logic to the Caddy's putting advice, ensuring it no longer "shrugs" on difficult greens.
## [4.50.0] - 2026-03-23
### Changed
- **AI Common Sense Filter:** The bot Oracle will no longer consider Woods or Drivers for any target under 120 yards, forcing it to use appropriate short irons rather than hitting 19% power drives.
- **AI Humanized Power:** The bot's `botPower` calculation for fairway and tee shots is now strictly rounded to the nearest 5% increment (e.g., 85%, 90%) to simulate realistic human clock-angle swings instead of hyper-precise fractional math.
- **Putting Oracle Upgrade:** The Putting Oracle no longer operates on a strict Pass/Fail basis. If a perfectly holed putt cannot be calculated, it will return the aim and pace that results in the mathematically closest "miss" (lag putt) rather than silently failing and shrugging.
### Added
- **Global Caddy Telemetry:** The Fairway/Tee shot `Shift + C` telemetry clipboard exports now include the pre-shot `[Oracle Says: ...]` advice block, mirroring the putting diagnostic behavior.
## [4.49.7] - 2026-03-23
### Fixed
- **Hole Transition Fatal Halt:** Fixed a critical order-of-operations bug in `loadHole()` where `window.loadActivePlayer` was being called before the hole's default club was securely assigned. This caused players transitioning off the putting green to temporarily hold a Putter on the next tee box, generating a silent crash when the dashboard attempted to read the club's shot styles.
- **Load Hole Exception Armor:** Wrapped the entirety of `loadHole()` in an aggressive `try...catch` block. If any variable mismatch occurs during hole generation, the engine will now gracefully catch it and read the exact error string aloud via ARIA rather than failing silently.
## [4.49.6] - 2026-03-23
### Fixed
- **Turn Manager Fatal Halts:** Wrapped the entire `advanceTurn()` function in strict `try...catch` isolation blocks. Previously, if a bot's inherited state from a previous hole (e.g., leaving the putting green) caused a minor variable mismatch, the engine would silently crash before playing the turn-swap audio cue or scheduling the bot's swing.
- **Hole Briefing Audio Drop:** Increased the hole transition delay to a full 1000ms and added a subtle UI chime (`ui_nav_03`) to audibly confirm that the new hole has successfully loaded before the screen reader speaks the description.
## [4.49.5] - 2026-03-23
### Fixed
- **ARIA DOM-Shift Drops:** Fixed a critical bug where massive DOM repaints (like updating the dashboard during a player swap) would swallow the screen reader's ARIA updates. All Turn Manager announcements are now decoupled and wrapped in a 300ms isolation buffer to guarantee playback.
- **Hole Briefing Dropout:** Increased the hole load transition buffer from 400ms to 600ms to ensure the browser has fully reflowed the layout before announcing the new hole.
### Added
- **AI Acoustic Feedback:** Bots will now trigger a subtle UI sound (`ui_nav_01`) when they successfully calculate a shot and equip their club, providing immediate acoustic feedback that their turn is executing.
## [4.49.4] - 2026-03-23
### Added
- **Turn Manager Diagnostic (Shift + T):** Added a developer tool that reads out the raw backend state of the Turn Manager (Active Index, Bot Status, Waiting Flag, and Timeout Queue) to help debug multiplayer freezes.
### Fixed
- **Hole Transition Audio Drop:** Increased the transition delay from 150ms to 400ms. This prevents the browser's DOM layout shift (closing the scorecard) from swallowing the screen reader's ARIA live-region hole briefing.
- **Turn Manager Exception Handling:** Added strict safety fallbacks to the `advanceTurn()` and `takeAITurn()` functions to ensure undefined club references cannot silently halt the script before the bot takes its swing.
## [4.49.3] - 2026-03-23
### Fixed
- **Oracle Exception Handling:** Wrapped the AI's `getOracleBlueprint()` function in a strict `try...catch` block. If the Oracle encounters a mathematical exception (e.g., dividing by zero or missing coordinate nodes on complex fairways), the bot will no longer hard-lock the turn loop and will instead safely execute a fallback straight shot.
- **Hole Briefing Audio Drop:** Added a 150ms buffer to the automated hole briefing on transition to prevent the browser's DOM repaint from flushing the screen reader's ARIA live-region queue.
## [4.49.2] - 2026-03-23
### Fixed
- **Bot Null Oracle Crash:** Fixed a critical bug where bots would permanently freeze on the tee box if the `getOracleBlueprint()` logic failed to find a safe shot and returned `null`. The AI will now safely fallback to a default swing if the Oracle cannot provide a blueprint.
- **Stale Putting Cursor:** Fixed a multiplayer state-restoration bug where a player's putting cursor would be restored to its previous distance (e.g., 11 feet) instead of auto-snapping to their new proximity (e.g., 3 feet) after the Turn Manager swapped control back to them.
## [4.49.1] - 2026-03-23
### Added
- **Telemetry Hole Header:** Pressing `Shift + C` now prepends the Hole Number, Par, Distance, and Pin Location to the top of the clipboard export.
- **Bot Manual Pacing Reminder:** If Game Pacing is set to Manual and the player presses an arrow key during a bot's turn, the engine will explicitly remind the user to "Press Spacebar to let them strike."
### Fixed
- **Hole Transition Audio Overlap:** Removed legacy `window.announce` calls from the `Enter` and `Digit` key handlers that were instantly overriding and killing the new auto-hole descriptions.
- **Telemetry State Leakage:** Enforced a strict `holeTelemetry = []` wipe at the absolute top of the `loadHole()` function to prevent shots from previous holes appending to the clipboard export.
## [4.49.0] - 2026-03-23
### Added
- **Unplayable Lie Confirmation:** Added a two-stage safety lock to the `U` key to prevent accidental penalty strokes.
- **Multiplayer Player ID:** The `Tab` key now prepends the active player's name (e.g., "[Bot Woods] Hole 5...") for better situational awareness.
- **Auto-Hole Description:** Loading a new hole automatically triggers the hole's narrative description, fairway layout details, and announces who has honors.
### Fixed
- **Bot Putting Freeze:** Fixed a state leakage bug where bots remained in `isPutting = true` mode after transitioning to a new hole, causing the engine to freeze on the next tee.
- **Zero-Foot Putt Bug:** Applied a 1-foot minimum floor to the auto-snapping putting cursor so it no longer breaks when 0.3 yards or closer to the cup.
- **Oracle Putting Advice:** Fixed a telemetry glitch where the Caddy would mistakenly use the short-game fairway oracle on the green and advise the player to "lay up" on a putt.
## [4.48.2] - 2026-03-22
### Fixed
- **Bot Input Lock:** Humans can no longer use `ArrowDown` or `ArrowUp` to initiate a swing while it is a bot's turn, preventing users from accidentally clearing the timeout queue and freezing the game.
- **Pacing Optimization:** Reduced the agonizing 4.5-second dead-air delay between the Turn Manager's player swap announcement and the bot's setup phase.
- **True Golf Honors:** The Turn Manager now accurately awards tee box honors to the player who recorded the lowest stroke count on the previous hole.
- **Multiplayer Persistence:** The `players` roster is now correctly serialized into `localStorage`, ensuring multiplayer profiles and bot skill levels survive browser refreshes.
## [4.48.1] - 2026-03-22
### Fixed
- **AI Putting Cursor Bug:** Fixed an issue where bots would not update their `puttTargetDist` after a missed putt, resulting in fractional power calculations (e.g., hitting 0% power) and getting trapped in an infinite loop near the hole.
- **HTML Typo:** Fixed a malformed `<head>` tag in the `index.html` file.
## [4.48.0] - 2026-03-22
### Added
- **Game Pacing Modes (Key P):** Added 4 pacing tiers for multiplayer/bot rounds (Fast, Medium, Slow, Manual). Fast adds +1000ms to the base delay, Medium adds +2500ms, and Slow adds +4000ms to allow screen readers ample time to finish. 
- **Manual Bot Execution:** In Manual pacing mode, the engine pauses and waits for the player to press `Spacebar` before the bot strikes the ball, ensuring zero audio overlap and preventing accidental player swings on the `Down Arrow`.
### Fixed
- **The Short Game Brain:** Upgraded the Oracle AI. Bots inside 100 yards will now actively cycle through Pitch, Half-Pitch, and Chip `shotStyles`, and will calculate fractional power (e.g., 62%) rather than trying to hit 100% full swings with a Sand Wedge from 20 yards away.
- **Long Putt Fallback:** Fixed a bug where the AI would aim sideways on impossibly long putts (45+ feet). It now falls back to aiming straight with estimated distance pace.
- **Prefix Preservation:** Fixed a bug where missed putts would overwrite the `[Player Name]` prefix in the Caddy report.
## [4.47.0] - 2026-03-22
### Added
- **Ghost Swing AI:** Replaced Player 2 with "Bot Woods" (Tour Skill Level).
- **Automated Execution:** The Turn Manager now detects if the active player is a bot. If so, it silently queries the Oracle Blueprint, auto-equips the ideal setup, rolls randomized timing mechanics based on its skill level, and automatically executes the shot.
- **Seamless Acoustic Integration:** Bot shots dynamically trigger the same physical strike, wind, bounce, and roll audio textures as human players, creating an immersive, real-time multiplayer experience.
## [4.46.0] - 2026-03-22
### Changed
- **Asymmetric Player Profiles:** The Multiplayer Turn Manager now isolates and swaps 8 additional game settings per player. `difficultyIndex`, `caddyLevel`, `activeBallIndex`, `shotStyleIndex`, `isChokedDown`, and the three Dev Shortcuts (`devPower`, `devHinge`, `devImpact`) are now bound to the individual player's profile rather than bleeding globally across all players.
## [4.45.1] - 2026-03-22
### Fixed
- **ARIA Interruption Bug:** Added a 4,000ms delay before the Turn Manager swaps players after a fairway shot, ensuring screen readers have enough time to read the shot summary before the "Swapped to Player 2" announcement overrides it.
- **Telemetry Ownership:** The active player's name is now automatically prefixed to all Caddy Reports, `holeTelemetry` exports, and the `Shift + Semicolon` diagnostic tool, ensuring clarity during multiplayer rounds.
## [4.45.0] - 2026-03-22
### Added
- **Automated Turn Manager:** The engine now enforces standard golf etiquette. After every shot, the engine calculates the absolute distance to the pin for all incomplete players and automatically swaps control to the "Away Player" (the player furthest from the hole).
- **Multiplayer Hole Completion:** The game no longer advances or prompts the next hole until all active players have successfully holed out.
### Changed
- **Roster Persistence:** Fixed an issue where `loadHole` was wiping player data on every new tee box. The engine now correctly resets geometric coordinates while preserving player identities and total scores.## [4.44.0] - 2026-03-22
### Added
- **Multiplayer State Infrastructure:** Converted the global single-player sandbox into a dynamic `players` array capable of tracking independent coordinates, lies, strokes, and equipment for multiple players simultaneously.
- **Manual Hot-Seat Swapping (Key M):** Added a developer hotkey to manually cycle control between Player 1 and Player 2 mid-round. The engine actively saves and loads spatial coordinates, allowing players to play competitive rounds against themselves for playtesting.
## [4.43.2] - 2026-03-22
### Changed
- **Auto-Focus Refinement:** Rebuilt the `autoSetFocus` priority tree to ensure contextual accuracy. 
  - Hazard lies (Sand/Rough) now strictly override all other conditions to guarantee Recovery Focus is equipped.
  - Equipping a Wood in the Fairway now correctly auto-equips Power Focus.
  - Touch Focus is now strictly reserved for Wedges/9-Irons when `< 50 yards` from the pin, allowing standard approaches to properly utilize Accuracy Focus.
## [4.43.1] - 2026-03-22
### Fixed
- **Fairway Diagnostic Bug:** Fixed an issue where `Shift + Semicolon` would read old putting telemetry after hitting a fairway shot. The fairway delta math has been successfully re-injected.
### Added
- **Practice Swing Diagnostics:** Practice swings (`ArrowUp`) now actively populate the `lastTimingReport`, allowing players to check their timing accuracy and mechanics before hitting the real shot.
## [4.43.0] - 2026-03-22
### Added
- **Dynamic Timing Diagnostics (Shift + Semicolon):** Added a rapid-fire telemetry readout (`:`) that isolates the player's mechanical performance on the previous shot.
- **Backspin Delta Math:** The engine now calculates a "sterile baseline" backspin for every shot based on the active club, stance, and style. The Diagnostic Report compares the player's actual generated RPM against this baseline to clearly demonstrate how their swing timing (or Spin Focus) altered the ball's stopping power.
## [4.42.0] - 2026-03-22
### Added
- **Global Difficulty Scaling (Key I):** Added four difficulty tiers (Casual, Amateur, Pro, Tour) to scale the game's challenge to the player's physical reaction times. 
- **The Reflex Buffer:** Rebuilt the power penalty system. On lower difficulties, players can safely overswing up to 108% (Amateur) or 112% (Casual) before "over-torque" penalties shrink the sweet spot. This safely absorbs normal human auditory reaction times without punishing the player for swinging past the 100% audio cue.
- **Dynamic Sweet Spots:** The Impact window (+/- 60ms) and Spacebar Hinge window (+/- 50ms) now dynamically expand up to 2.0x (Casual) or shrink down to 0.6x (Tour) based on the active difficulty.
- **Dispersion Scaling:** Mishit slices and hooks are mathematically reduced by 50% on Casual and 25% on Amateur to keep balls playable.
## [4.41.1] - 2026-03-21
### Fixed
- **Fairway Oracle Restoration:** Fixed an architectural overwrite bug where the Putting Oracle patch accidentally masked the Fairway Oracle's simulation loops. The Level 3 God Caddy will once again calculate the perfect club, stance, and aiming angle from the tee box and fairway.
## [4.41.0] - 2026-03-21
### Added
- **Contextual Shot Focus (Key J):** Added 6 distinct "Focus Modes" (Standard, Power, Touch, Spin, Accuracy, Recovery). The engine dynamically auto-equips the most logical Focus based on your club, distance, and lie (e.g., pulling a Driver on the Tee auto-equips Power Focus; Wedges auto-equip Touch Focus).
- **Risk/Reward Hinge Scaling:** The Spacebar Hinge timing now acts as a linear multiplier for Focus effects.
  - `< 50ms:` 100% Maximum Buff (e.g., +10% Distance).
  - `50ms to 150ms:` The buff gracefully bleeds down to 0 (Standard Shot).
  - `150ms to 250ms+:` The buff flips into a Negative Penalty (e.g., -10% Distance, 50% wider dispersion, shrunk cup radius). 
- **Natural Dispersion Circle:** Perfectly struck fairway shots now inherently suffer a natural scatter of +/- 1.5% distance and +/- 5% of the club's Max Dispersion laterally. Accuracy Focus cuts this natural scatter in half.
## [4.40.0] - 2026-03-21
### Fixed
- **Perspective Audio Panning:** Corrected the putting audio engine. `panValue` is now calculated relative to the player's Line of Sight vector rather than absolute World Coordinates, ensuring breaking putts accurately pan left or right of the player's center view.
- **Oracle Arrogance Bug:** The God Caddy (`getCaddyAdvice`) now assumes a standard 5.4-inch hole (`tempoBonus = 1.0`) when calculating putting lines. This forces the Oracle to provide exact micro-degrees of aim for short breaking putts, rather than lazily assuming the player will trigger the Touch Magnetism expanded cup.
- **Target Distance Formatting:** Cleaned up the raw Caddy broadcast to round fractional yardages (e.g., 0.66666y) to a readable single decimal place.
## [4.39.0] - 2026-03-21
### Added
- **Automatic Diagnostic Memory:** The physics engine now silently polls the God Caddy (Oracle) and the Green Reading algorithm the millisecond a putt is struck. These predictive reads are automatically injected into the `Shift + C` telemetry report, allowing playtesters to directly compare the expected math against the actual physical result of the ball without needing to manually trigger the `A` or `B` keys before the shot.
## [4.38.0] - 2026-03-21
### Added
- **Advanced Putting Telemetry:** Massively expanded the post-putt Caddy Report to include deep diagnostic data for playtesting. The Caddy (via `Shift + C` or terminal export) now reports the exact Target Cursor location, Aim Angle, Impact/Touch timings, Touch Magnetism multiplier, Slope Dampener percentage, and the literal Effective Hole Radius in inches.
## [4.37.3] - 2026-03-21
### Fixed
- **Synchronized Putting Oracle:** Rebuilt the `getCaddyAdvice` Brute-Force Oracle to inherit the `slopeDampener` and `tempoBonus` variables introduced in the recent short-game overhauls. The Caddy will no longer drastically overestimate breaks on short putts. 
- **Oracle Unit Scaling:** The God Caddy now accurately reports its putting advice in "feet" rather than "yards" when the target is inside 5 yards, matching the UI Grid standards.
i## [4.37.2] - 2026-03-21
### Fixed
- **Terrain Probe Restoration:** Corrected an issue in the physics engine where the `getTerrainAt` helper was accidentally overwritten by the Landing Zone Oracle. Both functions are now running concurrently, allowing the Pin Finder to correctly announce both the surface terrain (e.g., "In the Rough") and the tactical green slopes (e.g., "kicks left and feeds downhill").
## [4.37.1] - 2026-03-21
### Added
- **Landing Zone Oracle:** Restored and enhanced the predictive tactical feedback in the Pin Finder. When the target cursor is placed on the green, the engine now probes the `greenDictionary` contours at those exact coordinates and announces the predicted landing effect (e.g., "Landing here kicks right and feeds downhill").
## [4.36.2] - 2026-03-21
### Fixed
- **Vertical Break Dampening:** Corrected an oversight in the putting physics where the extreme slope dampener for short putts was only being applied to the horizontal axis (`sx`). It is now correctly applied to the vertical axis (`sy`), preventing short uphill putts from dying prematurely.
## [4.36.1] - 2026-03-21
### Changed
- **Linear Touch Magnetism:** Replaced the hard-stepped tiers for Hinge timing bonuses with a smooth, linear mathematical curve.
  - Timings between 0ms and 50ms still guarantee the maximum hole expansion.
  - Timings between 50ms and 150ms now gracefully scale down to 1.0x, rewarding players proportionally for their accuracy rather than penalizing them with arbitrary drop-offs.
## [4.35.1] - 2026-03-21
### Fixed
- **Grid Scaling Logic:** Removed the strict `currentLie === "Green"` requirement for dynamic grid scaling. The Pin Finder will now smoothly transition to 1-foot increments anytime you are inside 5 yards of the pin, regardless of the terrain you are standing on or aiming at.
- **Putting Target Unification:** Applied the dynamic 1-foot scaling logic directly to the Putting Green targeting system (`puttState === 0`), ensuring distance adjustments are made in feet on short putts.
## [4.35.0] - 2026-03-21
### Added
- **Dynamic Putting Grid:** The Pin Finder grid now automatically scales its resolution based on distance. Outside 5 yards, it moves in 1-yard increments. Inside 5 yards, it shifts to 1-foot (1/3 yard) increments for surgical precision, complete with dynamic TTS distance announcements (e.g., "4 feet left, 2 feet short").
- **Tap-In Magnetism:** Putts originating inside 3 feet (1 yard) now feature a massively expanded hole hit-box (300% larger). A standard, well-struck tap-in is now a guaranteed "gimme," virtually eliminating lip-outs on trivial putts.

### Changed
- **Break Dampening Mechanics:** Added a severe friction/gravity dampener to putts inside 5 yards. Because short putts are struck with enough velocity to cut through slopes, the engine now ignores 70% of lateral break on short putts, preventing the unrealistic "14-degree aim" requirement.
## [4.34.2] - 2026-03-21
### Fixed
- **Tactile UI Audio Routing:** Corrected an issue where UI sounds were failing to trigger. The engine now properly routes UI interactions through the internal `golf_audio_bank.js` synthesizer rather than looking for external MP3 files.
- **Audio Bank Expansion:** Injected 4 new procedural sound recipes from the Bunker Audio Laboratory directly into the primary audio bank (`bunker_03`, `bunker_04`, `bunker_11`, and `bunker_33`) to provide mechanical feedback for shot selection and menu navigation.
## [4.34.0] - 2026-03-21
### Added
- **Tactile UI Audio Integration**: Mapped high-fidelity recorded assets from the AG Sound Tester and Bunker Audio Laboratory to core navigation and setup keys.
  - **Clubhouse Confirmation**: Mapped **AG Bank #11** to the `Enter` key for a clean selection "ping" when starting rounds or entering practice modes.
  - **Shot Style Switching**: Mapped **Bunker Lab #03** (Forward) and **Bunker Lab #04** (Reverse) to the `S` and `Shift + S` keys to provide mechanical feedback when changing swing shapes (Full, Punch, Pitch, etc.).
  - **Quick Bag Check**: Mapped **Bunker Lab #11** to the `Q` key to provide an auditory "click" when checking current equipment and lie.
  - **Menu Exit**: Mapped **Bunker Lab #33** to the `Escape` key for a distinct "back-out" sound when closing the scorecard or help menus.

### Changed
- **Version Synchronization**: Bumped all internal telemetry, dashboard headers, and script cache-busting references to v4.34.0.
## [4.33.0] - 2026-03-21
### Added
- **Target Lie Laboratory:** Introduced `Shift + L` on the Driving Range and Chipping Green to toggle the terrain at the target location.
- **Practice Surface Physics:** Changing the Target Lie now dynamically alters ball behavior and audio during practice:
    - **Green:** Triggers Theatrical Rolls and MP3 Proximity Rewards.
    - **Water:** Triggers splash effects and penalty resets.
    - **Sand:** Triggers sand-splash audio and kills ball roll.
- **Command Feedback:** The Caddy now announces the active target terrain upon toggling.
## [4.32.0] - 2026-03-21
### Added
- **Dynamic Audio Asset Reveal**: Replaced synthesized proximity chimes with a randomized lookup system for recorded .mp3 assets located in the `/audio` folder.
  - **Inside 7 Yards**: Randomly selects `test_close1.mp3` or `test_close2.mp3`.
  - **7 to 12 Yards**: Randomly selects `testmid1.mp3` or `testmid2.mp3`.
  - **Outside 12 Yards**: Randomly selects `test_far1.mp3` or `test_far2.mp3`.
- **Synchronized Caddy Logic**: Implemented an event listener to detect the duration of proximity audio assets, ensuring the Caddy TTS report waits until 500ms after the recorded clip finishes.

### Changed
- **Theatrical Pacing**: Reduced the "Dramatic Pause" between the ball settling on the green and the audio reveal from 2000ms to 1000ms to tighten gameplay flow.
- **Version Migration**: Updated all global telemetry and script cache-busting references to v4.32.0.
## [4.31.8] - 2026-03-21
### Added
- **Theatrical Green Roll:** Decoupled the audio roll duration from raw physics on approach shots. Balls landing on the green now trigger a suspenseful 3 to 6-second decelerating metronome to build tension before settling.
- **Proximity Audio Rewards:** Added a tiered chime system during a new 2-second "dramatic pause" before the Caddy speaks.
  - **Tier 1 (Inside 7 yards):** Plays a triumphant 3-note major chord arpeggio ("The Dart").
  - **Tier 2 (Inside 12 yards):** Plays a positive double-chime ("Great Shot").
## [4.31.7] - 2026-03-21
### Changed
- **Audio Texture Cleanup:** Removed the simulated "Brown Noise" grass rustling effect from fairway and rough landings to eliminate residual static and keep the shot audio punchy and clean.
- **The Green Auditory Reward:** If an approach shot successfully lands and stays on the Green, the engine now seamlessly transitions from the landing bounces into the rhythmic, slowing "ping" metronome used in the putting engine. This provides instant, visceral feedback that the player has hit the putting surface.
## [4.31.6] - 2026-03-21
  ### Fixed
- **Grass Roll Static Clipping:** Fixed an issue where the spatial audio roll sequence would result in a harsh burst of static. The engine now uses a native `BiquadFilterNode` configured as a 350Hz low-pass filter to generate smooth, muffled "Brown Noise" that realistically mimics a ball rolling through fairway grass without clipping the audio context.## [4.31.5] - 2026-03-21
### Changed
- **Ball Equipment Flavor:** Updated the temporary technical names of the audio ball profiles to humorous, knock-off brand names (e.g., "Title-ish", "Semi-Pro V1") to lay the groundwork for a future unlockable career progression system.
## [4.31.4] - 2026-03-21
### Fixed
- **Spatial Audio Continuity:** Fixed an auditory jarring effect where a heavily panned shot shape would correctly land in the left or right ear, but the subsequent cascading bounces and grass roll would snap back to the center audio channel. Bounces and rolls now inherit the exact spatial panning coordinate of the ball's final landing spot.
## [4.31.3] - 2026-03-21
### Added
- **Ball Equipment Selection:** Players can now press `Shift + Y` to cycle between 6 distinct "Ball Types." Each ball has a unique auditory signature (waveforms and landing frequencies) to suit different sensory preferences.
- **Live 3D Flight Integration:** The 3D audio flight harness has been fully wired into the core `calculateShot()` engine. Every shot now dynamically generates pitch, rhythm, and panning based on the live `hangTimeSecs`, `dynamicLoft`, and `lateralTotal` dispersion of that specific swing.
### Changed
- **Layered Acoustics:** The new 3D flight pings are successfully layered *over* the existing aerodynamic "whoosh" sound to create a rich, realistic audio bed.
- **Bounce Stretching:** The gap between the first and second landing bounce has been mathematically stretched, capping at exactly 600ms on massive drives to create a heavier, more realistic sense of impact.
## [4.31.2] - 2026-03-21
### Changed
- **3D Audio Sandbox Expansion:** Upgraded the temporary flight test harness (`Shift + Y`) to support dynamic waveforms (`sine`, `square`, `triangle`, `sawtooth`) and variable landing frequencies.
- **Flight Profiles:** Expanded the test harness from 4 to 6 samples to test different acoustic textures for shot shapes, including "The Moonball Wedge" (sharp, biting landing) and "The Rough Flier" (wobbly flight, deep thud).
## [4.31.1] - 2026-03-21
### Fixed
- **3D Flight Panning:** Fixed an issue in the 3D flight prototype where the final landing thud was defaulting to the center audio channel. The landing thud now correctly inherits the final spatial panning value of the shot shape, dropping perfectly into the left or right ear.
## [4.31.0] - 2026-03-20
### Added
- **3D Flight Audio Prototype:** Introduced a temporary audio test harness to simulate the physical flight path of a golf ball using spatial audio.
  - **Height via Pitch:** The frequency of the audio rises to a peak at the apex of the shot and falls as the ball drops.
  - **Speed via Rhythm:** Simulates the "Magnus Effect" by firing rapid audio pings at launch, slowing them down as the ball balloons and stalls at the apex, and slightly accelerating as gravity pulls it down.
  - **Shape via Stereo Panning:** The audio physically travels between the left and right headphone channels to simulate shot shapes (Draw, Fade, Slice, Stinger).
- **Test Harness Controls:** Added a temporary hotkey (`Shift + Y` while idle on the course) to trigger and cycle through 4 distinct, pre-programmed 3D audio flight paths for testing and calibration.
## [4.30.3] - 2026-03-20
### Fixed
- **Scorecard Announcement Conflict:** Removed the legacy hardcoded keyboard announcement for `Shift + E` so the new dynamic, paginated ARIA announcement can play uninterrupted.
### Added
- **Putt Distance Metric:** Added a 6th column to Page 1 (Traditional) of the Scorecard to display the exact length of the final holed putt ("Putt Dist") for each hole.
## [4.30.2] - 2026-03-20
### Changed
- **Paginated Scorecard:** The Scorecard (`Shift + E`) has been split into two distinct pages to prevent column bloat. 
  - **Page 1 (Traditional):** Hole, Par, Score, Result, Putts.
  - **Page 2 (Analytics):** Hole, Drive Dist, FIR, App. Start, App. Prox, GIR.
- **Scorecard Navigation:** Pressing `Spacebar` while viewing the scorecard toggles seamlessly between the pages, automatically announcing the page name and your current grid coordinate.
- **Contextual ARIA Overhaul:** The initial opening of the scorecard now dynamically reads the course name and explicitly instructs the user to "Use Spacebar to flip pages."
## [4.30.1] - 2026-03-20
### Changed
- **Broadcast-Style Journaling:** Upgraded the hole narrative generator to sound like a live sports broadcast, dynamically stitching together your drive distance, approach distance, proximity, putt length, and running score.
- **Advanced Stat Tracking:** The engine now permanently tracks and saves `approachStart` (yards) and `approachProx` (absolute distance from pin) into the `roundData` array for every completed hole, establishing the foundation for future advanced analytics.
## [4.30.0] - 2026-03-20
### Added
- **Hole Journaling:** Sinking a putt now automatically generates a short, broadcast-style narrative of the completed hole (e.g., "Hole 4 (Par 4): Reached the green in 2 strokes and 2-putted for Par.").
- **Hole Review (Key N):** Pressing `N` during a round will now announce the narrative summary of the most recently completed hole.
- **Top 2 Highlight Tracking:** The engine now silently tracks the Top 2 longest fairway Drives, the Top 2 closest Sniper Approaches, and the Top 2 longest Clutch Putts made throughout the round.
- **Post-Round Share Sheet (Shift + N):** Completing Hole 18 now transitions the game into a `post_round` state, automatically opening the scorecard. Pressing `Shift + N` in this state compiles your score and your Top 2 highlights into a formatted text block and copies it to your clipboard for easy sharing.
## [4.29.0] - 2026-03-20
### Added
- **Oracle Chime:** The Level 3 God Caddy now plays a distinct "insight" chime (`caddy_02`) immediately before delivering a fairway target or putting read.
- **Ambient Wind:** Loading into a hole with high winds (Gusts or Windy) now triggers a subtle ambient wind sound (`env_02`) on the tee box to provide immediate auditory weather context.
- **Hazard Splashes & Sand:** Rolling into a bunker now triggers a distinct sand friction sound (`hazard_03`). Hitting into a water hazard replaces the generic splash with a heavy, plunging water impact (`hazard_05`).
## [4.28.0] - 2026-03-20
### Added
- **Dynamic Scoring Chords:** Replaced the generic end-of-hole chime with contextual, musical fanfares based on the player's score relative to par:
  - **Eagle/Albatross:** Grand, ascending fanfare (`score_04`).
  - **Birdie:** Bright, ascending arpeggio (`score_03`).
  - **Par:** Solid, warm C-Major chord (`score_01`).
  - **Bogey:** Two descending low tones (`score_05`).
  - **Double/Triple Bogey:** Mournful, sustained minor chords (`score_06` / `score_07`).
- **Lip-Out Audio:** Missing a putt by overpowering it through the cup now triggers a dissonant metallic rattle (`putt_06`).
- **Cup Sink Audio:** Sinking a putt now triggers a crisp, echoing ball-in-cup sound (`score_02`) instead of the standard UI beep.
## [4.27.0] - 2026-03-20
### Added
- **Dynamic Strike Audio:** Replaced the static impact tone with contextual strike sounds based on timing accuracy:
  - **Flushed (`flight_01`):** A deep, resonant strike for perfect or near-perfect timing.
  - **Thin (`flight_02`):** A sharp, high-pitched click for early impacts (swinging ahead of the ball).
  - **Fat (`flight_03`):** A muffled, dead thud for late impacts (hitting the ground behind the ball).
- **Whiff Recovery Audio:** Completely missing the impact window now plays a distinct descending "whiff" sound (`swing_10`) instead of a generic strike.
- **Overpower Warning:** Locking in a swing above 105% power now triggers a harsh sawtooth buzz (`swing_07`) the exact moment the downswing begins, providing immediate auditory feedback that you are risking extreme dispersion.
## [4.26.0] - 2026-03-20
### Added
- **Auditory Equipment Feedback:** Equipping clubs via `PageUp` or `PageDown` now triggers distinct audio cues based on the club category (Driver/Wood boom, Iron ping, Wedge clink, Putter tap).
- **Target Cycling Rhythm:** Pressing `Z` to cycle fairway and green landing zones now plays a soft, double-blip audio cue (`ui_nav_07`) to provide a rhythmic sense of movement through the targets.
- **Target Lock Confirmation:** Pressing `Enter` to lock a coordinate in the Pin Finder grid now triggers a distinct "Target Locked" echo cue (`club_09`) to confirm the selection before returning to swing mode.
# [4.25.0] - 2026-03-20
### Added
- **Keyboard Explore Mode (F12):** Added an accessibility toggle. When active, pressing any mapped key announces its function without executing the action in the game.
- **Green Reading (Key B):** When on the putting green, pressing 'B' translates the mathematical slope data into realistic golfer terminology (e.g., "plays 8 inches uphill, breaks 15 inches outside the right edge").
### Changed
- **Brute-Force Putting Oracle:** The God Caddy (Level 3) no longer guesses putts. It now runs a hidden, real-time 360-degree gravity simulation to find the exact aim and pace required to sink the putt.
- **Wider Cup:** Increased the putting `captureRadius` from 0.4 to 0.8 yards, making the hole slightly more forgiving for near-misses.
- **Hazard Proximity Formatting:** The Hazard List (`Key H`) now explicitly announces the raw distance to the obstacle before reading the edge-clearance math.
- **Hole 12 Navigation:** Adjusted the 'Corner Drive' landing zone from 280y back to 240y for a more realistic tactical target.
### Fixed
- **Putting Aim Retention Bug:** Fixed an issue where a missed putt would retain its lateral `aimAngle` for the subsequent putt. Missing a putt now correctly resets your aim dead-center to the cup.
## [4.24.0] - 2026-03-20
### Added
- **Scorecard Analytics Overhaul:** Expanded the Scorecard grid from 6 to 8 columns. It now tracks:
  - **Result:** Calculates and displays proper golf terminology for the hole (Birdie, Bogey, Eagle, Par, etc.).
  - **Drive Distance:** Tracks the total yardage of the tee shot on Par 4s and Par 5s.
  - **Putt Distance:** Records the exact length of the final successful putt made on the green.
- **Fractional Aggregation:** The "Totals" row at the bottom of the scorecard now correctly calculates and announces FIR (Fairways in Regulation) and GIR (Greens in Regulation) as true fractions (e.g., "7 of 11") rather than simple yes/no outputs.
## [4.23.1] - 2026-03-20
### Fixed
- **Landing Zone Arrays:** Corrected erroneous landing zone data on Holes 1, 4, 6, and 7 to match their actual topographies.
- **Oracle Putting Math:** Inverted the Caddy's putting aim logic. If a slope breaks right, the Oracle now correctly advises aiming left to counteract it.
- **Extreme Physics Cap:** Hard-capped `sideSpinRPM` at 4,500 to prevent mathematical glitches (like 500-yard lateral kicks) on massive whiffed swings.
### Changed
- **Shot Report Reordering:** The Caddy now announces the shot description, roll, and proximity *before* announcing the current stroke and remaining distance to the pin, leaving the most actionable data fresh in the player's mind.
- **Putting Narratives:** Replaced static "Target was X yards" with dynamic "You sank a putt from X feet away!" and "Missed a X foot putt."
- **Enhanced 'T' Key Sight Report:** Pressing T now actively scans for Bunkers and Water Hazards in your direct line of sight, not just trees.
- **Hazard Narrow Misses:** The engine now detects if a ball flies just outside the boundary of a bunker or water hazard and adds a "shaved the edge" narrative to the shot report.
### Added
- **Unplayable Lie (Key U):** Players hopelessly lost in the deep woods can now press `U` to take a 1-stroke penalty and drop their ball back in the center of the fairway at their current distance.
## [4.23.0] - 2026-03-20
### Added
- **The Back 9 Expansion:** Officially completed "The Holodeck Links" 18-hole championship routing. Added Holes 10 through 18, featuring complex risk/reward par 5s, heavily guarded par 3s, and the grueling 440-yard finishing hole with "The Augusta Monster" green complex.
- **Advanced Tactical Layouts:** The new holes are specifically designed to test all modern engine features. Players will face overhanging tree canopies requiring punch-outs, narrow fairway target zones forcing club-downs off the tee, and severe green topographies that make the Pin Finder (Shift + Z) essential for scoring.
## [4.22.1] - 2026-03-20
### Fixed
- **Tree Collision Acoustic Timing:** Fixed an issue where the TTS announcement and sound effects for hitting a tree (both Natural and Synth) would play instantly upon impact. The engine now calculates the exact millisecond the ball reaches the tree in mid-air (`hangTimeSecs * flightFraction`) and delays the collision audio and announcements to sync perfectly with the flight path.
## [4.22.0] - 2026-03-20
### Changed
- **The Canopy Gamble (Tree Physics Overhaul):** Replaced the guaranteed "Drop" penalty for tree collisions with a 3-tier RNG system when aiming through a canopy:
  - **The Narrow Miss (33%):** The ball punches through the leaves. Power is halved, and lateral dispersion is multiplied by 3. (Audio: Rustling leaves).
  - **The Glancing Blow (33%):** The ball clips a thick branch. Deflects 18-23 yards laterally toward the center of the fairway to ensure an unblocked next shot. (Audio: High-pitch crack).
  - **The Solid Strike (33%):** The ball hits the trunk dead-on. Drops 8-13 yards laterally toward the fairway. (Audio: Low-pitch thud).
- **Fairway Kick Logic:** Tree deflections no longer bounce randomly; they actively calculate the direction of the fairway center (`x: 0`) and kick the ball horizontally to prevent players from getting stuck in an infinite "blocked" loop.
## [4.21.1] - 2026-03-20
### Changed
- **Comprehensive Help Menu:** Completely overhauled the in-game Help Menu (`?` key). The documentation now includes full instructions for all modern engine features, including Practice Swings, the Pin Finder, Target Cycling, Quick Status, Scorecard navigation, and Choke Grip.
- **Screen Reader Formatting:** Refactored the Help Menu text array to explicitly announce heading levels, ensuring a smoother and more structured reading experience when navigating with the Up and Down arrows.
## [4.21.0] - 2026-03-19
### Added
- **Quick Status (Tab Key):** Introduced a "Quick Summary" hotkey. Pressing Tab now instantly announces your current Hole, Stroke, Distance to Pin, and Lie without the extra telemetry of the Caddy Report.
- **Under the Canopy Logic:** Overhauled the 'Edge Finder' (Key H). If you are within the radius of a tree, the engine now simulates being "under the canopy." It calculates escape angles based on the trunk width rather than the full branches, allowing for realistic 5-15 degree punch-outs.
### Changed
- **Relative Target Awareness:** The 'Z' key now detects if a landing zone is behind your current ball position. If so, it appends "(Behind you)" to the announcement to prevent accidental backwards shots.
- **Improved Tree Physics:** Replaced the "Vertical Drop" mechanic with "Deflection Physics." Hitting a tree now results in a 70-90% velocity loss and a randomized bounce angle, rather than dropping the ball directly behind the trunk.
## [4.20.0] - 2026-03-19
### Added
- **Fairway Target Density:** Populated Holes 1-9 with strategic "Fairway Landing Zones" every 80-120 yards. These new targets are prepended to existing landing zone arrays to ensure green-side targets are preserved.
### Fixed
- **Z-Key Cycle Logic:** Overhauled the `Z` key behavior to intelligently cycle through all targets in a loop: [The Pin] -> [Fairway Zones] -> [Green Zones] -> [The Pin].
- **Auto-Equip Integration:** The cycle now triggers the Auto-Equip system on every step, ensuring the player is handed the correct club for the newly targeted distance.
## [4.19.7] - 2026-03-19
### Changed
- **Style Cycle Logic:** Reordered the 'S' key navigation. Pressing 'S' now advances forward through the styles (e.g., Normal -> Pitch -> Chip -> Flop), while 'Shift + S' moves in reverse.
- **Integrated Distance Feedback:** Cycling through shot styles now automatically announces the expected distance (at 100% power and perfect impact) based on your current club, lie, and stance. This eliminates the need to press 'X' after every style change.
- **Shot Style Reset:** The engine now automatically resets your Shot Style to "Normal" after every shot and when starting a new hole, ensuring you never accidentally start a tee shot with a Flop style equipped.
## [4.19.6] - 2026-03-19
### Added
- **Shot Recovery Failsafe:** Introduced a 20-second "watchdog" timer that fires when the ball is hit. This ensures that even if a logic error occurs during flight, the game will automatically restore keyboard input and reset to a playable state.
- **Robust Physics Exception Handling:** Wrapped the shot resolution engine in a try/catch architecture. Any unexpected data errors during the caddy report or transition sequence now trigger a "Safe Recovery" instead of freezing the browser tab.

### Fixed
- **State Deadlock Prevention:** Reinforced the transition between Flight (State 5) and Idle (State 0) to ensure the player is never locked out of their controls after a tee shot.
- **Script Cache Synchronization:** Unified all versioning and cache-busting tags to 4.19.6.
## [4.19.5] - 2026-03-19
### Fixed
- **Atomic State Recovery:** Synchronized all files to the latest versioning.
- **Recursive Timer Tracking:** Updated the physics engine to ensure that EVERY timeout—including nested flight timers, caddy sequences, and recursive putting roll steps—is registered in the `stateTimeouts` array. 
- **The "Hole 2" Freeze:** Warp commands now successfully kill any pending Green Transitions or Post-Shot Reports from the previous hole, preventing state-bleeding and "ghost" putting modes.
## [4.19.4] - 2026-03-19
### Fixed
- **Ghost Timer Collision:** Resolved a critical bug where the game would freeze or behave erratically on the second hole of a session. This was caused by "ghost" timeouts from the previous hole's caddy report or green transition firing during the next hole's tee shot.
- **Global Timeout Clearing:** Updated `loadHole` to explicitly clear all pending timeouts (milestones, flight sequences, caddy reports, and green transitions) before initializing a new hole.
- **State Integrity:** Standardized the tracking of all `setTimeout` calls within the `calculateShot` engine, ensuring they are correctly added to the `stateTimeouts` registry.
- **Green Transition Safety:** Fixed a race condition where the engine would remain in "Flight Mode" (State 5) for too long during green transitions, appearing as a "freeze" to the player.
## [4.19.3] - 2026-03-19
### Fixed
- **Universal Target Auto-Equip:** Standardized club selection behavior. Now, whenever the player uses the `Z` key to switch targets (cycling between Pin and Landing Zones), the engine automatically evaluates the new distance and equips the most appropriate club based on the current lie and stance.
## [4.19.2] - 2026-03-19
### Added
- **Dynamic Landing Zones:** The `loadHole` function now automatically injects a "Green Approach" landing zone into any hole that lacks defined zones (specifically Par 3s). This ensures the `Z` key always has a tactical target to cycle through.
### Fixed
- **Green Transition Instructions:** Updated `initPutting` in `physics_ag.js` to remove legacy references to the "P" key. It now correctly instructs the player to use the unified Arrow Keys and Enter/Escape locking mechanism.
- **Putting Mode State Clarity:** Improved the arrival announcement when landing on the green to clearly state that the player is in "Targeting Mode" by default.
## [4.19.1] - 2026-03-19
### Fixed
- **Swing Style Toggle:** Corrected the 'S' key behavior. It now correctly cycles through your Swing Styles (Full, Pitch, Chip, etc.) instead of Stances. Stance adjustments remain mapped to the `Home` and `End` keys for consistent tactical control.
## [4.19.0] - 2026-03-19
### Changed
- **Swing Engine Restoration:** Reverted the core swing, power, and impact mechanics to the v4.14.0 logic. This eliminates the "whiffing" bug by returning to a direct-call system for shot calculation, bypassing the browser's event-loop desync.
- **Unified Logic:** Successfully merged the v4.14.0 timing foundation with the v4.18 feature set, including Practice Swings (ArrowUp), Audio Priming (Sound 37), and Unified Targeting (Shift + Z).
## [4.18.3] - 2026-03-19
### Fixed
- **Impact Registration Bug:** Resolved an issue where regular swings would "whiff" despite the player pressing the impact button. 
- **Instant Impact Feedback:** Removed the 400ms calculation delay. Pressing `ArrowDown` or `ArrowUp` during the impact phase now triggers the physics engine (or practice evaluation) immediately, providing instant auditory and data feedback.
## [4.18.2] - 2026-03-19
### Added
- **Audio Context Priming:** Integrated "Bouncing Confirm" (Sound 37) into the `loadHole` and `initGame` sequences. This 1.0s audio asset forcefully wakes the browser's Web Audio API during hole transitions, eliminating the "cold start" stutter on the first tee shot of a session.
## [4.18.1] - 2026-03-19
### Fixed
- **Swing Timing Bug:** Removed the 400ms "Takeaway Buffer" introduced in v4.18.0, which was desyncing `performance.now()` and causing all standard shots to register as 400ms late ("whiffed"). The swing timing is restored to its original, mathematically perfect state.
- **Practice Swing Inputs:** Fixed a bug where releasing the `ArrowUp` key during a practice swing failed to lock the power meter, and pressing it during impact failed to lock the strike. Both `ArrowUp` and `ArrowDown` now correctly share `keyup` and impact-lock listeners.
## [4.18.0] - 2026-03-19
### Added
- **Practice Swings (ArrowUp):** Players can now press `ArrowUp` to take a practice swing. The engine runs the full swing/metronome sequence but bypasses the physics engine, instead providing Caddy feedback on tempo, power, and impact timing without hitting the ball.
- **The Takeaway Buffer:** Added a 400ms delay between pressing the swing initiator and the start of the backswing metronome. This allows the browser's Web Audio API to fully initialize, completely eliminating audio stutter and tempo lag on the first swing.

### Changed
- **Pin Finder Unification (Shift + Z):** Unified the Pin Finder and Putting Grid keybinds. `Shift + Z` now universally opens the 2D targeting grid regardless of whether the player is in the fairway or on the green. (`Shift + T` and `KeyP` have been deprecated).
## [4.17.0] - 2026-03-19
### Changed
- **Putting UI Consistency:** Unified the targeting controls across the game. When using the 2D Targeting Grid on the putting green, pressing `Enter` (or `Escape`) now instantly locks the chosen coordinate and switches the engine to Swing Mode (matching the behavior of the Fairway Pin Finder). Players no longer have to manually toggle modes using the `P` key.
## [4.16.1] - 2026-03-19
### Fixed
- **Grid Warp Focus Trap:** Fixed a bug where warping to a new hole (using the `Digit` keys) while the Pin Finder was active would fail to release the arrow keys. `loadHole()` now explicitly resets `isGridNavigating` to `false`.
## [4.16.0] - 2026-03-19
### Fixed
- **Putting Caddy Restored:** Fixed an issue where the Fairway Oracle had overwritten the Putting Caddy. The `A` key now correctly detects if the player is on the green and restores the multi-tier God Mode contour analysis (e.g., "From 15 to 0 yards, it plays downhill and breaks right...").
- **Concise Oracle Feedback:** Stripped out the overly verbose introductory data from the Fairway Oracle. It now immediately gives the actionable blueprint (e.g., "[Oracle]: To hit the Pin (165y), equip 6 Iron...").
- **The Cup Locator:** When using Arrow Keys to adjust targets in Putting Mode, the engine now actively announces "(The Cup)" when the player's target distance and aim angle perfectly intersect the physical hole, preventing players from getting lost on the green.
## [4.14.5] - 2026-03-19
### Changed
- **Pin Finder Auto-Equip:** When locking a target in the Pin Finder (`Shift + T` then `Enter`), the engine now automatically calculates the distance to your custom coordinate and equips the best club for that yardage based on your current lie and stance. It also resets your `aimAngle` to 0 so you are aimed dead-center at your new target, perfectly setting up an Oracle Caddy (`A`) query.
## [4.14.4] - 2026-03-19
### Changed
- **Grid Initialization:** When activating the Tactical Grid (`Shift + T`), the initial broadcast now explicitly identifies the starting position as "The Pin" (instead of 0 yards offset) and announces the Macro Elevation from the player's current lie. 
- **De-cluttered Navigation:** Macro elevation is intentionally *omitted* when moving the cursor from square to square to prevent audio fatigue, leaving only the Predictive Effect and distance.## [4.14.3] - 2026-03-19
### Changed
- **Predictive Topography:** The `Shift + T` grid no longer announces raw slopes (e.g., "Downhill, Breaks Right"). It now translates the slopes into actionable Predictive Effects based on a standard trajectory (e.g., "Effect: Kicks right and releases forward").
- **Macro Elevation (KeyT):** Moved elevation reporting to the `T` (Target Info) keybind. It now includes an elevation suffix (e.g., "Plays Uphill" or "Plays flat"), laying the architectural groundwork for future Fairway Contours.
## [4.14.1] - 2026-03-19
### Fixed
- **Grid Target Lock:** Fixed a bug in `input_ag.js` where pressing Enter to confirm a Tactical Grid coordinate would accidentally reset the target mode back to the pin. It now correctly locks `activeTargetType = 'grid'`.
- **Target State Leakage:** Fixed a bug in `main_ag.js` where finishing a hole with a custom grid target active would carry that state over to the next tee box. `loadHole()` now safely resets all targeting modes to `'pin'`.
- **Oracle Math Calibration:** Upgraded the Level 3 Caddy simulation math in `main_ag.js` to perfectly match the core physics engine:
  - It now correctly applies the 0.70x Sand and 0.90x Rough distance penalties during its simulation.
  - It now uses the updated `hangTime / 2.5` formula for crosswinds, preventing it from vastly over-calculating required aim adjustments.
## [4.14.0] - 2026-03-19
### Added
- **Two-Tier Targeting System:** - **Fairway:** Replaced the simple `Z` toggle with a "Landing Zone" system. Holes now contain strategic hubs (e.g., "Left Dogleg," "Safe Layup") that the Caddy uses for recovery advice.
  - **The Green (Micro-Grid):** Introduced `Shift + T` to activate a 20x20 yard Tactical Grid centered on the green. Players can move a targeting square with arrow keys to aim for specific slopes or safe areas away from the pin.
- **Oracle Caddy v2:** The Level 3 Caddy now dynamically calculates the perfect shot for the *active* target square or zone, accounting for tree collisions and release/roll on the specific green coordinate.

### Changed
- **Keybind Corrections:** - `S` / `Shift + S` now cycles Stance forward and backward.
  - `E` / `Shift + E` now handles Quick Score and the Full Scorecard.
## [4.13.0] - 2026-03-19
### Changed
- **Menu Audio De-Clutter:** The Clubhouse menu now only announces the words "Clubhouse Menu" and its instructional text when first opened. Navigating with arrow keys now strictly reads the highlighted option and position to eliminate screen reader fatigue.
- **Calm Defaults:** Changed the default `windLevelIndex` from 3 to 0, ensuring players begin their sessions with calm weather.
- **Context-Sensitive Quit:** Replaced `Shift + Q` with a global `Q` hotkey.
  - If in a Practice Area: `Q` immediately returns the player to the Clubhouse.
  - If on the Course: `Q` freezes the game and triggers a safety prompt: "Press S to Save and Exit, A to Abandon Round, or Escape to cancel," protecting players from accidental progress loss.
## [4.12.0] - 2026-03-19
### Added
- **The Clubhouse (Main Menu):** Introduced a completely new `clubhouse` game state. Pressing Enter at the title screen now loads a fully accessible, Arrow-Key navigated Main Menu instead of instantly dropping the player onto the course.
- **Save State Management:** Added menu options to "Resume Saved Session" (dynamically appears if a save file exists) and "Start New Round" (safely wipes the scorecard and local storage for a fresh 18-hole start).
- **Shift + Q (Quit to Menu):** Added a global hotkey to instantly pause the game, hide the gameplay UI, and return to the Clubhouse menu. Because the game autosaves after every shot, players can safely quit mid-hole and resume later.
## [4.11.0] - 2026-03-19
### Changed
- **Grid Scorecard Navigation:** The `Shift + S` full scorecard no longer relies on native HTML table elements or screen reader browse modes. It has been rebuilt as a custom 2D JavaScript grid. Arrow keys are now strictly intercepted, maintaining the application focus trap while dynamically generating highly readable ARIA announcements (e.g., "Hole 3, Putts: 2") based on your position in the grid.
## [4.10.1] - 2026-03-19
### Fixed
- **The "Permanent Putter" Bug:** Fixed a critical state-management flaw where progressing to the next hole via the `Enter` key failed to reset the `isPutting` and `puttState` variables. `loadHole()` now explicitly wipes all short-game states and grip modifiers, guaranteeing a fresh, standard swing mode on the next tee box.
## [4.10.0] - 2026-03-19
### Added
- **Round Progression System:** The game now flows continuously. Sinking a putt triggers a prompt to "Press Enter" to proceed to the next hole, looping seamlessly through the 9-hole course.
- **Advanced Stat Tracking:** The engine now silently tracks Putts, FIR (Fairways in Regulation), and GIR (Greens in Regulation) based on your lies and stroke counts.
- **The Scorecard Array:** Added a `roundData` object array that persists in `localStorage` alongside your physical coordinates.
- **Two-Tier Score Readouts:** - Press `S` for a quick auditory summary (e.g., "Through 4 holes, you are +2"). 
  - Press `Shift + S` to open the Full Scorecard. This dynamically generates a semantic HTML `<table>`, allowing screen reader users to navigate row-by-row and column-by-column using native table commands.## [4.9.0] - 2026-03-19
### Added
- **Browser Persistence (Autosave):** Implemented HTML5 `localStorage`. The engine now silently saves the complete game state (coordinates, lie, strokes, wind, telemetry, and active club) after every shot and hole change. Reloading the page will automatically resume the game exactly where the player left off.

### Fixed
- **The Hazard Menu Trap:** Fixed a soft-lock issue where players could get stuck in the `KeyH` hazard menu. The `ArrowDown` key no longer gets silently swallowed if the player forgets to close the menu, and `Enter` has been added as a secondary exit key alongside `Escape`.
## [4.8.0] - 2026-03-18
### Added
- **Putter Strike Sound:** Added a distinct, sharp "tink" (`800Hz triangle`) the exact millisecond the putter strikes the ball, creating a clear auditory anchor before the rolling metronome begins.
- **The Green Arpeggio:** Reintroduced the victory arpeggio (`440Hz -> 554Hz -> 659Hz`) when a player successfully lands an approach shot on the putting surface.
- **Global Volume Overdrive:** Wrote a wrapper function that intercepts all Web Audio API calls (`playTone` and `playNoise`) and multiplies their volume by 2.5x, massively boosting the audibility of the swing mechanics and physics engine without affecting your screen reader's TTS volume.

### Fixed
- **Telemetry Wipe Bug:** Fixed a critical order-of-operations bug where landing on the green would trigger an early `return` and bypass the `holeTelemetry.push()` function. The engine now safely records the approach shot to your clipboard data *before* warping you to the putting interface.
## [4.7.2] - 2026-03-18
### Fixed
- **The True North Putting Bug:** Fixed a critical 3D geometry bug where the putting engine and God Caddy failed to calculate the relative angle between the ball and the pin on the course. `currentHeading` now correctly integrates `Math.atan2()`, ensuring `0 degrees` of aim is always perfectly locked onto the center of the cup regardless of where the player lands on the green.
## [4.7.1] - 2026-03-18
### Fixed
- **Ghost Simulator Accuracy:** Fixed a bug where the Level 3 Caddy would occasionally recommend extreme aim angles on short putts because it was locking onto the very first angle that clipped the outer edge of the cup's capture radius. The `for` loop now radiates outward from `0` degrees, guaranteeing it always finds the dead-center line of the putt first.
## [4.7.0] - 2026-03-18
### Added
- **The Green Dictionary:** Added `greenDictionary` to `data_ag.js`, storing 9 classic golf architecture templates (e.g., The Redan, The False Front, The Turtleback) as pre-configured arrays of Contour Zones.
- **Course Integration:** All 9 holes of "The Holodeck Links" now have a `greenType` assigned to them. When the player reaches the green on the course, the engine and the Caddy dynamically load the contour math from the dictionary.
## [4.6.1] - 2026-03-18
### Fixed
- **Putt Timing & Suspense:** Significantly adjusted the Spatial Audio Metronome math. The minimum delay between yardage steps was increased from 40ms to 100ms, and the speed divisor was doubled. This effectively doubles the real-world time it takes for a putt to complete, mimicking realistic green friction and vastly increasing the suspense as the ball slows down near the hole.
## [4.6.0] - 2026-03-18
### Added
- **Spatial Audio Rolling:** Putts no longer calculate and complete instantly. The physics engine now records the step-simulation and plays it back in real-time. 
- **The Rolling Metronome:** As the ball rolls, it emits an auditory pulse. The time between pulses is directly tied to the ball's speed. As the ball goes uphill, the pulses slow down. As it crests a ridge and goes downhill, the pulses speed up, allowing the player to literally hear the gravity acting on the ball.
- **Hyper-Panning:** The lateral movement of the ball is aggressively mapped to a Stereo Panner. A ball that breaks just 3 yards to the left or right will pan 100% into that respective ear, creating an exaggerated, highly readable auditory trace of the break.
## [4.5.0] - 2026-03-18
### Changed
- **The Augusta Practice Green:** The Practice Putting Green has been expanded and redesigned into a massive 45-yard, 3-tier monster. 
    - Tier 1 (45y - 25y): The False Front (Steep uphill, hard Right-to-Left).
    - Tier 2 (25y - 10y): The Plateau (Flat, subtle Left-to-Right).
    - Tier 3 (10y - 0y): The Bowl (Fast downhill, sweeping Right-to-Left).
- **Expanded Putting Targets:** Pressing `T` on the practice green now spawns targets anywhere from 5 yards up to 45 yards to test all three tiers.
## [4.4.2] - 2026-03-18
### Fixed
- **Putting Dev Keys:** Hooked `F1` (Dev Power) into the putting simulation. It will now correctly force 100% power on the green.
- **The Ghost Oracle:** Completely rewrote the Level 3 Caddy math. Instead of estimating breaks using flat algebra, the Caddy now runs a brute-force "Ghost Simulation," testing thousands of aim and distance combinations against the Gravity Engine to guarantee a mathematically perfect read.
## [4.4.1] - 2026-03-18
### Fixed
- **Putting Input Bug:** Fixed a critical bug where the `Down Arrow` target adjustment prevented the player from initiating a putting swing.
- **Swing Mode Toggle:** Reintroduced the `P` key toggle on the green. `puttState = 0` (Targeting Mode) maps arrows to aim and distance. `puttState = 1` (Swing Mode) locks aim and maps the `Down Arrow` back to the standard swing initiator, allowing for Spacebar Tempo touches in phases 1 and 3.
## [4.4.0] - 2026-03-18
### Changed
- **Streamlined Putting Controls:** Removed the Braille Grid and the `Enter/P` mode toggles. While on the green, the Arrow Keys are now instantly mapped to your putt setup: `Up/Down` adjusts target distance, and `Left/Right` adjusts aim. 
- **The Gravity Engine:** Upgraded the putting physics from a flat 2D line to a 1-yard Step Simulation. As the ball rolls, it calculates its exact position every yard, applies gravity from the active Contour Zone to bend the trajectory, and accelerates/decelerates based on the slope.
- **Narrative Caddy:** The Caddy (`KeyA`) now provides 3 distinct tiers of green-reading advice. Level 1 gives basic directions, Level 2 provides conversational strategic advice, and Level 3 acts as a mathematical oracle to help you perfectly calibrate your aim and target distance.
- **Forgiving Cup Size:** The physical capture radius of the hole has been widened to 15 inches (0.4 yards) to accommodate the new complex breaking physics, making putts far more realistic to sink.
## [4.3.0] - 2026-03-18
### Added
- **Contour Zones:** Replaced the single-plane green math with an array-based Zone system. Greens can now have infinite tiers, ridges, and double-breaks based on distance to the hole.
- **Double-Breaker Practice:** Upgraded the Practice Putting Green to feature a severe ridge at the 10-yard mark to test multi-zone putts.
- **The Putting Caddy:** The Level 3 Caddy can now read the green. Pressing `A` while putting will calculate which Contour Zones lie between the ball and the hole, and verbally describe the elevation and break of each segment.
- **Zone-Aware Braille Grid:** The `Arrow` key exploration now dynamically pulls the `slopeX` and `slopeY` of whichever specific Contour Zone your virtual finger is currently touching.
## [4.1.0] - 2026-03-18
### Added
- **Practice Putting Green:** Press `Shift + P` from anywhere to travel to the dedicated Putting Sandbox.
- **Infinite Retries:** On the Practice Putting Green, the hole is never "completed." After your putt stops, the ball instantly teleports back to your starting position so you can try the exact same break and distance again to dial in your tempo.
- **Quick Targets:** Press `T` while in Explore Mode on the Putting Green to instantly generate a new random target flag (between 5 and 30 yards away).
## [4.0.0] - 2026-03-18
### Added
- **The Putting Engine (v1):** The game now transitions into a dedicated 2D Putting Phase when the ball lands on the green within 20 yards of the pin.
- **Braille Green Exploration:** Press `P` to enter Pre-Putt mode. Use the `Arrow Keys` to explore a 3-column audio grid representing the green's topography. Tones are stereo-panned (Left/Center/Right) and pitch-modulated (High = Uphill, Low = Downhill).
- **Targeting Mechanics:** Use `[` and `]` to adjust aim angle, and `-` and `=` to push your target distance past the hole for uphill/slow putts. You will always swing to the 100% audio mark.
- **"Jam It In" Physics:** Putts under 2 yards (6 feet) automatically snap the target to the cup. Hitting 100% power with >90% accuracy on short putts dramatically widens the cup's capture radius, allowing you to blast through minor breaks.
## [3.83.0] - 2026-03-18
### Added
- **Predictive Shape Caddy:** The Launch Monitor (`KeyA`) now runs a full lateral physics simulation before your shot. It calculates your expected wind drift and the side-spin generated by your Stance Alignment (Open/Closed) to predict the exact flight path curve. The Caddy will now warn you if your shot shape curves back into an obstacle, or praise you if your shape successfully bends around one.
## [3.82.0] - 2026-03-18
### Added
- **Comprehensive Telemetry Export:** The `lastShotReport` (and the `Shift + C` clipboard export) now captures the complete pre-shot Setup State. It logs the active Club, Choke status, Shot Style, Stance, Alignment, Aim Angle, and exact Wind conditions alongside the Execution metrics.
- **Environment Logging:** The active state of the Synth Tree (Distance, X-Coordinate, and Threshold Height) is now permanently stamped into the shot's telemetry log for post-shot analysis.
## [3.81.1] - 2026-03-18
### Fixed
- **Launch Monitor Parity:** Fixed a bug where the `getCaddyAdvice` and the `KeyY` Ghost Simulation were calculating flight apexes using raw total distances. They now correctly apply the `loftDistMod` penalty and subtract spin-calculated roll to find the true `expectedCarry`, perfectly syncing Caddy predictions with the actual `physics_ag.js` engine.
- **Bag Check Accuracy:** Updated `getSetupReport()` (`KeyX`) to include the `loftDistMod` penalty so the reported expected distance accurately reflects your active stance adjustments.
## [3.81.0] - 2026-03-18
### Fixed
- **Parabolic Flight Math:** Replaced the linear trigonometric height formula with a proper parabolic projectile motion formula (`y = (tan(θ) / D) * x * (D - x)`). 
- **Tree Collision Accuracy:** Ball height mid-flight is now significantly more realistic. Irons apex between 70-100 feet instead of scaling infinitely upwards, meaning players must make smarter tactical decisions when trying to clear tall trees.
## [3.80.2] - 2026-03-18
### Added
- **Flight Path Narrator:** The engine now tracks vertical and lateral margins as the ball passes obstacles. The generic shot shape description (e.g., "A high draw") is dynamically replaced with contextual narratives (e.g., "A high draw that shaved the right edge of the Synth Tree canopy by just 2 yards").
- **Mid-Air Course Collision:** Upgraded natural course trees to use the same mid-air flight interpolation as the Synth Trees, allowing players to curve the ball around trunks mid-flight or hit branches before the ball lands.
- **Hole Reports:** Because the Flight Path Narrator alters the core `lastShotReport`, pressing `Shift + C` now effectively generates a highly descriptive, shot-by-shot story of the entire hole.
## [3.80.1] - 2026-03-18
### Added
- **Choke Down Grip:** Press `V` to toggle your grip. Choking down caps the club's potential distance at 90% (allowing you to comfortably swing to the 100% audio ping for shorter targets) and increases the timing forgiveness windows for Hinge and Impact by 20%. 
- **Caddy Edge Finder:** The Telemetric Caddy (`KeyA`) on the Holo Range now explicitly calculates and announces the exact degrees left and right required to bypass the active Synth Tree's canopy.
## [3.80.0] - 2026-03-18
### Added
- **Synth Trees (Threshold Sandbox):** Added dynamic obstacles to the Holo Range and Pitching Green. Press `Y` to spawn a Synth Tree exactly 70% of the way to the pin on your target line. 
- **Coordinate Control:** Use `[` and `]` to move the tree left and right in 5-yard increments. Use `-` and `=` to move it closer or further.
- **Ghost Simulation (Threshold Math):** When spawned, the tree automatically sets its height to the exact apex of a 110% power, 90% hinge, 95% impact shot for your currently equipped club and stance. 
- **Manual Override:** Press `Shift + Y` to manually cycle the tree's height in 10-foot increments.
- **Launch Monitor Caddy:** Pressing `A` on the range now runs a live trigonometric simulation to tell you if your current setup will clear the Synth Tree or hit the trunk.
## [3.70.0] - 2026-03-17
### Added
- **Navigable Help Dialog:** Pressing `?` (Shift + `/`) now opens a navigable, line-by-line Help Menu dialog, replacing the old static audio dump.
- **Screen Reader Parity:** The Help Menu acts like a DOM document. Players use `ArrowUp` and `ArrowDown` to read line-by-line, and `H` or `Shift + H` to jump forward and backward between major headings. `Enter` or `Escape` closes the dialog and restores game controls.
## [3.62.0] - 2026-03-17
### Added
- **Telemetry Dashboard:** Added a persistent 4-column visual dashboard to the HUD for sighted players. It continuously displays Hole/Target Info, Environment (Wind/Lie), Equipment (Club/Style), and Setup (Aim/Stance).
- **Real-Time UI Binding:** Wired `window.updateDashboard()` into all relevant keyboard inputs (`input_ag.js`) and physics loops (`physics_ag.js`) to ensure the visual dashboard instantly reflects the player's active state.
## [3.61.0] - 2026-03-17
### Added
- **Visual Marquee:** Added a retro, scrolling text marquee at the top of the HUD that visually mirrors all `window.announce()` ARIA broadcasts in real-time.
- **Dedicated Caddy Panel:** Created a permanent, stylized UI panel to display the post-shot `lastShotReport` and telemetry, cleanly separating it from the primary swing status prompts.
## [3.60.0] - 2026-03-17
### Added
- **Canvas Rhythm Bar (Swing Meter):** Introduced a 60fps, GPU-accelerated visual representation of the audio mechanics for sighted spectators. 
- **Visual Hinge Tracking:** Tapping the Spacebar now drops permanent visual markers (Yellow for Backswing, Orange for Downswing) on the bar to visually map the `hingeDiff`.
- **Visual Impact Zone:** The Impact Drop phase now displays a moving cursor traveling toward a glowing green "Sweet Spot" target line.
## [3.51.0] - 2026-03-17
### Changed
- **Advanced Caddy Brain:** Overhauled `window.getCaddyAdvice` to track granular lie states and distance brackets. The semantic engine now recognizes `Bunker_Fairway`, `Rough_Deep`, `Approach_Scoring` (inside 120 yards), and `Approach_Long`.
- **Hole 3 Master Data:** Expanded Hole 3's `caddyNotes` to serve as the gold standard template for all future holes, including specific advice for fairway bunkers, deep rough, scoring wedges, and layup zones.
## [3.50.0] - 2026-03-17
### Added
- **Front Nine Yardage Book:** Added full semantic `caddyNotes` (Levels 1-3) and `fairwayDescription` strings for Holes 1, 2, 5, 7, 8, and 9.
### Fixed
- **Hole 6 Caddy Logic:** Corrected a semantic trigger bug on Hole 6 where "Approach" advice was mistakenly being read on the "Tee".
## [3.46.0] - 2026-03-17
### Changed
- **Caddy Advice Hotkey:** Moved from `N` / `Shift+N` to `A` / `Shift+A`.
- **Green Proximity Broadcast:** The detailed "Feet & Inches" proximity and roll report is now automatically announced via ARIA when finishing a hole, eliminating the need to press `C` to check where the ball landed and rolled.
### Added
- **Fairway Description (KeyF):** Pressing 'F' now reads a detailed description of the fairway's shape, width, and "dead space" to help map areas between target zones. Added custom fairway descriptions to Holes 3 and 6.
## [3.45.0] - 2026-03-17
### Added
- **Caddy Skill System:** Added a `caddyLevel` variable (Levels 1 to 3) that dictates the quality of strategic advice available to the player.
- **The Yardage Book (KeyN):** Pressing 'N' now prompts the Caddy to read context-aware advice based on your current semantic location (Tee, Approach, Greenside, Trouble_Left, Trouble_Right).
- **Dynamic Caddy Toggle:** Pressing 'Shift + N' cycles the Caddy Level between Rookie (1), Veteran (2), and Tour Pro (3) for testing.
- **Hole Data Expansion:** Added `caddyNotes` arrays to Holes 3, 4, and 6 to serve as the prototype for the future master CSV database.
## [3.44.0] - 2026-03-17
### Fixed
- **Bunker Interception:** Added roll-path detection. If a ball's roll phase enters a bunker or hazard, the roll is now terminated, and the ball settles inside the hazard.
- **Stroke Counter Hard-Reset:** Added an explicit stroke reset to the Warp (Digit) keys to prevent "shot-bleeding" between holes.
- **Sand Sound Trigger:** Fixed a bug where balls rolling into sand would still play the "Fairway Roll" grass noise.
## [3.43.0] - 2026-03-17
### Added
- **Tactical Hybrid Hazard Reporting (KeyH):** Re-integrated "Aim Line" raycasting into the Hazard menu. The Caddy now provides a two-part report for every obstacle:
    1. **Edge Finder:** The angles (degrees) required to aim left or right to bypass the obstacle.
    2. **Aim Line Intersect:** The specific distance required to reach and clear the obstacle on your *current* heading.
- **Dynamic Aim Updates:** If you rotate your aim (Arrow Keys) while the Hazard menu is open, the Caddy will now update the "Aim Line" distances in real-time.
## [3.42.0] - 2026-03-17
### Added
- **Loft-Based Distance Scaling:** Ball placement now physically affects shot distance. Moving the ball back (Lower Loft) increases forward velocity, while moving it forward (Higher Loft) increases vertical apex and reduces total distance.
- **Spin-Dependent Roll:** The `rollDistance` calculation now factors in `backspinRPM`. High-spin shots (from a 'Back' stance or flushing a wedge) will "check up" faster, while low-spin shots will release and roll further.
## [3.41.0] - 2026-03-17
### Added
- **Dynamic Green Lie:** The engine now uses the specific `greenRadius` from the course data to determine if a ball is on the green, replacing the previous hardcoded 20-yard limit.
- **Extended Precision Reporting:** Increased the threshold for high-precision reporting from 5 yards to 30 yards. Any ball landing or finishing within 30 yards of the pin (covering the entire green and fringe) will now report in feet and inches.
- **Landing Awareness:** The Caddy now intelligently detects if a ball *hit* the green during flight, even if it eventually rolled off into the rough. In these cases, it provides the full "Landed... Rolled... Finished..." narrative to help the player understand why the ball failed to hold the green.
## [3.40.1] - 2026-03-17
### Fixed
- **Edge Finder Math:** Fixed a coordinate typo in `input_ag.js` where the Caddy was looking at the wrong axis to calculate hazard center-points.
- **Redundancy Cleanup:** Removed the duplicate `announceHazard` function from `main_ag.js`.
- **Physics Polish:** Cleaned up unreachable logic in `physics_ag.js` and removed unused variables in the proximity reporting block.
## [3.40.0] - 2026-03-17
### Added
- **The Edge Finder (KeyH):** Upgraded the Obstacle List math. When inspecting a tree or hazard, the Caddy now calculates the exact aim angles (in degrees) required to clear the left and right edges of the obstacle from your current position.
- **Granular Green Reporting:** When a ball settles on the Green, the narrative now shifts to "Proximity Mode." It reports the landing spot relative to the pin (e.g., "Landed 4 yards short"), the roll-out behavior ("Rolled 6 feet past"), and the final miss distance in feet and inches.
- **Micro-Distance Units:** Added a unit conversion engine that automatically switches from yards to feet and inches for high-precision proximity reporting (under 5 yards).
## [3.39.0] - 2026-03-17
### Changed
- **Progressive Power Pressure:** Removed the harsh >105% power penalty cliff. Powering up over 100% now smoothly and progressively widens the lateral dispersion cone (e.g., 106% power results in a ~24% wider dispersion radius) and scales side spin naturally.
- **Short Game Side Spin Dampening:** Short game swing styles (Pitch, Chip, Flop) now actively suppress side spin generation to simulate locked wrists, preventing unrealistic lateral kicks on short shots.
- **Short Game Rough Mechanics:** When hitting short game styles out of the rough, impact timing errors now simulate real-world grass interactions. Early impacts (-25ms) simulate "trapped grass," severely deadening spin and distance. Late impacts (+25ms) simulate "skulled/bladed" shots, resulting in low trajectories, minimal spin, and massive rollouts.
## [3.38.0] - 2026-03-17
### Changed
- **Contextual Club Check (KeyX):** The `X` command now accurately reflects your current lie. If you are in the Sand or Light Rough, pressing `X` will announce the dynamically penalized distance range (e.g., 85%-95%) rather than the "ideal conditions" yardage.
## [3.37.1] - 2026-03-17
### Changed
- **Audio UX Reordering:** The `getSetupReport()` function (triggered when switching clubs) now reads in a strictly prioritized order: 1. Club, 2. Distance/Range, 3. Lie, 4. Shot Style.
- **Shot Style Toggle (KeyS):** Decoupled the `KeyS` audio from the full setup report. Pressing `S` now provides a short, snappy confirmation of the newly selected style (e.g., "Style: Flop") without reading redundant club or lie information.## [3.37.1] - 2026-03-17
### Changed
- **Audio UX Reordering:** The `getSetupReport()` function (triggered when switching clubs) now reads in a strictly prioritized order: 1. Club, 2. Distance/Range, 3. Lie, 4. Shot Style.
- **Shot Style Toggle (KeyS):** Decoupled the `KeyS` audio from the full setup report. Pressing `S` now provides a short, snappy confirmation of the newly selected style (e.g., "Style: Flop") without reading redundant club or lie information.
## [3.37.1] - 2026-03-17
### Changed
- **Audio UX Reordering:** The `getSetupReport()` function (triggered when switching clubs) now reads in a strictly prioritized order: 1. Club, 2. Distance/Range, 3. Lie, 4. Shot Style.
- **Shot Style Toggle (KeyS):** Decoupled the `KeyS` audio from the full setup report. Pressing `S` now provides a short, snappy confirmation of the newly selected style (e.g., "Style: Flop") without reading redundant club or lie information.
## [3.37.0] - 2026-03-17
### Added
- **Hole Telemetry (Flight Recorder):** Added a global `holeTelemetry` array that acts as a flight recorder. It stores the full physics readout and narrative description of every single shot taken during the current hole or practice session.
- **Bulk Export (Shift + C):** Upgraded the `KeyC` clipboard command. Pressing `Shift + C` now copies the entire `holeTelemetry` log for the current hole to the system clipboard, formatted chronologically, allowing players to export and review their stroke-by-stroke analytics.
## [3.36.1] - 2026-03-17
### Changed
- **Lie Penalties:** Separated "Sand" and "Light Rough" mechanics. Light Rough now yields 85%-95% distance (formerly 60%-90%), with slightly reduced dispersion penalties compared to Sand. Sand remains at a harsh 60%-80% distance penalty.
- **Smart Auto-Club (KeyZ):** The Caddy's auto-club selection algorithm now factors in your current lie. If you are in the rough or sand, it will automatically "club up" to account for the expected distance loss.
- **Caddy Reports:** `getSetupReport()` now dynamically announces the specific yardage ranges based on whether you are in Sand or Light Rough.
## [3.36.0] - 2026-03-17
### Added
- **Front Nine Complete:** Expanded `The Holodeck Links` in `data_ag.js` from 3 holes to 9 holes, featuring new dynamic obstacles like Driveable Par 4s, Island Greens, and Tree Clusters.
- **Narrative Descriptions:** Added a `description` property to all 9 holes to paint a vivid mental picture of the hazards and ideal strategy.
- **Course Guide (KeyD):** Players can press `D` at any time before swinging to hear the Caddy read the narrative description of the current hole.
## [3.35.0] - 2026-03-17

### Added
- **Lie Tracking (KeyL):** Added a global `currentLie` state. Players can press `L` at any time to hear their current lie (Tee, Fairway, Light Rough, Sand, Green). In Driving Range mode, `L` still toggles the virtual lie.
- **Club Check (KeyX):** Added the `X` key to announce the currently equipped club and its expected distance at 100% power under ideal conditions.
- **Persistent Telemetry (KeyC):** The `C` command now works even when the hole is complete, allowing players to review the final approach shot telemetry before advancing.

### Changed
- **Setup Report Reordering:** When switching clubs with `PageUp/PageDown`, the `getSetupReport()` function now announces the swing style *last*, prioritizing the club name and yardage.
## [3.34.0] - 2026-03-17
### Added
- **Tactical Landing Zones:** Added a `zones` array to `data_ag.js`. The engine now supports discrete fairway targets (e.g., "Safe Drive", "Aggressive Carry") distinct from the pin.
- **Zone Targeting (KeyZ):** Players can press `Z` to cycle through available landing zones that are ahead of the ball. The Caddy will automatically rotate the player's 0-degree aim line to the selected zone.
- **Auto-Caddy Club Selection:** When switching zones, the Caddy automatically equips the club whose base distance best matches the yardage to the new target.
- **Dynamic Target Math:** Upgraded `physics_ag.js` and `main_ag.js` to calculate shot angles and raycasting based on `targetX` and `targetY` instead of always defaulting to the pin.
## [3.32.0] - 2026-03-16
### Added
- **Tactical Hazard Raycasting:** Upgraded the `announceHazard` function to include "Carry and Clear" math.
- **Dynamic Caddy Advice:** The Caddy now actively calculates if a rectangular hazard (Water, Bunker) is in your current flight path based on your exact `aimAngle`. If it is, the Caddy announces the exact yardage required to reach the front edge and clear the back edge on that specific line.
## [3.33.1] - 2026-03-16
### Fixed
- **Data Syntax Error:** Fixed a critical missing comma and duplicated array objects in the Hole 1 `trees` array within `data_ag.js` that caused an engine crash.

### Added
- **Black Box Telemetry:** Added `treeCollisionReport` to the physics engine. The internal math (tree height vs. calculated ball flight height) is now appended to the hidden `metrics` string. 
- **Telemetry Export:** Players can now use the `Shift + C` command to copy the exact tree collision math to their system clipboard for testing and debugging.## [3.33.0] - 2026-03-16
### Added
- **Caddy Sight Cone:** The Caddy now filters tree announcements based on a 30-degree "in-play" cone extending toward the target.
- **Vertical Clearance:** Trees now have a `height` property. Ball flight logic in `physics_ag.js` now checks `dynamicLoft` to determine if a ball sails over a tree or strikes it.
- **Sight Report:** Added `getSightReport()` to `main_ag.js` to specifically announce trees that are currently blocking the line of sight.## [3.32.1] - 2026-03-16
### Changed
- **Hazard List Integration:** Updated KeyH logic to merge 'hazards' and 'trees' into a single navigable list for pre-shot scouting.
- **Unified Announcement:** Updated announceHazard to handle the circular coordinate format (x, y, radius) used by tree objects.## [3.32.0] - 2026-03-16
### Added
- **Tree Obstacles:** Added a `trees` array to hole data in `data_ag.js`, allowing for circular obstacles with specific `x`, `y`, and `radius`.
- **Collision Detection:** Implemented intersection math in `physics_ag.js` to determine if a ball's flight path crosses a tree's volume.
- **Tree Audio:** Added a "thwack" sound effect for branch/trunk collisions.## [2.31.0] - 2026-03-16
### Added
- **Pin Placement Data:** Added a `pinLocation` string property (e.g., "Front-Left", "Back-Right") to the hole data objects in `data_ag.js`.
- **Target Context (KeyT):** Upgraded the `KeyT` (Target) command in `input_ag.js` to append the specific `pinLocation` to the distance announcement when playing on the course.

### Changed
- **Version Headers:** Updated the top comment lines of `data_ag.js` and `input_ag.js` to reflect v3.31.0.## [2.30.2] - 2026-03-16
### Added
- **Hazard Audio:** Added `playSplash()` to the audio engine, synthesizing a splash and droplet "bloop" sound for water hazards.
- **Water Penalty Logic:** The physics engine now detects if a lie contains the word "water". If so, it mutes standard bounce/roll audio, plays the splash sound, increments the `strokes` counter by 1, and explicitly announces the penalty in the subsequent Caddy report (e.g., "Stroke 4 (includes 1 stroke water penalty).")
- **Hazard Green Protection:** Prevented the `isHoleComplete` trigger from firing if a ball lands in a water hazard within 20 yards of the pin.## [2.30.1] - 2026-03-16
### Fixed
- **Absolute Positional Reporting:** Updated the Caddy's post-shot broadcast in `physics_ag.js` to report the ball's absolute distance from the center of the fairway (`ballX`), rather than its lateral deviation from the aimed line. This gives blind players true grid-coordinate awareness.

### Changed
- **Hole 1 Hazard:** Expanded the Hole 1 water hazard in `data_ag.js` to be twice as wide (40 yards), keeping its right edge 25 yards left of the fairway and extending it deeper into the left rough.## [2.30.0] - 2026-03-16
### Added
- **Hazard Data System:** Added `hazards` arrays to course data. 
    - Hole 1: Left-side Water Hazard (100y to 350y).
    - Hole 2: Greenside Sand Bunker (Right side).
    - Hole 3: Center Cross-Bunker at 270y (forces layup or 120% drive).
- **Dynamic Choke Points & Aprons:** Added `approachWidth` and `apronRadius` parameters to course data to simulate fairways narrowing near the green (Hole 3 narrows to 10 yards, with a 30-yard wide apron).
- **Hazard List UI (KeyH):** Players can now press `H` to enter the Hazard List and use `Up/Down` arrows to scan the exact distance, width, and depth of all hazards on the current hole.
- **Hazard Physics:** The physics engine now detects if a ball lands in a hazard rectangle. Bunkers change the lie; Water adds a 1-stroke penalty and forces a drop.## [2.29.1] - 2026-03-16
### Fixed
- **Hole 3 Geometry:** Adjusted Hole 3's `pinY` to `537` so the hypotenuse perfectly matches the 540-yard total distance.
- **State Soft-Lock:** Added `swingState = 0;` to `loadHole()` and removed duplicated function definitions in `main_ag.js` to prevent the engine from freezing if players change holes mid-swing.
- **Dynamic Fairway Bounds:** Updated `physics_ag.js` to evaluate rough penalties based on the specific `fairwayWidth` of the current hole, rather than relying on a hardcoded 35-yard global width.

### Changed
- **Modular Versioning:** Added v2.29.1 version stamps to the top comment line of all JavaScript module files.## [2.29.0] - 2026-03-16
### Added
- **Course Data Structure:** Added a `courses` array to `data_ag.js` to store specific hole layouts, pars, and pin coordinates (X, Y offsets).
- **Hole Loading Logic:** Introduced a `loadHole()` function in `main_ag.js` to automatically configure the environment, tee position, and default club based on the specific hole data.

### Changed
- **Initialization & Navigation:** Refactored `initGame()` and the `Digit` key shortcuts to utilize the new `loadHole()` function rather than hardcoding default 420-yard Par 4s.## [2.28.0] - 2026-03-16
### Added
- **Lateral Ground Mechanics (The "Kick"):** The engine now simulates how side spin grips the turf upon landing. Shots with high side spin will bounce and roll laterally (kicking left for hooks/draws, right for slices/fades).
- **Caddy Kick Quantification:** The Caddy's shot narrative has been updated to explicitly report the lateral ground movement, ensuring blind players can accurately map the turf interaction in their mental model (e.g., "rolls 5 and kicks 3 yds right").## [2.27.0] - 2026-03-16
### Changed
- **Gyroscopic Wind Physics:** Overhauled how wind affects ball flight in `calculateShot`.
- **Wind Base Nerf:** Increased the divisor for base wind effect, preventing the "parachute" ballooning effect on high-trajectory shots. A 3mph crosswind will now push a perfectly struck ball ~4 yards instead of 17.
- **Backspin Stability:** Added `gyroMod`. High backspin (>4000 RPM) now "cuts" through the wind, reducing lateral and vertical wind drift by up to 30%. Low backspin makes the ball more susceptible to wind.
- **Spin Amplification:** Side spin and crosswind now interact. Hitting a slice into a right-to-left wind will cause the spin to "fight" the wind, keeping the ball straighter. Hitting a slice with a left-to-right wind will amplify the curve.## [2.26.4] - 2026-03-16
### Added
- **Developer Testing Shortcuts:** Added three "Macro" keys to bypass swing timing for physics calibration.
    - **F1 (Perfect Power):** Forces `finalPower` to exactly 100%.
    - **F2 (Perfect Hinge):** Forces `hingeTimeBack` and `hingeTimeDown` to match, resulting in a 0ms Hinge Diff.
    - **F3 (Perfect Impact):** Forces `impactDiff` to 0ms.
- **Toggle State:** Pressing these keys toggles the "Cheat" mode on/off, with an audio announcement for each.## [2.26.3] - 2026-03-16
### Fixed
- **Range Telemetry Logging:** Fixed a bug where Driving Range shots were not saving to the `lastShotReport` variable. Pressing `KeyC` on the range will now correctly read back the most recent shot's data and telemetry.
- **Initial Callout Consistency:** Ensured the `rangeMsg` is correctly passed to the screen reader immediately following the shot.## [2.26.2] - 2026-03-16
### Added
- **Relative Proximity Reporting:** The Driving Range now provides directional context for shot results. Instead of just "yards from target," the Caddy now reports if the ball finished "Short-Left," "Long-Right," etc., relative to the player's initial line of sight.## [2.26.1] - 2026-03-16
### Changed
- **Physics Calibration:** Removed all hardcoded accuracy tiers.
- **Spin-Based Dampening:** Distance is now calculated using a base potential (Power) and then dampened based on Side Spin RPM.
- **Scaling Curve:** A 2000 RPM miss (roughly a 200ms error) now results in a ~10% distance penalty.
- **Shot Floor:** Implemented a physics floor. Even the worst possible "shank" (max side spin) will still travel at least 25% of the club's potential distance.
- **Lateral Weighting:** Increased the lateral movement multiplier so high-spin shots "leak" and "slice" further offline, matching player expectations.## [2.26.0] - 2026-03-16
### Changed
- **Impact Forgiveness Curve:** Removed the hardcoded accuracy tiers (the "85% distance cliff"). Impact accuracy (`impactAcc`) and lateral spray (`spinPenalty`) now scale smoothly using linear interpolation based on the exact millisecond offset. A near-perfect shot now receives a near-perfect distance multiplier.## [2.25.1] - 2026-03-16
### Added
- **Telemetry Export:** Added a `Shift + C` keybinding. Pressing this will instantly copy the entire `lastShotReport` (including all background math and telemetry) to your system clipboard, allowing for easy pasting into external tools or chats for physics debugging.## [2.25.0] - 2026-03-16
### Added
- **Holodeck Driving Range:** The Driving Range has been completely overhauled for rapid physics testing. 
- **Auto-Spawning Targets:** The target flag (`pinY`) automatically snaps to match the active club's base distance whenever the player changes clubs on the range.
- **Virtual Lie Toggle:** Added `KeyL` (active only on the Range) to instantly cycle the ground beneath the player's feet between "Fairway" and "Rough."
### Changed
- **Range Flow:** After every shot on the range, the ball instantly resets to the tee `(0, 0)`, aim and stance reset, but the wind continues to drift natively to simulate persistent weather. The Caddy now reports exact proximity to the auto-spawned target.## [2.23.1] - 2026-03-16
### Fixed
- **Lateral Dispersion Disconnect:** Fixed a critical bug where the ball's lateral movement (`physicsX`) was bypassing the `sideSpinRPM` variable. The physical ball flight now correctly matches the severe spin numbers and shot shapes reported by the Caddy.## [2.23.0] - 2026-03-16
### Fixed
- **Side Spin Multiplier:** Restored the missing `* 100` multiplier in the `sideSpinRPM` calculation. Poor impact timing will now correctly generate severe hooks and slices.
- **Hole Navigation Reset:** Using the number keys to warp to a new hole now automatically resets the active club to the Driver, resets the shot style to "Full", and clears the putting state.## [2.21.3] - 2026-03-16
### Fixed
- **Auto-Face Pin Navigation:** The engine now dynamically calculates the angle between the ball's current coordinates and the pin. `aimAngle = 0` now always points directly at the hole, fixing the issue where players would shoot parallel to the green on close approach shots.## [2.21.2] - 2026-03-16
### Fixed
- **Rough Distance Reporting:** The Caddy's pre-shot setup report now accurately detects if the ball is in the rough and dynamically announces a distance *range* (60% to 90% of base) instead of a single, misleading maximum distance.
### Added
- **Rough Contact Penalty:** Hitting out of the rough now shrinks the player's Hinge and Impact timing windows by 30% (`lieForgivenessMod`), making it harder to strike the ball cleanly.## [2.21.1] - 2026-03-16
### Changed
- **Keybindings:** Re-mapped the Wind Tier toggle from `Shift + M` to `Shift + W`. Pressing `W` alone reads the current wind; `Shift + W` cycles the intensity tier.
- **Victory Feedback:** The "Hole complete!" announcement now explicitly reports the ball's final proximity to the pin (e.g., "finishing 8 yards from the pin").## [2.21.0] - 2026-03-16
### Added
- **Wind Tiers:** Added four distinct wind intensity levels (Calm, Light Breeze, Gusts, Windy) with defined minimum and maximum speeds.
- **Wind Drifting:** Wind is now generated once at the start of a hole. After each shot, the wind "drifts" within a set variance rather than randomizing completely.
### Changed
- **Keybindings:** Added `Shift + M` to cycle through the wind tiers (State 0 only). Changing the tier instantly regenerates the base wind and announces the new conditions.## [2.18.1] - 2026-03-16
### Fixed
- **2D Geometry Restoration:** Successfully re-implemented `ballX`, `ballY`, `pinX`, and `pinY` tracking.
- **Pythagorean Math:** Replaced linear distance subtraction with true hypotenuse calculation for accurate off-course distance reporting.
- **Dynamic Zones:** Re-integrated power-based timing windows and spin penalties for overswings.
- **Shot Styles:** Restored the expanded 6-style array with specific wind and loft modifiers.## [2.17.1] - 2026-03-16
### Fixed
- **Physics Hotfix:** Successfully injected the v2.17 physics overhaul into the `calculateShot` function, replacing the legacy `calculateZoneAccuracy` logic with the new Dynamic Impact Zones.
- **Spin Math Corrections:** Applied the `distMod` scaling to `backspinRPM` (preventing short chips from spinning backward) and integrated the `spinPenalty` multiplier for overswings into the `sideSpinRPM` calculation.## [2.17.0] - 2026-03-16
### Added
- **Dynamic Impact Zones:** Replaced linear accuracy math with a tiered Zone system (Perfect, Good, Poor, Mishit).
- **Forgiveness Multiplier:** Swing power now directly affects the width of the Impact Zones. Swings under 100% power expand the perfect timing window; overswings drastically shrink it, heavily penalizing players with extreme sidespin.
- **Distance Readout Toggle:** Added a dedicated key (`T`) to announce the current distance to the pin from any state.
### Changed
- **Keybindings:** Fixed the Shot Style toggle to map to `S` instead of `T`. 
- **Spin Physics:** Scaled base backspin calculations by the Shot Style distance modifier (`distMod`), correctly preventing short chips from spinning backward on the green.## [2.16.0] - 2026-03-16
### Added
- **Expanded Shot Styles:** Replaced "Swing Types" with an expanded "Shot Styles" object array. Added "Half Pitch" and "Short Chip" to allow players to hit precise yardages using 100% power swings.
- **Wind Resistance Modifiers:** Short game shots are now highly resistant to wind. Pitches and Chips take 25% wind effect; Flops take 50%.
- **Dynamic Yardage Calculation:** Changing clubs or shot styles now prompts the Caddy to announce the exact total yardage (carry + roll) a 100% power swing will achieve with the current setup.
### Changed
- **Keybindings:** Shot Style toggle moved from `T` to `S`. PageUp/PageDown club selections now include the active Shot Style calculations in the readout.## [2.15.1] - 2026-03-16
    ### Fixed
- **Short Game Physics Hotfix:** Successfully injected the mathematical modifiers for Pitch, Chip, and Flop shots into the `calculateShot` function (which failed to deploy in 2.15.0).
- **Chipping Green Reveal Hotfix:** Updated the post-shot Reveal logic to correctly calculate proximity to the pin, announce the results, and generate a new random target distance when in `chipping` mode.## [2.10.0] - 2026-03-15
### Added
- **Stance Mechanics:** Players can now move the ball forward or backward in their stance across 5 positions (Far Forward, Forward, Neutral, Back, Far Back).
- **Trajectory & Spin Modifiers:** Stance position directly alters `dynamicLoft` (+/- 10 degrees) and `backspinRPM` (+/- 1000 RPM).
- **Keybindings:** Mapped `Home` (move ball forward) and `End` (move ball backward) to control the stance while in the Idle state.
- **Auto-Reset:** Stance automatically resets to Neutral after every completed shot to prevent accidental mishits on the next swing.## [2.9.0] - 2026-03-15
### Added
- **Rotational Aiming:** Players can now rotate their stance using the `Left` and `Right` arrow keys.
- **Precision Modifiers:** Pressing `Shift` + `Left/Right` shifts the aim by 5 degrees at a time, while standard presses shift by 1 degree.
- **Trigonometric Dispersion:** The `lateralX` landing position now incorporates the `aimAngle`. The engine calculates the lateral offset using `Math.tan` based on the total distance of the shot.
- **Aim Reset:** The player's aim automatically resets to 0 (dead straight) after every completed shot.## [2.8.0] - 2026-03-15
### Added
- **Dynamic Wind Physics:** Introduced randomized wind vectors (`windX` for crosswinds, `windY` for headwinds/tailwinds) ranging from -15 to +15 mph.
- **Trajectory Interaction:** Wind effect is multiplied by `hangTimeSecs`. High-loft shots (Wedges, "Fat" strikes) are heavily affected by the wind; low-loft shots (Drivers, "Thin" stingers) pierce through it.
- **Weather Report (Key W):** Mapped the `W` key to announce the current wind speed and direction while in the Idle state.
- **Pre-Shot Randomization:** Wind conditions regenerate automatically after every completed shot.## [2.7.0] - 2026-03-15
### Added
- **Spin Rate Physics (RPM):** Introduced explicit Backspin and Sidespin calculations derived from Hinge and Impact timing.
- **Spin-to-Roll Linkage:** Roll distance is now a direct function of landing velocity and Backspin RPM rather than a flat multiplier.
- **Caddy Report Expansion:** Added "Spin Metrics" to the Caddy Report (Key C) to provide feedback on ball rotation.## [2.6.0] - 2026-03-15
### Changed
- **Polyphonic Audio Overlap:** The flight "whoosh" now persists and fades during the first 300ms of the landing phase, preventing sharp auditory cut-offs.
- **Wider Bounce Spacing:** Increased the base gap and distance-scaling factor to ensure skips remain distinct even on high-velocity shots.
- **Improved Decay Physics:** Updated the timing decay to 0.85 and volume to 0.8 to keep late-sequence bounces audible and rhythmic.
- **Independent Gain Nodes:** Each bounce now utilizes a distinct gain envelope to maximize volume without clipping the master output.## [2.5.0] - 2026-03-15
### Changed
- **Bounce Interval Physics**: Recalibrated the timing between bounces to prevent "clustering." 
- **Dynamic Gaps**: The initial gap between the first and second bounce now scales with `rollDistance`, and the decay factor has been eased to ensure skips are distinct even at high roll speeds.
- **Audio Sequencing**: Refined the transition between the final bounce and the start of the "Roll" rumble to ensure zero auditory overlap.## [2.4.0] - 2026-03-15
### Changed
- **Bounce Density Physics**: Rebalanced the bounce-count formula to better reflect low-trajectory clubs.
- **Trajectory Integration**: Added `dynamicLoft` as a variable in determining bounce frequency. A well-struck Driver (10-12 degrees) now consistently produces 4-5 skips on firm fairways.
- ## [2.3.0] - 2026-03-15

### Added
- **Dynamic Bounce Audio:** Replaced the static landing thud with a dynamic bounce sequence.
- **Trajectory-Based Ground Interaction:** The number of bounces, the timing between them, and their volume now scale mathematically with the `rollDistance`. High-loft clubs (Wedges) produce a single heavy thud, while low-loft clubs (Driver, Woods) produce multiple cascading, skipping bounces before the roll out.## [2.1.0] - 2026-03-15

### Added
- **Golf Bag Array:** Introduced 11 selectable clubs (Driver through Sand Wedge), each with specific base distances, lofts, and roll percentages.
- **Club Selection:** Mapped `PageUp` and `PageDown` to cycle through clubs while in State 0 (Idle), complete with `window.announce` feedback.
- **Flight Phase (State 5):** Added a new state that locks out keyboard input while the ball is "in the air."
- **Trajectory & Roll Physics:** Introduced dynamic loft and roll calculations. Fat/Thin hinge misses now directly impact hang time and roll-out distance.
- **Audio Sequencer:** Replaced instant impact reporting with a sequenced audio event: Strike (triangle wave) -> Flight Wind (white noise) -> Landing (sine thud) -> Roll (brown noise rumble) -> Caddie Reveal.