## [4.11.0] - 2026-03-19
### Changed
- **Grid Scorecard Navigation:** The `Shift + S` full scorecard no longer relies on native HTML table elements or screen reader browse modes. It has been rebuilt as a custom 2D JavaScript grid. Arrow keys are now strictly intercepted, maintaining the application focus trap while dynamically generating highly readable ARIA announcements (e.g., "Hole 3, Putts: 2") based on your position in the grid.
## [4.10.1] - 2026-03-19
### Fixed
- **The "Permanent Putter" Bug:** Fixed a critical state-management flaw where progressing to the next hole via the `Enter` key failed to reset the `isPutting` and `puttState` variables. `loadHole()` now explicitly wipes all short-game states and grip modifiers, guaranteeing a fresh, standard swing mode on the next tee box.
## [4.10.0] - 2026-03-19
### Added
- **Round Progression System:** The game now flows continuously. Sinking a putt triggers a prompt to "Press Enter" to proceed to the next hole, looping seamlessly through the 9-hole course.
- **Advanced Stat Tracking:** The engine now silently tracks Putts, FIR (Fairways in Regulation), and GIR (Greens in Regulation) based on your lies and stroke counts.
- **The Scorecard Array:** Added a `roundData` object array that persists in `localStorage` alongside your physical coordinates.
- **Two-Tier Score Readouts:** - Press `S` for a quick auditory summary (e.g., "Through 4 holes, you are +2"). 
  - Press `Shift + S` to open the Full Scorecard. This dynamically generates a semantic HTML `<table>`, allowing screen reader users to navigate row-by-row and column-by-column using native table commands.## [4.9.0] - 2026-03-19
### Added
- **Browser Persistence (Autosave):** Implemented HTML5 `localStorage`. The engine now silently saves the complete game state (coordinates, lie, strokes, wind, telemetry, and active club) after every shot and hole change. Reloading the page will automatically resume the game exactly where the player left off.

### Fixed
- **The Hazard Menu Trap:** Fixed a soft-lock issue where players could get stuck in the `KeyH` hazard menu. The `ArrowDown` key no longer gets silently swallowed if the player forgets to close the menu, and `Enter` has been added as a secondary exit key alongside `Escape`.
## [4.8.0] - 2026-03-18
### Added
- **Putter Strike Sound:** Added a distinct, sharp "tink" (`800Hz triangle`) the exact millisecond the putter strikes the ball, creating a clear auditory anchor before the rolling metronome begins.
- **The Green Arpeggio:** Reintroduced the victory arpeggio (`440Hz -> 554Hz -> 659Hz`) when a player successfully lands an approach shot on the putting surface.
- **Global Volume Overdrive:** Wrote a wrapper function that intercepts all Web Audio API calls (`playTone` and `playNoise`) and multiplies their volume by 2.5x, massively boosting the audibility of the swing mechanics and physics engine without affecting your screen reader's TTS volume.

### Fixed
- **Telemetry Wipe Bug:** Fixed a critical order-of-operations bug where landing on the green would trigger an early `return` and bypass the `holeTelemetry.push()` function. The engine now safely records the approach shot to your clipboard data *before* warping you to the putting interface.
## [4.7.2] - 2026-03-18
### Fixed
- **The True North Putting Bug:** Fixed a critical 3D geometry bug where the putting engine and God Caddy failed to calculate the relative angle between the ball and the pin on the course. `currentHeading` now correctly integrates `Math.atan2()`, ensuring `0 degrees` of aim is always perfectly locked onto the center of the cup regardless of where the player lands on the green.
## [4.7.1] - 2026-03-18
### Fixed
- **Ghost Simulator Accuracy:** Fixed a bug where the Level 3 Caddy would occasionally recommend extreme aim angles on short putts because it was locking onto the very first angle that clipped the outer edge of the cup's capture radius. The `for` loop now radiates outward from `0` degrees, guaranteeing it always finds the dead-center line of the putt first.
## [4.7.0] - 2026-03-18
### Added
- **The Green Dictionary:** Added `greenDictionary` to `data_ag.js`, storing 9 classic golf architecture templates (e.g., The Redan, The False Front, The Turtleback) as pre-configured arrays of Contour Zones.
- **Course Integration:** All 9 holes of "The Holodeck Links" now have a `greenType` assigned to them. When the player reaches the green on the course, the engine and the Caddy dynamically load the contour math from the dictionary.
## [4.6.1] - 2026-03-18
### Fixed
- **Putt Timing & Suspense:** Significantly adjusted the Spatial Audio Metronome math. The minimum delay between yardage steps was increased from 40ms to 100ms, and the speed divisor was doubled. This effectively doubles the real-world time it takes for a putt to complete, mimicking realistic green friction and vastly increasing the suspense as the ball slows down near the hole.
## [4.6.0] - 2026-03-18
### Added
- **Spatial Audio Rolling:** Putts no longer calculate and complete instantly. The physics engine now records the step-simulation and plays it back in real-time. 
- **The Rolling Metronome:** As the ball rolls, it emits an auditory pulse. The time between pulses is directly tied to the ball's speed. As the ball goes uphill, the pulses slow down. As it crests a ridge and goes downhill, the pulses speed up, allowing the player to literally hear the gravity acting on the ball.
- **Hyper-Panning:** The lateral movement of the ball is aggressively mapped to a Stereo Panner. A ball that breaks just 3 yards to the left or right will pan 100% into that respective ear, creating an exaggerated, highly readable auditory trace of the break.
## [4.5.0] - 2026-03-18
### Changed
- **The Augusta Practice Green:** The Practice Putting Green has been expanded and redesigned into a massive 45-yard, 3-tier monster. 
    - Tier 1 (45y - 25y): The False Front (Steep uphill, hard Right-to-Left).
    - Tier 2 (25y - 10y): The Plateau (Flat, subtle Left-to-Right).
    - Tier 3 (10y - 0y): The Bowl (Fast downhill, sweeping Right-to-Left).
- **Expanded Putting Targets:** Pressing `T` on the practice green now spawns targets anywhere from 5 yards up to 45 yards to test all three tiers.
## [4.4.2] - 2026-03-18
### Fixed
- **Putting Dev Keys:** Hooked `F1` (Dev Power) into the putting simulation. It will now correctly force 100% power on the green.
- **The Ghost Oracle:** Completely rewrote the Level 3 Caddy math. Instead of estimating breaks using flat algebra, the Caddy now runs a brute-force "Ghost Simulation," testing thousands of aim and distance combinations against the Gravity Engine to guarantee a mathematically perfect read.
## [4.4.1] - 2026-03-18
### Fixed
- **Putting Input Bug:** Fixed a critical bug where the `Down Arrow` target adjustment prevented the player from initiating a putting swing.
- **Swing Mode Toggle:** Reintroduced the `P` key toggle on the green. `puttState = 0` (Targeting Mode) maps arrows to aim and distance. `puttState = 1` (Swing Mode) locks aim and maps the `Down Arrow` back to the standard swing initiator, allowing for Spacebar Tempo touches in phases 1 and 3.
## [4.4.0] - 2026-03-18
### Changed
- **Streamlined Putting Controls:** Removed the Braille Grid and the `Enter/P` mode toggles. While on the green, the Arrow Keys are now instantly mapped to your putt setup: `Up/Down` adjusts target distance, and `Left/Right` adjusts aim. 
- **The Gravity Engine:** Upgraded the putting physics from a flat 2D line to a 1-yard Step Simulation. As the ball rolls, it calculates its exact position every yard, applies gravity from the active Contour Zone to bend the trajectory, and accelerates/decelerates based on the slope.
- **Narrative Caddy:** The Caddy (`KeyA`) now provides 3 distinct tiers of green-reading advice. Level 1 gives basic directions, Level 2 provides conversational strategic advice, and Level 3 acts as a mathematical oracle to help you perfectly calibrate your aim and target distance.
- **Forgiving Cup Size:** The physical capture radius of the hole has been widened to 15 inches (0.4 yards) to accommodate the new complex breaking physics, making putts far more realistic to sink.
## [4.3.0] - 2026-03-18
### Added
- **Contour Zones:** Replaced the single-plane green math with an array-based Zone system. Greens can now have infinite tiers, ridges, and double-breaks based on distance to the hole.
- **Double-Breaker Practice:** Upgraded the Practice Putting Green to feature a severe ridge at the 10-yard mark to test multi-zone putts.
- **The Putting Caddy:** The Level 3 Caddy can now read the green. Pressing `A` while putting will calculate which Contour Zones lie between the ball and the hole, and verbally describe the elevation and break of each segment.
- **Zone-Aware Braille Grid:** The `Arrow` key exploration now dynamically pulls the `slopeX` and `slopeY` of whichever specific Contour Zone your virtual finger is currently touching.
## [4.1.0] - 2026-03-18
### Added
- **Practice Putting Green:** Press `Shift + P` from anywhere to travel to the dedicated Putting Sandbox.
- **Infinite Retries:** On the Practice Putting Green, the hole is never "completed." After your putt stops, the ball instantly teleports back to your starting position so you can try the exact same break and distance again to dial in your tempo.
- **Quick Targets:** Press `T` while in Explore Mode on the Putting Green to instantly generate a new random target flag (between 5 and 30 yards away).
## [4.0.0] - 2026-03-18
### Added
- **The Putting Engine (v1):** The game now transitions into a dedicated 2D Putting Phase when the ball lands on the green within 20 yards of the pin.
- **Braille Green Exploration:** Press `P` to enter Pre-Putt mode. Use the `Arrow Keys` to explore a 3-column audio grid representing the green's topography. Tones are stereo-panned (Left/Center/Right) and pitch-modulated (High = Uphill, Low = Downhill).
- **Targeting Mechanics:** Use `[` and `]` to adjust aim angle, and `-` and `=` to push your target distance past the hole for uphill/slow putts. You will always swing to the 100% audio mark.
- **"Jam It In" Physics:** Putts under 2 yards (6 feet) automatically snap the target to the cup. Hitting 100% power with >90% accuracy on short putts dramatically widens the cup's capture radius, allowing you to blast through minor breaks.
## [3.83.0] - 2026-03-18
### Added
- **Predictive Shape Caddy:** The Launch Monitor (`KeyA`) now runs a full lateral physics simulation before your shot. It calculates your expected wind drift and the side-spin generated by your Stance Alignment (Open/Closed) to predict the exact flight path curve. The Caddy will now warn you if your shot shape curves back into an obstacle, or praise you if your shape successfully bends around one.
## [3.82.0] - 2026-03-18
### Added
- **Comprehensive Telemetry Export:** The `lastShotReport` (and the `Shift + C` clipboard export) now captures the complete pre-shot Setup State. It logs the active Club, Choke status, Shot Style, Stance, Alignment, Aim Angle, and exact Wind conditions alongside the Execution metrics.
- **Environment Logging:** The active state of the Synth Tree (Distance, X-Coordinate, and Threshold Height) is now permanently stamped into the shot's telemetry log for post-shot analysis.
## [3.81.1] - 2026-03-18
### Fixed
- **Launch Monitor Parity:** Fixed a bug where the `getCaddyAdvice` and the `KeyY` Ghost Simulation were calculating flight apexes using raw total distances. They now correctly apply the `loftDistMod` penalty and subtract spin-calculated roll to find the true `expectedCarry`, perfectly syncing Caddy predictions with the actual `physics_ag.js` engine.
- **Bag Check Accuracy:** Updated `getSetupReport()` (`KeyX`) to include the `loftDistMod` penalty so the reported expected distance accurately reflects your active stance adjustments.
## [3.81.0] - 2026-03-18
### Fixed
- **Parabolic Flight Math:** Replaced the linear trigonometric height formula with a proper parabolic projectile motion formula (`y = (tan(θ) / D) * x * (D - x)`). 
- **Tree Collision Accuracy:** Ball height mid-flight is now significantly more realistic. Irons apex between 70-100 feet instead of scaling infinitely upwards, meaning players must make smarter tactical decisions when trying to clear tall trees.
## [3.80.2] - 2026-03-18
### Added
- **Flight Path Narrator:** The engine now tracks vertical and lateral margins as the ball passes obstacles. The generic shot shape description (e.g., "A high draw") is dynamically replaced with contextual narratives (e.g., "A high draw that shaved the right edge of the Synth Tree canopy by just 2 yards").
- **Mid-Air Course Collision:** Upgraded natural course trees to use the same mid-air flight interpolation as the Synth Trees, allowing players to curve the ball around trunks mid-flight or hit branches before the ball lands.
- **Hole Reports:** Because the Flight Path Narrator alters the core `lastShotReport`, pressing `Shift + C` now effectively generates a highly descriptive, shot-by-shot story of the entire hole.
## [3.80.1] - 2026-03-18
### Added
- **Choke Down Grip:** Press `V` to toggle your grip. Choking down caps the club's potential distance at 90% (allowing you to comfortably swing to the 100% audio ping for shorter targets) and increases the timing forgiveness windows for Hinge and Impact by 20%. 
- **Caddy Edge Finder:** The Telemetric Caddy (`KeyA`) on the Holo Range now explicitly calculates and announces the exact degrees left and right required to bypass the active Synth Tree's canopy.
## [3.80.0] - 2026-03-18
### Added
- **Synth Trees (Threshold Sandbox):** Added dynamic obstacles to the Holo Range and Pitching Green. Press `Y` to spawn a Synth Tree exactly 70% of the way to the pin on your target line. 
- **Coordinate Control:** Use `[` and `]` to move the tree left and right in 5-yard increments. Use `-` and `=` to move it closer or further.
- **Ghost Simulation (Threshold Math):** When spawned, the tree automatically sets its height to the exact apex of a 110% power, 90% hinge, 95% impact shot for your currently equipped club and stance. 
- **Manual Override:** Press `Shift + Y` to manually cycle the tree's height in 10-foot increments.
- **Launch Monitor Caddy:** Pressing `A` on the range now runs a live trigonometric simulation to tell you if your current setup will clear the Synth Tree or hit the trunk.
## [3.70.0] - 2026-03-17
### Added
- **Navigable Help Dialog:** Pressing `?` (Shift + `/`) now opens a navigable, line-by-line Help Menu dialog, replacing the old static audio dump.
- **Screen Reader Parity:** The Help Menu acts like a DOM document. Players use `ArrowUp` and `ArrowDown` to read line-by-line, and `H` or `Shift + H` to jump forward and backward between major headings. `Enter` or `Escape` closes the dialog and restores game controls.
## [3.62.0] - 2026-03-17
### Added
- **Telemetry Dashboard:** Added a persistent 4-column visual dashboard to the HUD for sighted players. It continuously displays Hole/Target Info, Environment (Wind/Lie), Equipment (Club/Style), and Setup (Aim/Stance).
- **Real-Time UI Binding:** Wired `window.updateDashboard()` into all relevant keyboard inputs (`input_ag.js`) and physics loops (`physics_ag.js`) to ensure the visual dashboard instantly reflects the player's active state.
## [3.61.0] - 2026-03-17
### Added
- **Visual Marquee:** Added a retro, scrolling text marquee at the top of the HUD that visually mirrors all `window.announce()` ARIA broadcasts in real-time.
- **Dedicated Caddy Panel:** Created a permanent, stylized UI panel to display the post-shot `lastShotReport` and telemetry, cleanly separating it from the primary swing status prompts.
## [3.60.0] - 2026-03-17
### Added
- **Canvas Rhythm Bar (Swing Meter):** Introduced a 60fps, GPU-accelerated visual representation of the audio mechanics for sighted spectators. 
- **Visual Hinge Tracking:** Tapping the Spacebar now drops permanent visual markers (Yellow for Backswing, Orange for Downswing) on the bar to visually map the `hingeDiff`.
- **Visual Impact Zone:** The Impact Drop phase now displays a moving cursor traveling toward a glowing green "Sweet Spot" target line.
## [3.51.0] - 2026-03-17
### Changed
- **Advanced Caddy Brain:** Overhauled `window.getCaddyAdvice` to track granular lie states and distance brackets. The semantic engine now recognizes `Bunker_Fairway`, `Rough_Deep`, `Approach_Scoring` (inside 120 yards), and `Approach_Long`.
- **Hole 3 Master Data:** Expanded Hole 3's `caddyNotes` to serve as the gold standard template for all future holes, including specific advice for fairway bunkers, deep rough, scoring wedges, and layup zones.
## [3.50.0] - 2026-03-17
### Added
- **Front Nine Yardage Book:** Added full semantic `caddyNotes` (Levels 1-3) and `fairwayDescription` strings for Holes 1, 2, 5, 7, 8, and 9.
### Fixed
- **Hole 6 Caddy Logic:** Corrected a semantic trigger bug on Hole 6 where "Approach" advice was mistakenly being read on the "Tee".
## [3.46.0] - 2026-03-17
### Changed
- **Caddy Advice Hotkey:** Moved from `N` / `Shift+N` to `A` / `Shift+A`.
- **Green Proximity Broadcast:** The detailed "Feet & Inches" proximity and roll report is now automatically announced via ARIA when finishing a hole, eliminating the need to press `C` to check where the ball landed and rolled.
### Added
- **Fairway Description (KeyF):** Pressing 'F' now reads a detailed description of the fairway's shape, width, and "dead space" to help map areas between target zones. Added custom fairway descriptions to Holes 3 and 6.
## [3.45.0] - 2026-03-17
### Added
- **Caddy Skill System:** Added a `caddyLevel` variable (Levels 1 to 3) that dictates the quality of strategic advice available to the player.
- **The Yardage Book (KeyN):** Pressing 'N' now prompts the Caddy to read context-aware advice based on your current semantic location (Tee, Approach, Greenside, Trouble_Left, Trouble_Right).
- **Dynamic Caddy Toggle:** Pressing 'Shift + N' cycles the Caddy Level between Rookie (1), Veteran (2), and Tour Pro (3) for testing.
- **Hole Data Expansion:** Added `caddyNotes` arrays to Holes 3, 4, and 6 to serve as the prototype for the future master CSV database.
## [3.44.0] - 2026-03-17
### Fixed
- **Bunker Interception:** Added roll-path detection. If a ball's roll phase enters a bunker or hazard, the roll is now terminated, and the ball settles inside the hazard.
- **Stroke Counter Hard-Reset:** Added an explicit stroke reset to the Warp (Digit) keys to prevent "shot-bleeding" between holes.
- **Sand Sound Trigger:** Fixed a bug where balls rolling into sand would still play the "Fairway Roll" grass noise.
## [3.43.0] - 2026-03-17
### Added
- **Tactical Hybrid Hazard Reporting (KeyH):** Re-integrated "Aim Line" raycasting into the Hazard menu. The Caddy now provides a two-part report for every obstacle:
    1. **Edge Finder:** The angles (degrees) required to aim left or right to bypass the obstacle.
    2. **Aim Line Intersect:** The specific distance required to reach and clear the obstacle on your *current* heading.
- **Dynamic Aim Updates:** If you rotate your aim (Arrow Keys) while the Hazard menu is open, the Caddy will now update the "Aim Line" distances in real-time.
## [3.42.0] - 2026-03-17
### Added
- **Loft-Based Distance Scaling:** Ball placement now physically affects shot distance. Moving the ball back (Lower Loft) increases forward velocity, while moving it forward (Higher Loft) increases vertical apex and reduces total distance.
- **Spin-Dependent Roll:** The `rollDistance` calculation now factors in `backspinRPM`. High-spin shots (from a 'Back' stance or flushing a wedge) will "check up" faster, while low-spin shots will release and roll further.
## [3.41.0] - 2026-03-17
### Added
- **Dynamic Green Lie:** The engine now uses the specific `greenRadius` from the course data to determine if a ball is on the green, replacing the previous hardcoded 20-yard limit.
- **Extended Precision Reporting:** Increased the threshold for high-precision reporting from 5 yards to 30 yards. Any ball landing or finishing within 30 yards of the pin (covering the entire green and fringe) will now report in feet and inches.
- **Landing Awareness:** The Caddy now intelligently detects if a ball *hit* the green during flight, even if it eventually rolled off into the rough. In these cases, it provides the full "Landed... Rolled... Finished..." narrative to help the player understand why the ball failed to hold the green.
## [3.40.1] - 2026-03-17
### Fixed
- **Edge Finder Math:** Fixed a coordinate typo in `input_ag.js` where the Caddy was looking at the wrong axis to calculate hazard center-points.
- **Redundancy Cleanup:** Removed the duplicate `announceHazard` function from `main_ag.js`.
- **Physics Polish:** Cleaned up unreachable logic in `physics_ag.js` and removed unused variables in the proximity reporting block.
## [3.40.0] - 2026-03-17
### Added
- **The Edge Finder (KeyH):** Upgraded the Obstacle List math. When inspecting a tree or hazard, the Caddy now calculates the exact aim angles (in degrees) required to clear the left and right edges of the obstacle from your current position.
- **Granular Green Reporting:** When a ball settles on the Green, the narrative now shifts to "Proximity Mode." It reports the landing spot relative to the pin (e.g., "Landed 4 yards short"), the roll-out behavior ("Rolled 6 feet past"), and the final miss distance in feet and inches.
- **Micro-Distance Units:** Added a unit conversion engine that automatically switches from yards to feet and inches for high-precision proximity reporting (under 5 yards).
## [3.39.0] - 2026-03-17
### Changed
- **Progressive Power Pressure:** Removed the harsh >105% power penalty cliff. Powering up over 100% now smoothly and progressively widens the lateral dispersion cone (e.g., 106% power results in a ~24% wider dispersion radius) and scales side spin naturally.
- **Short Game Side Spin Dampening:** Short game swing styles (Pitch, Chip, Flop) now actively suppress side spin generation to simulate locked wrists, preventing unrealistic lateral kicks on short shots.
- **Short Game Rough Mechanics:** When hitting short game styles out of the rough, impact timing errors now simulate real-world grass interactions. Early impacts (-25ms) simulate "trapped grass," severely deadening spin and distance. Late impacts (+25ms) simulate "skulled/bladed" shots, resulting in low trajectories, minimal spin, and massive rollouts.
## [3.38.0] - 2026-03-17
### Changed
- **Contextual Club Check (KeyX):** The `X` command now accurately reflects your current lie. If you are in the Sand or Light Rough, pressing `X` will announce the dynamically penalized distance range (e.g., 85%-95%) rather than the "ideal conditions" yardage.
## [3.37.1] - 2026-03-17
### Changed
- **Audio UX Reordering:** The `getSetupReport()` function (triggered when switching clubs) now reads in a strictly prioritized order: 1. Club, 2. Distance/Range, 3. Lie, 4. Shot Style.
- **Shot Style Toggle (KeyS):** Decoupled the `KeyS` audio from the full setup report. Pressing `S` now provides a short, snappy confirmation of the newly selected style (e.g., "Style: Flop") without reading redundant club or lie information.## [3.37.1] - 2026-03-17
### Changed
- **Audio UX Reordering:** The `getSetupReport()` function (triggered when switching clubs) now reads in a strictly prioritized order: 1. Club, 2. Distance/Range, 3. Lie, 4. Shot Style.
- **Shot Style Toggle (KeyS):** Decoupled the `KeyS` audio from the full setup report. Pressing `S` now provides a short, snappy confirmation of the newly selected style (e.g., "Style: Flop") without reading redundant club or lie information.
## [3.37.1] - 2026-03-17
### Changed
- **Audio UX Reordering:** The `getSetupReport()` function (triggered when switching clubs) now reads in a strictly prioritized order: 1. Club, 2. Distance/Range, 3. Lie, 4. Shot Style.
- **Shot Style Toggle (KeyS):** Decoupled the `KeyS` audio from the full setup report. Pressing `S` now provides a short, snappy confirmation of the newly selected style (e.g., "Style: Flop") without reading redundant club or lie information.
## [3.37.0] - 2026-03-17
### Added
- **Hole Telemetry (Flight Recorder):** Added a global `holeTelemetry` array that acts as a flight recorder. It stores the full physics readout and narrative description of every single shot taken during the current hole or practice session.
- **Bulk Export (Shift + C):** Upgraded the `KeyC` clipboard command. Pressing `Shift + C` now copies the entire `holeTelemetry` log for the current hole to the system clipboard, formatted chronologically, allowing players to export and review their stroke-by-stroke analytics.
## [3.36.1] - 2026-03-17
### Changed
- **Lie Penalties:** Separated "Sand" and "Light Rough" mechanics. Light Rough now yields 85%-95% distance (formerly 60%-90%), with slightly reduced dispersion penalties compared to Sand. Sand remains at a harsh 60%-80% distance penalty.
- **Smart Auto-Club (KeyZ):** The Caddy's auto-club selection algorithm now factors in your current lie. If you are in the rough or sand, it will automatically "club up" to account for the expected distance loss.
- **Caddy Reports:** `getSetupReport()` now dynamically announces the specific yardage ranges based on whether you are in Sand or Light Rough.
## [3.36.0] - 2026-03-17
### Added
- **Front Nine Complete:** Expanded `The Holodeck Links` in `data_ag.js` from 3 holes to 9 holes, featuring new dynamic obstacles like Driveable Par 4s, Island Greens, and Tree Clusters.
- **Narrative Descriptions:** Added a `description` property to all 9 holes to paint a vivid mental picture of the hazards and ideal strategy.
- **Course Guide (KeyD):** Players can press `D` at any time before swinging to hear the Caddy read the narrative description of the current hole.
## [3.35.0] - 2026-03-17

### Added
- **Lie Tracking (KeyL):** Added a global `currentLie` state. Players can press `L` at any time to hear their current lie (Tee, Fairway, Light Rough, Sand, Green). In Driving Range mode, `L` still toggles the virtual lie.
- **Club Check (KeyX):** Added the `X` key to announce the currently equipped club and its expected distance at 100% power under ideal conditions.
- **Persistent Telemetry (KeyC):** The `C` command now works even when the hole is complete, allowing players to review the final approach shot telemetry before advancing.

### Changed
- **Setup Report Reordering:** When switching clubs with `PageUp/PageDown`, the `getSetupReport()` function now announces the swing style *last*, prioritizing the club name and yardage.
## [3.34.0] - 2026-03-17
### Added
- **Tactical Landing Zones:** Added a `zones` array to `data_ag.js`. The engine now supports discrete fairway targets (e.g., "Safe Drive", "Aggressive Carry") distinct from the pin.
- **Zone Targeting (KeyZ):** Players can press `Z` to cycle through available landing zones that are ahead of the ball. The Caddy will automatically rotate the player's 0-degree aim line to the selected zone.
- **Auto-Caddy Club Selection:** When switching zones, the Caddy automatically equips the club whose base distance best matches the yardage to the new target.
- **Dynamic Target Math:** Upgraded `physics_ag.js` and `main_ag.js` to calculate shot angles and raycasting based on `targetX` and `targetY` instead of always defaulting to the pin.
## [3.32.0] - 2026-03-16
### Added
- **Tactical Hazard Raycasting:** Upgraded the `announceHazard` function to include "Carry and Clear" math.
- **Dynamic Caddy Advice:** The Caddy now actively calculates if a rectangular hazard (Water, Bunker) is in your current flight path based on your exact `aimAngle`. If it is, the Caddy announces the exact yardage required to reach the front edge and clear the back edge on that specific line.
## [3.33.1] - 2026-03-16
### Fixed
- **Data Syntax Error:** Fixed a critical missing comma and duplicated array objects in the Hole 1 `trees` array within `data_ag.js` that caused an engine crash.

### Added
- **Black Box Telemetry:** Added `treeCollisionReport` to the physics engine. The internal math (tree height vs. calculated ball flight height) is now appended to the hidden `metrics` string. 
- **Telemetry Export:** Players can now use the `Shift + C` command to copy the exact tree collision math to their system clipboard for testing and debugging.## [3.33.0] - 2026-03-16
### Added
- **Caddy Sight Cone:** The Caddy now filters tree announcements based on a 30-degree "in-play" cone extending toward the target.
- **Vertical Clearance:** Trees now have a `height` property. Ball flight logic in `physics_ag.js` now checks `dynamicLoft` to determine if a ball sails over a tree or strikes it.
- **Sight Report:** Added `getSightReport()` to `main_ag.js` to specifically announce trees that are currently blocking the line of sight.## [3.32.1] - 2026-03-16
### Changed
- **Hazard List Integration:** Updated KeyH logic to merge 'hazards' and 'trees' into a single navigable list for pre-shot scouting.
- **Unified Announcement:** Updated announceHazard to handle the circular coordinate format (x, y, radius) used by tree objects.## [3.32.0] - 2026-03-16
### Added
- **Tree Obstacles:** Added a `trees` array to hole data in `data_ag.js`, allowing for circular obstacles with specific `x`, `y`, and `radius`.
- **Collision Detection:** Implemented intersection math in `physics_ag.js` to determine if a ball's flight path crosses a tree's volume.
- **Tree Audio:** Added a "thwack" sound effect for branch/trunk collisions.## [2.31.0] - 2026-03-16
### Added
- **Pin Placement Data:** Added a `pinLocation` string property (e.g., "Front-Left", "Back-Right") to the hole data objects in `data_ag.js`.
- **Target Context (KeyT):** Upgraded the `KeyT` (Target) command in `input_ag.js` to append the specific `pinLocation` to the distance announcement when playing on the course.

### Changed
- **Version Headers:** Updated the top comment lines of `data_ag.js` and `input_ag.js` to reflect v3.31.0.## [2.30.2] - 2026-03-16
### Added
- **Hazard Audio:** Added `playSplash()` to the audio engine, synthesizing a splash and droplet "bloop" sound for water hazards.
- **Water Penalty Logic:** The physics engine now detects if a lie contains the word "water". If so, it mutes standard bounce/roll audio, plays the splash sound, increments the `strokes` counter by 1, and explicitly announces the penalty in the subsequent Caddy report (e.g., "Stroke 4 (includes 1 stroke water penalty).")
- **Hazard Green Protection:** Prevented the `isHoleComplete` trigger from firing if a ball lands in a water hazard within 20 yards of the pin.## [2.30.1] - 2026-03-16
### Fixed
- **Absolute Positional Reporting:** Updated the Caddy's post-shot broadcast in `physics_ag.js` to report the ball's absolute distance from the center of the fairway (`ballX`), rather than its lateral deviation from the aimed line. This gives blind players true grid-coordinate awareness.

### Changed
- **Hole 1 Hazard:** Expanded the Hole 1 water hazard in `data_ag.js` to be twice as wide (40 yards), keeping its right edge 25 yards left of the fairway and extending it deeper into the left rough.## [2.30.0] - 2026-03-16
### Added
- **Hazard Data System:** Added `hazards` arrays to course data. 
    - Hole 1: Left-side Water Hazard (100y to 350y).
    - Hole 2: Greenside Sand Bunker (Right side).
    - Hole 3: Center Cross-Bunker at 270y (forces layup or 120% drive).
- **Dynamic Choke Points & Aprons:** Added `approachWidth` and `apronRadius` parameters to course data to simulate fairways narrowing near the green (Hole 3 narrows to 10 yards, with a 30-yard wide apron).
- **Hazard List UI (KeyH):** Players can now press `H` to enter the Hazard List and use `Up/Down` arrows to scan the exact distance, width, and depth of all hazards on the current hole.
- **Hazard Physics:** The physics engine now detects if a ball lands in a hazard rectangle. Bunkers change the lie; Water adds a 1-stroke penalty and forces a drop.## [2.29.1] - 2026-03-16
### Fixed
- **Hole 3 Geometry:** Adjusted Hole 3's `pinY` to `537` so the hypotenuse perfectly matches the 540-yard total distance.
- **State Soft-Lock:** Added `swingState = 0;` to `loadHole()` and removed duplicated function definitions in `main_ag.js` to prevent the engine from freezing if players change holes mid-swing.
- **Dynamic Fairway Bounds:** Updated `physics_ag.js` to evaluate rough penalties based on the specific `fairwayWidth` of the current hole, rather than relying on a hardcoded 35-yard global width.

### Changed
- **Modular Versioning:** Added v2.29.1 version stamps to the top comment line of all JavaScript module files.## [2.29.0] - 2026-03-16
### Added
- **Course Data Structure:** Added a `courses` array to `data_ag.js` to store specific hole layouts, pars, and pin coordinates (X, Y offsets).
- **Hole Loading Logic:** Introduced a `loadHole()` function in `main_ag.js` to automatically configure the environment, tee position, and default club based on the specific hole data.

### Changed
- **Initialization & Navigation:** Refactored `initGame()` and the `Digit` key shortcuts to utilize the new `loadHole()` function rather than hardcoding default 420-yard Par 4s.## [2.28.0] - 2026-03-16
### Added
- **Lateral Ground Mechanics (The "Kick"):** The engine now simulates how side spin grips the turf upon landing. Shots with high side spin will bounce and roll laterally (kicking left for hooks/draws, right for slices/fades).
- **Caddy Kick Quantification:** The Caddy's shot narrative has been updated to explicitly report the lateral ground movement, ensuring blind players can accurately map the turf interaction in their mental model (e.g., "rolls 5 and kicks 3 yds right").## [2.27.0] - 2026-03-16
### Changed
- **Gyroscopic Wind Physics:** Overhauled how wind affects ball flight in `calculateShot`.
- **Wind Base Nerf:** Increased the divisor for base wind effect, preventing the "parachute" ballooning effect on high-trajectory shots. A 3mph crosswind will now push a perfectly struck ball ~4 yards instead of 17.
- **Backspin Stability:** Added `gyroMod`. High backspin (>4000 RPM) now "cuts" through the wind, reducing lateral and vertical wind drift by up to 30%. Low backspin makes the ball more susceptible to wind.
- **Spin Amplification:** Side spin and crosswind now interact. Hitting a slice into a right-to-left wind will cause the spin to "fight" the wind, keeping the ball straighter. Hitting a slice with a left-to-right wind will amplify the curve.## [2.26.4] - 2026-03-16
### Added
- **Developer Testing Shortcuts:** Added three "Macro" keys to bypass swing timing for physics calibration.
    - **F1 (Perfect Power):** Forces `finalPower` to exactly 100%.
    - **F2 (Perfect Hinge):** Forces `hingeTimeBack` and `hingeTimeDown` to match, resulting in a 0ms Hinge Diff.
    - **F3 (Perfect Impact):** Forces `impactDiff` to 0ms.
- **Toggle State:** Pressing these keys toggles the "Cheat" mode on/off, with an audio announcement for each.## [2.26.3] - 2026-03-16
### Fixed
- **Range Telemetry Logging:** Fixed a bug where Driving Range shots were not saving to the `lastShotReport` variable. Pressing `KeyC` on the range will now correctly read back the most recent shot's data and telemetry.
- **Initial Callout Consistency:** Ensured the `rangeMsg` is correctly passed to the screen reader immediately following the shot.## [2.26.2] - 2026-03-16
### Added
- **Relative Proximity Reporting:** The Driving Range now provides directional context for shot results. Instead of just "yards from target," the Caddy now reports if the ball finished "Short-Left," "Long-Right," etc., relative to the player's initial line of sight.## [2.26.1] - 2026-03-16
### Changed
- **Physics Calibration:** Removed all hardcoded accuracy tiers.
- **Spin-Based Dampening:** Distance is now calculated using a base potential (Power) and then dampened based on Side Spin RPM.
- **Scaling Curve:** A 2000 RPM miss (roughly a 200ms error) now results in a ~10% distance penalty.
- **Shot Floor:** Implemented a physics floor. Even the worst possible "shank" (max side spin) will still travel at least 25% of the club's potential distance.
- **Lateral Weighting:** Increased the lateral movement multiplier so high-spin shots "leak" and "slice" further offline, matching player expectations.## [2.26.0] - 2026-03-16
### Changed
- **Impact Forgiveness Curve:** Removed the hardcoded accuracy tiers (the "85% distance cliff"). Impact accuracy (`impactAcc`) and lateral spray (`spinPenalty`) now scale smoothly using linear interpolation based on the exact millisecond offset. A near-perfect shot now receives a near-perfect distance multiplier.## [2.25.1] - 2026-03-16
### Added
- **Telemetry Export:** Added a `Shift + C` keybinding. Pressing this will instantly copy the entire `lastShotReport` (including all background math and telemetry) to your system clipboard, allowing for easy pasting into external tools or chats for physics debugging.## [2.25.0] - 2026-03-16
### Added
- **Holodeck Driving Range:** The Driving Range has been completely overhauled for rapid physics testing. 
- **Auto-Spawning Targets:** The target flag (`pinY`) automatically snaps to match the active club's base distance whenever the player changes clubs on the range.
- **Virtual Lie Toggle:** Added `KeyL` (active only on the Range) to instantly cycle the ground beneath the player's feet between "Fairway" and "Rough."
### Changed
- **Range Flow:** After every shot on the range, the ball instantly resets to the tee `(0, 0)`, aim and stance reset, but the wind continues to drift natively to simulate persistent weather. The Caddy now reports exact proximity to the auto-spawned target.## [2.23.1] - 2026-03-16
### Fixed
- **Lateral Dispersion Disconnect:** Fixed a critical bug where the ball's lateral movement (`physicsX`) was bypassing the `sideSpinRPM` variable. The physical ball flight now correctly matches the severe spin numbers and shot shapes reported by the Caddy.## [2.23.0] - 2026-03-16
### Fixed
- **Side Spin Multiplier:** Restored the missing `* 100` multiplier in the `sideSpinRPM` calculation. Poor impact timing will now correctly generate severe hooks and slices.
- **Hole Navigation Reset:** Using the number keys to warp to a new hole now automatically resets the active club to the Driver, resets the shot style to "Full", and clears the putting state.## [2.21.3] - 2026-03-16
### Fixed
- **Auto-Face Pin Navigation:** The engine now dynamically calculates the angle between the ball's current coordinates and the pin. `aimAngle = 0` now always points directly at the hole, fixing the issue where players would shoot parallel to the green on close approach shots.## [2.21.2] - 2026-03-16
### Fixed
- **Rough Distance Reporting:** The Caddy's pre-shot setup report now accurately detects if the ball is in the rough and dynamically announces a distance *range* (60% to 90% of base) instead of a single, misleading maximum distance.
### Added
- **Rough Contact Penalty:** Hitting out of the rough now shrinks the player's Hinge and Impact timing windows by 30% (`lieForgivenessMod`), making it harder to strike the ball cleanly.## [2.21.1] - 2026-03-16
### Changed
- **Keybindings:** Re-mapped the Wind Tier toggle from `Shift + M` to `Shift + W`. Pressing `W` alone reads the current wind; `Shift + W` cycles the intensity tier.
- **Victory Feedback:** The "Hole complete!" announcement now explicitly reports the ball's final proximity to the pin (e.g., "finishing 8 yards from the pin").## [2.21.0] - 2026-03-16
### Added
- **Wind Tiers:** Added four distinct wind intensity levels (Calm, Light Breeze, Gusts, Windy) with defined minimum and maximum speeds.
- **Wind Drifting:** Wind is now generated once at the start of a hole. After each shot, the wind "drifts" within a set variance rather than randomizing completely.
### Changed
- **Keybindings:** Added `Shift + M` to cycle through the wind tiers (State 0 only). Changing the tier instantly regenerates the base wind and announces the new conditions.## [2.18.1] - 2026-03-16
### Fixed
- **2D Geometry Restoration:** Successfully re-implemented `ballX`, `ballY`, `pinX`, and `pinY` tracking.
- **Pythagorean Math:** Replaced linear distance subtraction with true hypotenuse calculation for accurate off-course distance reporting.
- **Dynamic Zones:** Re-integrated power-based timing windows and spin penalties for overswings.
- **Shot Styles:** Restored the expanded 6-style array with specific wind and loft modifiers.## [2.17.1] - 2026-03-16
### Fixed
- **Physics Hotfix:** Successfully injected the v2.17 physics overhaul into the `calculateShot` function, replacing the legacy `calculateZoneAccuracy` logic with the new Dynamic Impact Zones.
- **Spin Math Corrections:** Applied the `distMod` scaling to `backspinRPM` (preventing short chips from spinning backward) and integrated the `spinPenalty` multiplier for overswings into the `sideSpinRPM` calculation.## [2.17.0] - 2026-03-16
### Added
- **Dynamic Impact Zones:** Replaced linear accuracy math with a tiered Zone system (Perfect, Good, Poor, Mishit).
- **Forgiveness Multiplier:** Swing power now directly affects the width of the Impact Zones. Swings under 100% power expand the perfect timing window; overswings drastically shrink it, heavily penalizing players with extreme sidespin.
- **Distance Readout Toggle:** Added a dedicated key (`T`) to announce the current distance to the pin from any state.
### Changed
- **Keybindings:** Fixed the Shot Style toggle to map to `S` instead of `T`. 
- **Spin Physics:** Scaled base backspin calculations by the Shot Style distance modifier (`distMod`), correctly preventing short chips from spinning backward on the green.## [2.16.0] - 2026-03-16
### Added
- **Expanded Shot Styles:** Replaced "Swing Types" with an expanded "Shot Styles" object array. Added "Half Pitch" and "Short Chip" to allow players to hit precise yardages using 100% power swings.
- **Wind Resistance Modifiers:** Short game shots are now highly resistant to wind. Pitches and Chips take 25% wind effect; Flops take 50%.
- **Dynamic Yardage Calculation:** Changing clubs or shot styles now prompts the Caddy to announce the exact total yardage (carry + roll) a 100% power swing will achieve with the current setup.
### Changed
- **Keybindings:** Shot Style toggle moved from `T` to `S`. PageUp/PageDown club selections now include the active Shot Style calculations in the readout.## [2.15.1] - 2026-03-16
    ### Fixed
- **Short Game Physics Hotfix:** Successfully injected the mathematical modifiers for Pitch, Chip, and Flop shots into the `calculateShot` function (which failed to deploy in 2.15.0).
- **Chipping Green Reveal Hotfix:** Updated the post-shot Reveal logic to correctly calculate proximity to the pin, announce the results, and generate a new random target distance when in `chipping` mode.## [2.10.0] - 2026-03-15
### Added
- **Stance Mechanics:** Players can now move the ball forward or backward in their stance across 5 positions (Far Forward, Forward, Neutral, Back, Far Back).
- **Trajectory & Spin Modifiers:** Stance position directly alters `dynamicLoft` (+/- 10 degrees) and `backspinRPM` (+/- 1000 RPM).
- **Keybindings:** Mapped `Home` (move ball forward) and `End` (move ball backward) to control the stance while in the Idle state.
- **Auto-Reset:** Stance automatically resets to Neutral after every completed shot to prevent accidental mishits on the next swing.## [2.9.0] - 2026-03-15
### Added
- **Rotational Aiming:** Players can now rotate their stance using the `Left` and `Right` arrow keys.
- **Precision Modifiers:** Pressing `Shift` + `Left/Right` shifts the aim by 5 degrees at a time, while standard presses shift by 1 degree.
- **Trigonometric Dispersion:** The `lateralX` landing position now incorporates the `aimAngle`. The engine calculates the lateral offset using `Math.tan` based on the total distance of the shot.
- **Aim Reset:** The player's aim automatically resets to 0 (dead straight) after every completed shot.## [2.8.0] - 2026-03-15
### Added
- **Dynamic Wind Physics:** Introduced randomized wind vectors (`windX` for crosswinds, `windY` for headwinds/tailwinds) ranging from -15 to +15 mph.
- **Trajectory Interaction:** Wind effect is multiplied by `hangTimeSecs`. High-loft shots (Wedges, "Fat" strikes) are heavily affected by the wind; low-loft shots (Drivers, "Thin" stingers) pierce through it.
- **Weather Report (Key W):** Mapped the `W` key to announce the current wind speed and direction while in the Idle state.
- **Pre-Shot Randomization:** Wind conditions regenerate automatically after every completed shot.## [2.7.0] - 2026-03-15
### Added
- **Spin Rate Physics (RPM):** Introduced explicit Backspin and Sidespin calculations derived from Hinge and Impact timing.
- **Spin-to-Roll Linkage:** Roll distance is now a direct function of landing velocity and Backspin RPM rather than a flat multiplier.
- **Caddy Report Expansion:** Added "Spin Metrics" to the Caddy Report (Key C) to provide feedback on ball rotation.## [2.6.0] - 2026-03-15
### Changed
- **Polyphonic Audio Overlap:** The flight "whoosh" now persists and fades during the first 300ms of the landing phase, preventing sharp auditory cut-offs.
- **Wider Bounce Spacing:** Increased the base gap and distance-scaling factor to ensure skips remain distinct even on high-velocity shots.
- **Improved Decay Physics:** Updated the timing decay to 0.85 and volume to 0.8 to keep late-sequence bounces audible and rhythmic.
- **Independent Gain Nodes:** Each bounce now utilizes a distinct gain envelope to maximize volume without clipping the master output.## [2.5.0] - 2026-03-15
### Changed
- **Bounce Interval Physics**: Recalibrated the timing between bounces to prevent "clustering." 
- **Dynamic Gaps**: The initial gap between the first and second bounce now scales with `rollDistance`, and the decay factor has been eased to ensure skips are distinct even at high roll speeds.
- **Audio Sequencing**: Refined the transition between the final bounce and the start of the "Roll" rumble to ensure zero auditory overlap.## [2.4.0] - 2026-03-15
### Changed
- **Bounce Density Physics**: Rebalanced the bounce-count formula to better reflect low-trajectory clubs.
- **Trajectory Integration**: Added `dynamicLoft` as a variable in determining bounce frequency. A well-struck Driver (10-12 degrees) now consistently produces 4-5 skips on firm fairways.
- ## [2.3.0] - 2026-03-15

### Added
- **Dynamic Bounce Audio:** Replaced the static landing thud with a dynamic bounce sequence.
- **Trajectory-Based Ground Interaction:** The number of bounces, the timing between them, and their volume now scale mathematically with the `rollDistance`. High-loft clubs (Wedges) produce a single heavy thud, while low-loft clubs (Driver, Woods) produce multiple cascading, skipping bounces before the roll out.## [2.1.0] - 2026-03-15

### Added
- **Golf Bag Array:** Introduced 11 selectable clubs (Driver through Sand Wedge), each with specific base distances, lofts, and roll percentages.
- **Club Selection:** Mapped `PageUp` and `PageDown` to cycle through clubs while in State 0 (Idle), complete with `window.announce` feedback.
- **Flight Phase (State 5):** Added a new state that locks out keyboard input while the ball is "in the air."
- **Trajectory & Roll Physics:** Introduced dynamic loft and roll calculations. Fat/Thin hinge misses now directly impact hang time and roll-out distance.
- **Audio Sequencer:** Replaced instant impact reporting with a sequenced audio event: Strike (triangle wave) -> Flight Wind (white noise) -> Landing (sine thud) -> Roll (brown noise rumble) -> Caddie Reveal.